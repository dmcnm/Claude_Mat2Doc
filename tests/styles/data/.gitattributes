# Byte-exact fixture pin (Gate-4 byte-fixture .gitattributes lesson).
# s0032_document.xml is the frozen python-docx 1.2.0 word/document.xml (1774 B,
# LF newline after the prolog) built through the NOW-UN-STUBBED style-by-name
# delegation chain (Paragraph.style = "Heading 1"/by-object). It MUST be checked
# out byte-for-byte -- a CRLF translation on the master checkout would grow it to
# 1775 B and break the G-scenario byte/SHA pin in Test_p4_7a_styles_api. Pin it
# binary.
s0032_document.xml binary

# P4-7b (M2 milestone) byte-exact fixtures:
# s0033_document.xml is the frozen python-docx 1.2.0 word/document.xml of the M2
# milestone package (1865 B, SHA a71e5502...) -- Document(); add_heading(t0,0);
# add_heading(t1,1); add_heading(t2,2); add_paragraph(p); save(). It carries the
# Title/Heading1/Heading2 pStyle refs + the plain body paragraph and MUST be
# checked out byte-for-byte (a CRLF translation would grow it and break the M2
# milestone byte/SHA pin in Test_p4_7b_latent_m2). Pin it binary.
s0033_document.xml binary

# s0034_styles.xml is the frozen python-docx 1.2.0 word/styles.xml of the latent
# WRITE-path proof (349578 B, SHA 3981d463...) -- the latent default_* / load_count
# setters + add_lsdException + LatentStyle_ setters serialized. It MUST be checked
# out byte-for-byte (the latent-mutation byte pin depends on it). Pin it binary.
s0034_styles.xml binary

# (The s0031/s0035 *_oracle.json oracles are value/serhex + byteproof SHAs and are
#  read in binary mode; jsondecode is line-ending agnostic, so they need no pin --
#  s0028/s0030 precedent. Only the byte-exact XML fixtures are pinned here.)
