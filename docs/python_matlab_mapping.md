---
title: "Python → MATLAB mapping"
---

# Python → MATLAB mapping

Two living reference tables for the Mat2Doc port: **module → package** (where
each python-docx source module's symbols land in the `+mat2doc` namespace) and
**dunder / idiom** (how recurring Python constructs are rendered in base
MATLAB R2024b). Rows are appended per work package; this page starts with
**P1-1** (`docx/shared.py`).

## Module → package

| python-docx module `src/docx/...` | Mat2Doc package / files | WP | notes |
|---|---|---|---|
| `shared.py` — `Length`, `Inches`, `Cm`, `Emu`, `Mm`, `Pt`, `Twips`, `RGBColor` | `+mat2doc\+shared\` (one file per class) + `+shared\private\pyIntArg.m` | P1-1 | `Length < double` (int-subclass degradation); `RGBColor` is a value class (docx home package, not `dml/color.py`). `Twips` present / `Centipoints` absent per docx v1.2.0. `pyIntArg` is project infrastructure (no Python counterpart). |
| `oxml/ns.py` — `nsmap`, `pfxmap`, `nspfxmap`, `nsdecls`, `qn`, `NamespacePrefixedTag` | `+mat2doc\+oxml\` (`nsmap.m`, `pfxmap.m`, `nspfxmap.m`, `nsdecls.m`, `qn.m`, `NamespacePrefixedTag.m`) | P1-2 | `nsmap` is **public** in docx (16 entries, **no** `_nsmap`→`nsmap_` rotation); adds `dgm`/`w14`, rebinds `sl`, drops the pptx-only prefixes. `nspfxmap` is docx's subset function (pptx's `namespaces`/`nsuri` absent). OPC prefixes `ct`/`pr`/`r` live in a separate `opc/oxml.py` map (OPC-layer WP). |
| `oxml/parser.py` — `parse_xml`, `OxmlElement`, `register_element_cls` + `element_class_lookup`, `oxml_parser.makeelement` | `+mat2doc\+oxml\` (`parse_xml.m`, `OxmlElement.m`, `registry.m`, `createElement.m`) | P1-2 | `OxmlElement` is **3-arg** `(nsptag_str, attrs, nsdecls)` (docx delta vs pptx 2-arg); `attrs` is an Nx2 string `[name value]` (Clark keys aren't struct fields). `registry` EMPTY at P1-2 (120-row target lands with the `CT_*` WPs). `createElement` = the makeelement-equivalent (design-realization). |
| lxml `_Element` surface + `etree.fromstring(xml, oxml_parser)` read side (design-realization, **D-001**) | `+mat2doc\+oxml\XmlElement.m`, `XmlParser.m` | P1-2 | Own order-preserving OOXML-subset parser + `_Element` tree node (Heterogeneous root, Sealed eq/ne/deepcopy). `matlab.io.xml.dom` sorts attributes → harness-only, never on the toolbox path. Rejects `<!DOCTYPE>` (**D-006**), ASCII digit char-ref grammar (**D-002**), FIX-1 ws-tail-drop rule. |
| `opc/oxml.py::serialize_part_xml` (byte-matched to lxml `etree.tostring`) | `+mat2doc\+oxml\serialize_part_xml.m` | P1-2 | Placed in `+oxml` though the Python symbol lives in `opc/oxml.py` (XML-layer infra, design.md §3). lxml-matched: single-quote declaration, insertion-order attrs, H7 escaping, verbatim-until-moved nsdecls (**D-serializer-nsdecl**); residual foreign-file prefix-rewrite L2 (**D-nsprefix-rewrite**, case `a03`). |
| `oxml/xmlchemy.py` — `BaseOxmlElement` (tree-ops + `xpath`), `OptionalAttribute` / `RequiredAttribute` descriptor closures | `+mat2doc\+oxml\BaseOxmlElement.m` | P1-3a | `classdef BaseOxmlElement < XmlElement`. Metaclass (`MetaOxmlElement`) generated members replaced by **explicit delegating engine methods** (design.md §2): tree-ops `first_child_found_in`/`insert_element_before`/`remove_all`, attribute engine `getAttrTyped`/`setAttrTyped`/`getAttrRequired`/`setAttrRequired`. **Setter deltas vs pptx** (docx form ported, **no new D#**): **D-delta-1** `value is None OR ==default`→remove; **D-delta-2** post-`to_xml` None→remove; **D-delta-3** post-`to_xml` None→`ValueError`. Attribute engine **dormant** (targets `+oxml\+simpletypes`=P3, no `CT_*` delegates yet). Child-element descriptor engine = **P1-3b**; `xml`/`serialize_for_reading` clean `notYetPorted` stub. |
| `oxml/xmlchemy.py::BaseOxmlElement.xpath` → lxml `_Element.xpath` subset (design-realization, **D-001**) | `+mat2doc\+oxml\evaluate_xpath.m` | P1-3a | Mini XPath-1.0 engine, re-ported from the SOLVED Mat2Ppt `+oxml` evaluator (WP5+WP5-C, no shared code). Closed subset (child paths, `.`/`..`/`//`, wildcard, 1-based positional/attr-eq/existence predicates, `@attr`/`text()` terminals, `ancestor::`/`self::`/`parent::` named tests, group unions); out-of-subset → `mat2doc:XPathError` (**never silently mis-evaluate**, design.md §3). **Gate-2 FIX-1** = the `self::NAME`/`parent::NAME` name-test filter (was ignored — pptx has no such call-sites). **★ VERIFY-2 coverage gap CLOSED by P1-3x** (see next row). Shared xpath deviations (F1/F2/F3/WPC-F1, D10 `text_raw_` bypass) carried `mat2doc:`, no new D#. |
| `oxml/xmlchemy.py::BaseOxmlElement.xpath` — the **docx** `.xpath()` call-site surface the pptx-derived engine did not cover (design-realization, **D-001**) | `+mat2doc\+oxml\evaluate_xpath.m` (+384/−53) | P1-3x | Extends the P1-3a mini-engine to docx's broader surface, each value/byte-verified vs lxml 5.3.0: **bare top-level unions** (`./w:p \| ./w:tbl`, doc-order + identity-dedup H11), **`not(path)`** / union-subpath predicates, **`following-sibling::`/`preceding-sibling::`/`preceding::`** axes (reverse-axis `position()` counts nearest-first, H1), **`position()=n`/`(…)[last()]`/group `(…)[1]`** predicates, **predicate attribute sub-paths** (`w:style[w:name/@w:val="…"]`), and the **`//@attr[n]` terminal** form (per-element positional → typed-empty **string**, lxml encode() convention; the sole member removed from the F2 raise-set). Adopts the shared xpath/parser deviations already namespaced `mat2doc:` — **no new D#**. VERIFY-2 gap closed; R01–R09 pins flipped (`references\p1_3x\flip\flip_types.json`). `decision_2026-07-25_mat2doc_xpath_engine_extension.md`. |
| `oxml/xmlchemy.py` — child-element descriptors `ZeroOrOne` / `ZeroOrMore` / `OneAndOnlyOne` / `OneOrMore` / `Choice` / `ZeroOrOneChoice` (the `_BaseChildElement` family) | `+mat2doc\+oxml\BaseOxmlElement.m` (+1 additive `methods` block, 11 engine members) | P1-3b | The metaclass-generated child accessors (`get.x`/`x_lst`/`_new_x`/`_insert_x`/`_add_x`/`add_x`/`get_or_add_x`/`_remove_x`/`get_or_change_to_x`/`_remove_eg_x`) → **explicit engine methods** `getChild`/`getRequiredChild`/`getChildList`/`newChild`/`insertChildInSequence`/`addChild`/`getOrAddChild`/`removeChild`/`firstChildFoundIn`/`removeChildren`/`getOrChangeToChild` every future `CT_*` member delegates to (design.md §2). **H11 successor-ordering** rides the P1-3a `insert_element_before` (insert before first present successor, else append); H5 get-or-add same-handle; H9 materialized `1x0` list vs H3 `[]`/`InvalidXmlError` tri-state. **D-delta-4** (docx form, **no new D#**): docx `ZeroOrMore` also generates a **public `add_x`** (xmlchemy.py 536 → `_BaseChildElement` 340–352) routing through the same `addChild` — engine-neutral, a **genoxml-scaffolder obligation** (24 docx `ZeroOrMore` sites). `Choice`/`ZeroOrOneChoice` = **dead code in docx v1.2.0** (parity-only, synthetic coverage; no byte-oracle). **Completes xmlchemy.** Fold-forward: package-level L1 lands at the first `CT_*` WP; VERIFY-3 heterogeneous-tree re-verify at first CT registration. |
| `oxml/xmlchemy.py::serialize_for_reading` (the pretty-print engine) + `BaseOxmlElement.xml` (`@property`, un-stubbed) | `+mat2doc\+oxml\serialize_for_reading.m` (new engine) + `+oxml\BaseOxmlElement.m::xml` (un-stub) | P1-4 | New libxml2-style pretty printer (`etree.tostring(..., pretty_print=True)` replacement): 2-space indent per level; formatting turns **off** for mixed-content and **propagates off** through the whole subtree (never re-enabled); one trailing LF after the root incl. the root's tail. **Test-only surface** — kept OFF the L1 byte path; whitespace verified LIVE vs lxml 5.3.0 (13/13 vectors, F1 mixed-content-propagate + F2 root-tail fixes), not frozen as a golden. Un-stubs the P1-3a `BaseOxmlElement.xml` `notYetPorted` stub; `XmlString` relaxed-equality wrapper not ported (test-harness concern). |
| `opc/oxml.py` — two-tier serializer: `CT_Default`/`CT_Override`/`CT_Types` (`[Content_Types].xml`), `CT_Relationship`/`CT_Relationships` (`.rels`), OPC-local `BaseOxmlElement`, `nsmap`/`qn`/`parse_xml`/`serialize_part_xml`/`serialize_for_reading` | `+mat2doc\+opc\+oxml\` (11 files: `CT_*.m`, `BaseOxmlElement.m`, `nsmap.m`, `qn.m`, `parse_xml.m`, `serialize_part_xml.m`, `serialize_for_reading.m`) | P1-4 | **RAW etree `CT_*`** (docx deliberately rawer than pptx — plain `get`/`set`/`find`/`findall`/`append`, **no** `RequiredAttribute`/`OptionalAttribute` descriptors, no simple-type conversion, **no** validation; a missing attr returns `[]` (None, H3), never raises). OPC-local `BaseOxmlElement` extends `mat2doc.oxml.XmlElement` (the `ElementBase` analogue), **not** `mat2doc.oxml.BaseOxmlElement` — so the `CT_*` do **not** inherit `xpath`/`getChild`/`getAttrTyped`. `add_default`/`add_override`/`add_rel` **append verbatim in caller order, never reorder** (`<Default>`-before-`<Override>` + sort are the P1-6a caller's job); return **nothing** (like Python). **TargetMode-when-External** (H3 tri-state): `CT_Relationship.new` writes `TargetMode` **only** when `target_mode == "External"`; internal omits it; the getter defaults to `"Internal"`. **`.xml`-byte-override rotation** (condition B4): Python `CT_Relationships.xml` returns `.rels` bytes → rotated to **`xml_file_bytes`** (MATLAB can't override an inherited property); inherits the base pretty `.xml`; sole byte consumer `Relationships.xml` is **P1-5**. `nsmap`/`qn`/`parse_xml`/`serialize_part_xml`/`serialize_for_reading` are **thin delegators** (one unified parser+registry, condition B2; citation fidelity to the `opc/oxml.py` boundary). Registers 5 `CT_*` in `mat2doc.oxml.registry` by raw Clark name (disjoint `ct`/`pr` vs `w:` — behavior-preserving). **Gate-3 isolated-serializer L1** 4/4 three-way byte-identical (1738/734/1227/295 B). **0 new D#** — adopts D-001 (own parser) + D-serializer-nsdecl (verbatim-until-moved, single-root `xmlns`) + declaration/attr-order/escaping conventions + D-006. |
| `opc/constants.py` — `CONTENT_TYPE` (87) / `RELATIONSHIP_TYPE` (73) / `NAMESPACE` (5) / `RELATIONSHIP_TARGET_MODE` (2) | `+mat2doc\+opc\` (`CONTENT_TYPE.m`, `RELATIONSHIP_TYPE.m`, `NAMESPACE.m`, `RELATIONSHIP_TARGET_MODE.m`) | P1-4 | Python `class X:` with class-level constants → `classdef X … properties (Constant)`. Values transcribed **verbatim**, Python implicit-string-concatenation entries joined into the single literal they denote (incl. the anomalous `RELATIONSHIP_TYPE.PIVOT_CACHE_RECORDS` infix `…/relationships` + `/spreadsheetml/pivotCacheRecords`). Gate-2 mechanical source-diff: 87/87 + 73/73 + 5/5 + 2/2, zero mismatches. |
| `opc/spec.py::default_content_types` — the `(extension, content-type)` **duplicate-key tuple list** | `+mat2doc\+opc\default_content_types.m` (Nx2 `string`, 18 rows) | P1-4 | **CRITICAL: pair-LIST, not a dict.** Several extensions map to >1 content type (`bin` × 3 = printer-settings pptx/xlsx/docx); Python membership is `(ext.lower(), ct) in default_content_types` (BOTH fields), so the port is an Nx2 row-list with a **row test** `any(pairs(:,1)==ext & pairs(:,2)==ct)`, never a key lookup — a `containers.Map`/dict keyed by extension would **collapse** the three `bin` rows and break default-vs-override precedence. Membership is order-insensitive (a set test); row order preserved for readability only. |
| `opc/shared.py` — `CaseInsensitiveDict` (`dict` subclass, case-insensitive keys) / `cls_method_fn` (`getattr(cls, name)`) | `+mat2doc\+opc\` (`CaseInsensitiveDict.m`, `cls_method_fn.m`) | P1-4 | `CaseInsensitiveDict` = a `handle` class (Python subclasses `dict`); the case-fold is confined to `set`/`get`/`isKey` (H15) — stored keys are lowercased (so `keys()` returns them lowercased, what P1-6a's `sorted(keys())` sees), values verbatim; created **empty** (constructor keys unhandled, per docstring). `cls_method_fn(cls, name)` = `str2func(cls + "." + name)` (`cls` a fully-qualified class-name **string**); sole consumer `PartFactory` is **P1-6b** (VERIFY carried: class-name-string vs metaclass currency; `str2func` defers existence check to invocation). |
| `opc/packuri.py` — `PackURI` (str-subclass partname algebra) + module constants `PACKAGE_URI` / `CONTENT_TYPES_URI` | `+mat2doc\+opc\` (`PackURI.m`, `PACKAGE_URI.m`, `CONTENT_TYPES_URI.m`) | P1-5 | `PackURI` is a **value class** wrapping the URI text (`string` can't be subclassed) with `string()`/`char()` + `eq`/`ne` so partnames compare/key like the Python `str` (H2); ctor enforces the leading slash (`mat2doc:ValueError`). `baseURI`/`ext`/`filename`/`membername`/`rels_uri`/`relative_ref`/`from_rel_ref` are pure-string ports of the `posixpath` helpers (`split`/`splitext`/`join`/`abspath`/`relpath`), verbatim-equivalent to the Mat2Ppt PackURI (posix is library-identical). **`idx` uses the docx `[1-9][0-9]*` regex** (packuri.py:18, leading digit 1-9, NO leading zero — DIFFERS from pptx's `[0-9][0-9]*`): `image10.png`→10 but `image01.png`→`[]` (None, H12); three distinct `None` returns → `[]` (H3), value returned as data never index-shifted (H1). Module constants are 0-arg functions returning `PackURI("/")` / `PackURI("/[Content_Types].xml")`. |
| `opc/phys_pkg.py` — `PhysPkgReader`/`PhysPkgWriter` factories + `_DirPkgReader`/`_ZipPkgReader`/`_ZipPkgWriter` + the `java.util.zip` boundary | `+mat2doc\+opc\` (`PhysPkgReader.m`, `PhysPkgWriter.m`, `DirPkgReader_.m`, `ZipPkgReader_.m`, `ZipPkgWriter_.m`, `bytesToJava.m`, `bytesFromJava.m`) | P1-5 | Factory bases are **PUBLIC** in docx (no `_` — contrast pptx's private `_PhysPkgReader`); `__new__` subtype-dispatch → static `factory` (H10): reader path→dir=`DirPkgReader_`/zip=`ZipPkgReader_`/neither=`mat2doc:PackageNotFoundError`, stream(`uint8` bytes)=`ZipPkgReader_`; writer **always** `ZipPkgWriter_` (no dir writer). Concrete readers rotate `_X`→`X_` (design.md §2). **Per-reader error-id fidelity (H3):** `DirPkgReader_.blob_for`→`mat2doc:IOError` / `ZipPkgReader_.blob_for`→`mat2doc:KeyError`, each `rels_xml_for` catches ITS OWN id→`[]`; `content_types_xml` never catches. Reader preloads a `{membername→uint8 blob}` map via in-memory `java.util.zip` (design.md §4 realization of docx's lazy `ZipFile.read`). Writer is **STATEFUL** (ctor open / `write` append one DEFLATED entry / `close` finalize+flush-to-path, `to_bytes()` for streams); **D-zip-time** pins every `ZipEntry.setTime` to `1980-01-01` (adopt-only, byte-identical to the shipped Mat2Ppt writer, incl. the `0x5455` UT extra field — no new D#). `bytesToJava`/`bytesFromJava` = the sole audited Java boundary (`typecast` signed `byte[]`↔`uint8`, H2); **re-ported** (no cross-toolbox sharing, design.md §7). |
| `opc/pkgreader.py` — `PackageReader` + `_SerializedPart` / `_SerializedRelationship` / `_SerializedRelationships` / `_ContentTypeMap` | `+mat2doc\+opc\` (`PackageReader.m`, `SerializedPart_.m`, `SerializedRelationship_.m`, `SerializedRelationships_.m`, `ContentTypeMap_.m`) | P1-6a | The **serialized read path** (M1 byte-critical #2, load side). `PackageReader.from_file` DFS-walks the relationship graph; the three generators (`iter_sparts`/`iter_srels`/`_walk_phys_parts`) → **precomputed struct arrays** (H9), the shared mutable `visited_partnames` list → a **threaded** in/out `visited` arg (global visited-dedup). `iter_sparts` → `1xN` struct `{partname,content_type,reltype,blob}`; `iter_srels` → `1xM` struct `{source_uri,srel}` (package rels first). **V-3 `target_partname` lazy-cache lives in `SerializedRelationship_`** (the ONLY docx v1.2.0 relationship class that caches; `hasattr` → private `target_partname_` + logical `has_target_partname_` flag, NEVER `isempty`; internal → `from_rel_ref(baseURI,target_ref)` once, external → `mat2doc:ValueError`; `rel.py` untouched — `decision_2026-07-25_mat2doc_relcache_not_in_docx_rel.md`). `SerializedRelationships_.load_from_xml` returns an **empty** collection on `[]` (None, H3); `to_array` = the `__iter__` mapping. **`ContentTypeMap_` (reader)**: BOTH `_overrides` and `_defaults` are `CaseInsensitiveDict` → Override-beats-Default precedence with case-**insensitive** partname AND extension match (H15); miss → `mat2doc:KeyError` verbatim; non-`PackURI` key → `mat2doc:KeyError` (VERIFY-3, unreachable-diagnostic, msg `string` vs `<class 'str'>`, id exact, no D#). Underscore rotations `_X`→`X_`, `__getitem__`→`getitem`, `_add_*`→`add_*_`. **Gate-3:** reader round-trip `probe_diff` MATCH on real `default.docx` (13 sparts + 13 srels byte-exact, DFS order). VERIFY-1b hand-off: P1-6b `iter_parts` result MUST be an OBJECT array (not cell). **0 new D#.** |
| `opc/pkgwriter.py` — `PackageWriter` + `_ContentTypesItem` | `+mat2doc\+opc\` (`PackageWriter.m`, `ContentTypesItem_.m`) | P1-6a | The **physical write path** (M1 byte-critical #2, write side). **`PackageWriter.write` (Static)** emits the byte-critical M1 zip order: (0) `[Content_Types].xml` → (1) `/_rels/.rels` → (2..N) each part in the GIVEN `parts` order, each **followed immediately by its `.rels` iff `part.rels.len > 0`** (H4 = `if len(part.rels):`); no-rels parts emit none; `write_parts_` **preserves** the caller's order — NO sort/reorder/dedup (the DFS derivation is `iter_parts`, P1-6b/P1-8). Every value to `phys_writer.write` is `uint8` — no char reaches the zip layer (H2). **`ContentTypesItem_` (writer)**: `from_parts` classifies a part as `<Default>` iff `(ext.lower(),content_type)` is a **row** of `default_content_types` (pair-list, both cols), else `<Override>`; `_defaults` = `CaseInsensitiveDict` (ext folded lower), `_overrides` = **plain `dictionary` keyed by the case-PRESERVED partname** (the deliberate reader/writer case asymmetry vs `ContentTypeMap_`); `blob` serializes a fresh `<Types>` with defaults **sorted-by-ext** then overrides **sorted-by-partname** (sort-at-EMIT, H11; MATLAB `sort`==Python `sorted` for ASCII). Rotations `_ContentTypesItem`→`ContentTypesItem_`, `_add_content_type`→`add_content_type_`, `_element`→`element_`. **Gate-3:** writer full-package `pkgcompare` OVERALL PASS — L0 zip-DFS order + `[Content_Types].xml` **773 B L1** + both `.rels` L1, 8/8 parts byte-identical vs python-docx's own `PackageWriter`. Full 17-part L0 order proof DEFERS to **P1-8**. **0 new D#** (adopts D-zip-time [envelope-only] + D-001/D-serializer-nsdecl via `serialize_part_xml`). |
| `opc/package.py` — `OpcPackage` (open/save/traverse) + `Unmarshaller` (load-time graph builder) | `+mat2doc\+opc\` (`OpcPackage.m`, `Unmarshaller.m`) | P1-6b | The **package/part object model** (M1 byte-critical #3 / headline — joins the P1-6a reader to the P1-6a writer so the full 17-part round-trip runs end-to-end). `OpcPackage.open` → `Unmarshaller.unmarshal(reader, package, @PartFactory.create)`; a **handle** class (live object graph shared by reference, H5). **`iter_parts` returns a HETEROGENEOUS `mat2doc.opc.Part` OBJECT ARRAY, never a cell (VERIFY-1b DISCHARGED)** — the P1-6a `PackageWriter.write_parts_` indexes `parts(k)` and reads `.partname`/`.blob`/`.rels`; possible via `Part < handle & matlab.mixin.Heterogeneous` + Sealed `eq`/`ne` (identity dedup `any(visited==part)`). The two generators → precomputed handle arrays with a **nested-function closure `visited`** (fresh per call, H9): `iter_parts` yields each **part** once (visited gates yield+recursion), `iter_rels` yields **every rel** (external too; visited gates recursion only) — the H5 diamond dedup. `save` **REGENERATES** (does not copy the zip): `before_marshal` loop + `PackageWriter.write`, so `XmlPart` bodies reserialize + base-`Part` bodies stay verbatim (Python evaluates `self.parts` twice, MATLAB captures once — equivalent since docx never overrides `before_marshal`). `rels` `@lazyproperty` cache+logical-flag (never `isempty` sentinel); `open` `@classmethod` `cls()` → hard-constructs `OpcPackage` (P1-8 `Package` supplies its own `open`). Underscore rotations `_rels`→`rels_cache_`/`rels_computed_`, `_core_properties_part`→`core_properties_part_`, `_unmarshal_parts`→`unmarshal_parts_`. Parts map is an **insertion-ordered `{keys,vals}` struct** (NOT `containers.Map`, H11). **Gate-3:** the 17-part M1 open→save sweep `pkgcompare` **OVERALL PASS** three-way (MATLAB≡python `OpcPackage.open→save`≡`s0001 Document().save`). **0 new D#.** |
| `opc/part.py` — `Part` (base + verbatim-blob default), `XmlPart` (parse/reserialize), `PartFactory` (content-type→class registry) + the registration block in `docx/__init__.py` (37-51) | `+mat2doc\+opc\` (`Part.m`, `XmlPart.m`, `PartFactory.m`) | P1-6b | **`Part`** — base part + the default **verbatim-blob** class (`blob` returns `self._blob or b""` → `uint8` verbatim; the passthrough path keeping images/thumbnails/unregistered-XML byte-stable). **HETEROGENEOUS root** `Part < handle & matlab.mixin.Heterogeneous`, **Sealed** `eq`/`ne` (VERIFY-1b/H5). Every Python `@property` (`blob`/`content_type`/`package`/`partname`/`related_parts`/`rels`) → a **zero-arg method** so `XmlPart` can OVERRIDE `blob` (MATLAB can't override an inherited property accessor). Ctor arg order is the **docx** `(partname, content_type, blob=None, package=None)` — **blob BEFORE package** (pptx was `(…,package,blob)`); no `set_blob` (docx `blob` is getter-only). `set_partname` isinstance→`mat2doc:TypeError` (verbatim msg, bare class name). **`XmlPart`** — parses its blob to an `XmlElement` tree ONCE at `load`, RE-SERIALIZES on `blob` via `mat2doc.opc.oxml.serialize_part_xml` (**the whitespace-collapse path** — `remove_blank_text` strips indentation, reserialize collapses → L1); `element()` twice = same handle, `blob()` re-serialized on demand + stable (H9 currency). **`PartFactory.create`** — explicit switch/registry (H10, no `__new__`): selector (reltype `IMAGE`→image part) → content-type registry `part_type_for_` (ordered Nx2, mirrors `docx/__init__.py` 44-51) → default base `Part`; returns `PartClass.load(…)` via `cls_method_fn` (`getattr(cls,name)` analogue; `cls` = class-name **string**, resolves the P1-5 VERIFY). **M1 stand-in split** (Finding 1, `references\m1_skeleton_target.md`): all 8 registered CT → base `mat2doc.opc.XmlPart` (reserialize/collapse), IMAGE selector + everything unregistered → base `mat2doc.opc.Part` (verbatim); **P2 refines** each row to its feature subclass — bytes identical (same inherited `blob`), only the reloaded TYPE changes. Underscore rotations `_rel_ref_count`→`rel_ref_count_`, `_part_cls_for`→`part_cls_for_`. `drop_rel` delete branch = `mat2doc:notYetPorted` stub (`Relationships` delitem is P2; UNREACHABLE at M1). **Finding 1 (task #60, M1-UNREACHABLE):** `rel_ref_count_` ports `xpath("//@r:id")` but the merged `XmlElement` fallback lacks `xpath` — P2 DocumentPart un-stub gate item. **Gate-3:** dispatch matrix 8 registered→XmlPart / 8 unregistered→Part; XmlPart reserialize-collapse + base-Part verbatim byte-proven in the 17-part sweep. **0 new D#.** |
| `opc/rel.py` — `Relationships` (`Dict[str,_Relationship]` collection) + `_Relationship` (value object) | `+mat2doc\+opc\` (`Relationships.m`, `Relationship_.m`) | P1-5 | `Relationships` holds relationships in **insertion-ordered** parallel arrays keyed by rId; `add_relationship`/`get_or_add`/`get_or_add_ext_rel`/`part_with_reltype`/`related_parts`/`xml` + the inherited dict surface (`contains`/`getitem`/`keys`/`values`/`len`), plus private `get_matching_`/`get_rel_of_type_`/`next_rId_`. **`xml` emits `<Relationship>` in INSERTION order — NO rId sort (H11 docx-vs-pptx divergence)**; `_next_rId` reuses the lowest gap (`{rId3,rId1}`→`rId2`); routes through the P1-4 `xml_file_bytes` B4 rotation (sole consumer). `related_parts` = `dictionary(string→cell)`, internal targets only, a **value snapshot** (V-1: no P1-6+ consumer may cache it). `Relationship_` is **NON-caching** — every accessor a plain `@property`, `target_ref` recomputed each access (faithful to docx v1.2.0; the `@lazyproperty`/`target_partname` cache is pptx-only and belongs in `pkgreader.py` @ P1-6a — `decision_2026-07-25_mat2doc_relcache_not_in_docx_rel.md`); `target_part` raises `mat2doc:ValueError` when external (H5 handle-identity vs external string `==` in `get_matching_`). Underscore rotation `_Relationship`→`Relationship_`; `add_relationship` PUBLIC (no rotation). |

| `oxml/coreprops.py` — `CT_CoreProperties` (the `<cp:coreProperties>` element: 15 `ZeroOrOne` Dublin-Core descriptors, W3CDTF parse/format, the revision int reader/writer) | `+mat2doc\+oxml\+coreprops\CT_CoreProperties.m` + 1 `registry.m` main-map row (`cp:coreProperties → CT_CoreProperties`, `oxml/__init__.py:96`) | P1-7 | Near-direct re-port of the shared OPC core-properties lineage (docx `coreprops.py` is line-for-line the pptx one). 15 descriptors all `successors=()` (append) → Constant `TAG` table + delegating members over the P1-3b child engine. **W3CDTF grammar** = the strptime-faithful Mat2Ppt Gate-2 F2 grammar (four anchored templates, last-match-wins, component round-trip + `year>=1` floor); **inverted-sign offset** preserved (`-08:00`→`+8h`). **Date getters return NAIVE datetime** (`TimeZone=''`) vs Python tz-aware UTC — API-value only, bytes unaffected (VERIFY-tz, wall-clock compared). **`xsi` hoist** (created/modified): the lxml reconciliation trick → its documented intent `set_nsdecl_("xsi", …)` on the root (once, trailing `cp,dc,dcterms,xsi`) — **D-serializer-nsdecl**, byte-verified. **`revision` D-002** ASCII `[0-9]` grammar (absent→0, `str(None)`→0, exotic→0/nearest-double, never NaN/Inf). String setters length-limit to 255 **code points** (H2) and route through `pyStr` (H14, F2). Gate-3: **721 B re-serialize three-way + 681 B fresh-build byte-identical, 0 new D#.** |
| `opc/coreprops.py` — `CoreProperties` (15 `@property` pairs) | `+mat2doc\+opc\CoreProperties.m` | P1-7 | `handle` proxy wrapping a `CT_CoreProperties` element (shared-tree reference semantics, design.md §2); the 15 read/write accessors (`author`…`version`) are `Dependent` properties delegating 1:1 to the element's `*_text` / `*_datetime` / `revision_number`. Deviations apply transitively (D-002 on `revision`/dates, D-serializer-nsdecl on created/modified). |
| `opc/parts/coreprops.py` — `CorePropertiesPart` (+ `default()`) | `+mat2doc\+opc\+parts\CorePropertiesPart.m` | P1-7 | `XmlPart` subclass for `/docProps/core.xml`; parses on load, re-serializes on save. **docx ctor arg order** `(partname, content_type, element, package)` — element third. Own pass-through ctor + own static `load` (MATLAB neither inherits ctors nor dispatches inherited statics to the subclass — faithful to Python's `cls`-bound `XmlPart.load`). `default()` builds a fresh part with docx literals (title `"Word Document"`, last_modified_by `"python-docx"`, revision 1) + wall-clock `modified` (**D-coreprops-time**). **Both `default()` and the subclass are M1-UNREACHABLE** (default.docx HAS a core.xml; `PartFactory` maps `OPC_CORE_PROPERTIES`→base `XmlPart` at M1 — the part-class registry is a byte-neutral P2 refinement; only the element registry row is wired at P1-7). |
| `shared.py`-adjacent — `pyStr` (Python `str()` numeric formatting, H14) | `+mat2doc\+shared\pyStr.m` | P1-7 | **Mat2Doc infrastructure, no python-docx counterpart** — the mandated numeric→XML formatting helper (design.md §8 / H14; raw `num2str`/`sprintf('%g')` at a serialization site is a defect). Faithful re-port of Mat2Ppt `+util\pyStr.m` (verified code-identical modulo namespaces, 20/20 vs CPython `str()`; Python float shortest-round-trip repr `2.0`→`"2.0"`, `1/3`→`"0.333…"`, `inf`/`nan`). **First numeric-serialization site in the docx port** — established here for the `<cp:revision>` int; F2 also routes the string setters through it. |

| `api.py` — `Document` (public entry function) + `_default_docx_path` | `+mat2doc\Document.m` (entry **function** `mat2doc.Document`) | P1-8 | Ported **in full** (api.py is finished at P1-8; P2-3 never reopens it). The Python one-liner `Package.open(docx).main_document_part` is split into two statements (MATLAB can't chain a method on a static-call result). **STRICT public-entry None test** `strcmp(class(docx),'double') && isequal(size(docx),[0,0])` — a bare `isequal(docx,[])` is true for the `0×0` char `''`, which python-docx treats as a real path → `PackageNotFoundError` (H3, Gate-2 F1; `decision_2026-07-26_mat2doc_none_idiom.md`). `_default_docx_path` resolves **package-relative** via `fileparts(mfilename('fullpath'))` (not pwd). Content-type guard → verbatim `mat2doc:ValueError` (`file '%s' is not a Word file, content type is '%s'`). This entry **function** and the `mat2doc.document.Document` proxy **class** do not collide (mirrors `docx.api.Document` vs `docx.document.Document`, design.md §1). |
| `package.py` — `Package(OpcPackage)` (WML package: `after_unmarshal`/`_gather_image_parts` live, `get_or_add_image_part`/`image_parts` P7) | `+mat2doc\+package\Package.m` | P1-8 | `classdef Package < mat2doc.opc.OpcPackage` (handle). **Own static `open`** overrides the inherited `OpcPackage.open` (MATLAB statics are not polymorphic in `cls`; the base would construct a base `OpcPackage` and mis-set every part's package back-reference) — the faithful `Package.open` = inherited-`open`-with-`cls=Package`. `after_unmarshal` overrides the base no-op **without forwarding to super** (matching Python) → `gather_image_parts_` **LIVE on every open** (a no-op on `default.docx`: the `reltype ~= RT.IMAGE` guard short-circuits before `image_parts`). `image_parts` / `get_or_add_image_part` are `mat2doc:notYetPorted` **P7** stubs (unreachable on a plain open→save). Underscore rotation `_gather_image_parts`→`gather_image_parts_`. |
| `parts/document.py` — `DocumentPart` (M1 slice: `document`/`save`/`core_properties` live; styles/settings/numbering/comments/header-footer/style-lookup stubbed) | `+mat2doc\+parts\DocumentPart.m` | P1-8 | `classdef DocumentPart < mat2doc.opc.XmlPart` — parses on load, re-serializes on save (inherits `blob` unchanged → the `WML_DOCUMENT_MAIN`→`DocumentPart` PartFactory flip is **byte-neutral**). **Own constructor + own static `load`** (the inherited-static trap — without its own `load` the factory silently builds a base `XmlPart` and `.document` is missing). docx arg order `(partname, content_type, element, package)` — element third. `document` is a **plain `@property`** → a **fresh** `mat2doc.document.Document` each access (non-cached, two reads = distinct handles, H5). `core_properties` LIVE (P1-7). **VERIFY-M1-DOCPART-BASE:** docx `DocumentPart < StoryPart < BaseStoryPart < XmlPart`; the StoryPart tier is inserted above this class at **P2-2** (byte-neutral, methods-only). Feature accessors → `mat2doc:notYetPorted` stubs (P2/P2-2). |
| `document.py` — `Document(ElementProxy)` (M1 slice: `save`/`core_properties`/`part` live; content members stubbed) | `+mat2doc\+document\Document.m` | P1-8 | `classdef Document < handle` — the top API **proxy** (not constructed directly; use `mat2doc.Document(...)`). At M1 only `save` (→ `part.save`, the headline path), `core_properties` (→ `part.core_properties`, P1-7 real), and the trivial `part` accessor are live; every content member (`add_paragraph`/`add_heading`/`add_page_break`/`paragraphs`/`iter_inner_content`/`add_table`/`tables`/`add_section`/`sections`/`add_comment`/`comments`/`inline_shapes`/`settings`/`styles`/`add_picture`) → `mat2doc:notYetPorted` stub (P2-3 content / P2 / P7). **VERIFY-M1-DOC-BASE:** docx `Document` extends `ElementProxy` (element identity + `element`/`part` accessors); `ElementProxy`/`Parented` are P2-1 — until then this class derives `handle` only and stores `element_`/`part_` directly (**P2-1** retrofits the real base, byte-neutral — Mat2Ppt VERIFY-M1-C precedent). Underscore rotation `_element`/`_part`→`element_`/`part_`; **save-to-stream** noted, not stubbed (inherited from the `OpcPackage.save` contract; hardening is P2-3). |
| `shared.py` — `ElementProxy`, `Parented`, `StoryChild`, `TextAccumulator`, `lazyproperty`, `write_only_property` (the proxy base tier) | `+mat2doc\+shared\` (one file per symbol) | P2-1 | The **base tier every later API proxy extends** (P3–P6). `ElementProxy < handle` folds docx's optional `parent` + `part` accessor into the base (pptx kept them on a sibling); H5 element-identity `eq`/`ne` (`isa`-guarded, delegating to `XmlElement`'s Sealed handle `eq` — distinct byte-identical elements are NOT equal); `part` None-guard → `mat2doc:ValueError`. `Parented`/`StoryChild` are **standalone** non-proxy part-provider `handle` bases (no element, no `eq`/`ne`; two distinct classes preserve the docx hierarchy — block items derive `StoryChild`). `TextAccumulator` is a faithful new port (generator `pop`→`(1,:)` string, H9). `lazyproperty`/`write_only_property` are **non-callable idiom guides** (no `+util`; docx's home is `shared.py`) mandating the Dependent + private-cache + **logical computed-flag** pattern (never `isempty` sentinel). **`+document\Document.m` retrofitted** onto `mat2doc.shared.ElementProxy` (VERIFY-M1-DOC-BASE) — byte-neutral (17/17 M1 sweep), makes `Document ==` Python-faithful (VERIFY-1, ratified). **0 new D#.** |
| `exceptions.py` — `PythonDocxError`, `InvalidSpanError`, `InvalidXmlError` | `+mat2doc\+exc\` (function raisers) | P2-1 | Each Python exception **class** → a flat MATLAB error **identifier** `mat2doc:<Name>` (design.md §2; Mat2Ppt `+exc` precedent), message via `"%s"` (`%`-literal safe). The Python `issubclass` hierarchy **flattens** to independent identifiers (VERIFY-2, ratified) — INERT: the clone has **zero** internal `except` sites for any of the three (raise-only). `PackageNotFoundError` is NOT here (lives in `opc/exceptions.py`, already under `+opc`). |
| `types.py` — `ProvidesXmlPart`, `ProvidesStoryPart` (typing.Protocols) | `+mat2doc\+types\` (documentation-only `(Abstract)` classes) | P2-1 | Structural/duck-typed contracts → MATLAB `(Abstract) < handle` classes declaring the `part` Abstract method for `help`/class-browser discoverability (VERIFY-3, ratified). **Nothing inherits from them and nothing `isa`-gates on them**; any object with a `part()` method satisfies the contract structurally (e.g. `mat2doc.opc.Part`). |
| `parts/story.py` — `StoryPart(XmlPart)` (`next_id`, `_document_part`, `get_style`/`get_style_id`, `get_or_add_image`, `new_pic_inline`) | `+mat2doc\+parts\StoryPart.m` | P2-2 | The real base **inserted above `DocumentPart`** (`DocumentPart < StoryPart < XmlPart`) — discharges VERIFY-M1-DOCPART-BASE, **byte-neutral** (adds methods only). **NAME RATIFIED:** v1.2.0 has `StoryPart`, **no `BaseStoryPart`** anywhere (the M1 header + the P2-2 brief were wrong); ported source-exact per design.md §1. `next_id` **LIVE** (H1 — data arithmetic on `//@id` VALUES, `max+1`/`1`, gaps not filled; reachable via the #60 xpath hoist; `str.isdigit()` realized ASCII `[0-9]`, D-002-family note, no new D#). `_document_part` `@lazyproperty` (logical-flag cache) → `package.main_document_part` (same handle). `get_style`/`get_style_id` **live delegation** to `_document_part` (surfaces the **P4-7** feature stub). `get_or_add_image`/`new_pic_inline` → **P7** feature stubs (off the open/save path). |
| `parts/document.py` — `DocumentPart(StoryPart)` (P2-2 graph un-stub: `_styles_part`/`_settings_part` live) | `+mat2doc\+parts\DocumentPart.m` (reparented) | P2-2 | Reparented to `StoryPart` (**byte-neutral** superclass insertion). Object-graph accessors go **LIVE**: `_styles_part` (156-169) → real `StylesPart`, `_settings_part` (142-154) → real `SettingsPart` — plain `@property` (NOT lazyproperty), so each call runs `part_related_by` and returns the **same** loaded handle for a package that HAS the part (H5/H9), materialize-once when absent; **KeyError-only catch** (a `ValueError` duplicate-rel propagates, H3). `document`/`save`/`core_properties` stay M1-live. **Feature surface still stubbed at its real-phase owner** (FLAG-B relabels): `styles`/`get_style`/`get_style_id`→P4-7, `settings`→P5-1, `numbering_part`→P8-1, `comments`/`_comments_part`→P8-2, header/footer parts→P5-3b, `inline_shapes`→P7. The **object-graph-vs-feature-surface** split is the P2-2 thesis. |
| `parts/styles.py` — `StylesPart(XmlPart)` (`default`/`_default_styles_xml`, `styles`) | `+mat2doc\+parts\StylesPart.m` + `+mat2doc\templates\default-styles.xml` | P2-2 | A pure `XmlPart` **shell**: inherits `blob`/`element` unchanged, declares its **own static `load`** (the inherited-static trap — else the flip is inert). `default(package)` builds from the byte-identical template (**NOT** on the M1 path — default.docx ships `styles.xml`, validated at P4-7). `styles` proxy accessor → **P4-7** feature stub. Target of the byte-neutral `WML_STYLES → StylesPart` flip. |
| `parts/settings.py` — `SettingsPart(XmlPart)` (custom `__init__` storing `self._settings`, `default`/`_default_settings_xml`, `settings`) | `+mat2doc\+parts\SettingsPart.m` + `+mat2doc\templates\default-settings.xml` | P2-2 | `XmlPart` shell with a **faithful custom ctor** (settings.py 26 `self._settings = element` → private `settings_` cache, dead until P5-1). Own static `load`; `default(package)` from the byte-identical template (off the M1 path). `settings` proxy accessor → **P5-1** feature stub. Target of the byte-neutral `WML_SETTINGS → SettingsPart` flip. |
| `parts/numbering.py` — `NumberingPart(XmlPart)` (`new` = `raise NotImplementedError`, `numbering_definitions`) | `+mat2doc\+parts\NumberingPart.m` | P2-2 | `XmlPart` shell; own static `load` for the byte-neutral `WML_NUMBERING → NumberingPart` flip. `new()` raises `mat2doc:NotImplementedError` — **faithful upstream behavior, NOT a port stub** (numbering.py 11-14 is `raise NotImplementedError`); its only caller (`DocumentPart.numbering_part`) is a P8-1 feature stub, so it is unreached. `numbering_definitions` → **P8-1** feature stub (needs the `CT_Numbering` `num_lst` descriptor). |
| **task #60** — hoist `BaseOxmlElement.xpath` onto the `_Element` analogue (`oxml/xmlchemy.py:687-692`); `Relationships.__delitem__` (inherited `dict`, `opc/rel.py`); `Part.drop_rel` (`opc/part.py:75-82`) made live | `+mat2doc\+oxml\XmlElement.m::xpath` (hoisted; `+oxml\BaseOxmlElement.m` inherits) + `+opc\Relationships.m::delitem` + `+opc\Part.m::drop_rel` | P2-2 | **xpath hoist = PURE MOVE:** the P1-3 wrapper body relocated verbatim from `BaseOxmlElement` to `XmlElement` (only the `arguments` context type widened), so a plain **unregistered** parsed root (e.g. `w:document` before P2-3) evaluates `//@id`/`//@r:id` — the `StoryPart.next_id` + `XmlPart.rel_ref_count_` consumers; `BaseOxmlElement` inherits it (single `DefiningClass`), **byte-neutral** (Test_p1_3a/3b/3x green). **`delitem`** = inherited `dict.__delitem__` (prunes `rIds_`/`rels_`; **KeyError** if absent; **faithfully does NOT prune `target_parts_by_rId_`** → a stale `related_parts` survives a drop, H3/H11). **`drop_rel`** LIVE (was a P1-6b stub): `if rel_ref_count_(rId) < 2: rels().delitem(rId)` — threshold **`< 2`**, so refcount 2→KEPT, 1→dropped, 0-present→dropped, absent→KeyError (corrects the brief's count-0 hypothesis). |

| `oxml/document.py` — `CT_Document` (`w:document`) + `CT_Body` (`w:body`) | `+mat2doc\+oxml\+document\CT_Document.m` / `CT_Body.m` + 2 `registry.m` main-map rows (`w:body`→`CT_Body` `__init__.py:100`, `w:document`→`CT_Document` `__init__.py:101`) | P2-3 | **THE P2 byte-risk — proven byte-neutral.** Both `< mat2doc.oxml.BaseOxmlElement`, so `document.xml` now parses to `CT_Document`/`CT_Body` (descriptor machinery) instead of generic `XmlElement` and reserializes through the `BaseOxmlElement` path — **zero bytes moved** (no serialization override; the 17/17 M1 sweep incl. `document.xml` 1548 B byte-identical). `CT_Document`: `ZeroOrOne("w:body")` member family + `sectPr_lst` union property (`./w:body/w:p/w:pPr/w:sectPr \| ./w:body/w:sectPr`, doc-order, body-level last). `CT_Body`: `p`/`tbl` = `ZeroOrMore(successors=("w:sectPr",))` (docx D-delta-4 public `add_p`/`add_tbl`), `sectPr` = `ZeroOrOne(successors=())`; **H11** — new `p`/`tbl` insert BEFORE `sectPr`, fresh `sectPr` appends last (sentinel); `clear_content` (`./*[not(self::w:sectPr)]`, preserves sectPr), `inner_content_elements` (`./w:p \| ./w:tbl`, `w:ins/w:p` excluded). `add_section_break` → **P5** stub. Forward deps `CT_P`/`CT_Tbl`/`CT_SectPr` (P4/P6/P5) resolve generic `XmlElement` (byte-neutral, scope-driven). Main-map now **3/120**. |
| `document.py` — `Document` fleshed (`_body`/`_block_width` LIVE; all content members stubbed at their P4/P5/P6/P7/P8 owner) | `+mat2doc\+document\Document.m` | P2-3 | `_body` → `body_()` returns a cached `mat2doc.document.Body_` via the **manual `__body is None` cache** (`body__ = []` + `isequal(obj.body__,[])`, faithful to document.py 244-246, NOT a lazyproperty) — repeated access = same handle (H5). `_block_width` → `block_width_()` ported faithfully (`page_width or Inches(8.5)` H4 truthiness → `isequal(x,[])\|\|x==0`; `sections[-1]` → `section(end)`) but calls `sections()` first → raises the **P5** stub before any `Section` member resolves (dead until P5; only caller `add_table` is a P6 stub). Every content adder/reader stays `mat2doc:notYetPorted`, each naming its real-phase owner: `add_heading`/`add_paragraph`/`add_page_break`/`paragraphs`/`iter_inner_content`→**P4**, `add_table`/`tables`→**P6**, `add_section`/`sections`→**P5**, `add_picture`/`inline_shapes`→**P7**, `add_comment`/`comments`→**P8-2**, `settings`→**P5-1**, `styles`→**P4-7**. On the real `ElementProxy` base since P2-1. |
| `document.py` — `_Body` (block-item container proxy over `<w:body>`) | `+mat2doc\+document\Body_.m` | P2-3 | `Body_ < mat2doc.BlockItemContainer < mat2doc.shared.StoryChild` (`_Body`→`Body_` rotation). Ctor stores the `CT_Body` element again in `_body`→`body_` (faithful redundant store, document.py 255-257); `clear_content` overrides to delegate to the **LIVE** `CT_Body.clear_content`. Inherits the block-item surface (`add_paragraph`/`add_table`/`paragraphs`/`tables`/`iter_inner_content` — all P4/P6 stubs) + the parent→part chain. |
| `blkcntnr.py` (TOP-LEVEL module) — `BlockItemContainer` | `+mat2doc\BlockItemContainer.m` (package **root**) | P2-3 | Top-level module → lands at the `+mat2doc` package root (`mat2doc.BlockItemContainer`, mirroring `from docx.blkcntnr import BlockItemContainer`); does NOT collide with the `+mat2doc\Document.m` entry function. `< mat2doc.shared.StoryChild`. LIVE: the container wiring (ctor stores `_element`→`element_`, delegates parent to `StoryChild`). STUB: `add_paragraph`/`add_paragraph_`/`add_table`/`paragraphs`/`tables`/`iter_inner_content` — each raises **exactly at the item-construction boundary** (`blkcntnr.py:99-101` = `Paragraph(self._element.add_p(), self)`; `add_p()` is live but `Paragraph`→**P4**, `Table`→**P6**, so the whole method raises up front to avoid a stray `<w:p>`). This is the **canonical** stub location for the block-item adders; the `Document` feature adders stub above it (both layers stub → no live path reaches an unported dep). |

| `oxml/simpletypes.py` — 4 Base (`BaseSimpleType`/`BaseIntType`/`BaseStringType`/`BaseStringEnumerationType`) + 10 Xsd (`Xsd*`) + 21 `ST_*` attribute-value validators | `+mat2doc\+oxml\+simpletypes\` (35 class `.m` + `private\intFromXml.m` / `private\pyRound.m`) | P3-2 | The **value layer beneath the element tree** — validates/converts every typed `w:` attribute the P4–P6 `CT_*` classes read/write, via the `BaseOxmlElement.getAttrTyped`/`setAttrTyped` engine; **emits no package bytes.** Four-method surface `convert_from_xml`/`convert_to_xml`/`validate` + the `from_xml`/`to_xml` templates. **H10 re-declaration:** Python `from_xml`/`to_xml` `@classmethod`s late-bind `cls.convert_*`/`cls.validate`; MATLAB statics do not, so `from_xml` is re-declared on every `convert_from_xml`-overrider (10) and `to_xml` on every `validate`/`convert_to_xml`-overrider — templates hosted on the branch bases `BaseIntType`/`BaseStringType`, NOT on `BaseSimpleType` (the dead-abstract-base path: `BaseSimpleType.from_xml("42")` raises, never returns `int(42)`). **docx specifics:** **D-STYPE-2 re-home** — docx has **NO `BaseFloatType`** (37 files, none created); the only `float()` is `ST_UniversalMeasure.convert_from_xml` (:415, reused by `ST_SignedTwipsMeasure` :366), realized via `str2double` (D-STYPE-2/D-002 lexical fold). **`ST_HexColor`/`ST_HexColorAuto` split** (pptx had one `ST_HexColorRGB`) — `ST_HexColor` returns `RGBColor`-or-`"auto"`, `ST_HexColorAuto` is a one-member enum supplying `"auto"`; hex via `RGBColor.from_string` `[0-9A-Fa-f]` (D-STYPE-4, no length guard). **`ST_OnOff`** (docx-novel) — `{'1','0','true','false','on','off'}`, truthy `{'1','true','on'}`, else **InvalidXmlError**. **`ST_UniversalMeasure`** unit table mm 36000/cm 360000/in 914400/pt 12700/pc 152400/pi 152400 → `Emu(pyRound(quantity*mult))`, unknown unit → `mat2doc:KeyError`. **Banker's rounding** (`pyRound`, round-half-to-even) at the two `int(round(...))` sites — MATLAB `round` is away-from-zero (H6 defect). **Raise-map:** `mat2doc:InvalidXmlError` (XsdBoolean :116 / ST_OnOff :340 — the only two `docx.exceptions` sites), `mat2doc:ValueError` (membership/range/hex/parse), `mat2doc:TypeError` (validate_int/_string, XsdBoolean.validate, ST_DateTime.validate), `mat2doc:KeyError` (unknown unit). **`ST_DateTime` fromisoformat under-accept** — narrow subset; exotic schema-invalid inputs → 1970 epoch; unreachable (only consumer `w:comment/@w:date` is P8-2); final ruling deferred (`decision_2026-07-26_st_datetime_underaccept.md`). **Gate-3 175/175 probe facts** (154 byte-identical + 21 adopted-ruling divergences); Gate-2 **F1** XsdBoolean render fix. **0 new D-numbers** (adopt-verify D-STYPE-1..4/D-002/D-004/D-005). |
| `enum/base.py` — `BaseEnum` (MS-API-value enums) + `BaseXmlEnum` (adds `xml_value` + `from_xml`/`to_xml`) | `+mat2doc\+enum\+base\` (`BaseEnum.m`, `BaseXmlEnum.m`) | P3-1 | The **base machinery every docx enum extends** (P3-3/P3-4 concrete `WD_*` + P4/P5/P6 attribute (de)serializers). **Value classdefs** (NOT handle — enum members are value objects): a MATLAB `enumeration` class that subclasses a numeric builtin cannot add properties and statics have no `cls` binding, so the base owns `value`/`doc` (+ `xml_value`) props + a pass-through ctor, and shared `from_xml_`/`to_xml_` are **Hidden statics taking `fullClassName`**; each concrete enum declares an `enumeration` block passing `(ms_api_value,[xml_value,]docstr)` and exposes thin `from_xml`/`to_xml` forwarders with its own name hardcoded (no metaclass, no runtime dispatch). Member-by-value lookup `cls(int)` → `find(double([members.value])==v,1)` (first-declared-wins = Python alias resolution). **4 docx-vs-pptx deltas** (do NOT copy the Mat2Ppt `BaseXmlEnum` verbatim): **(1)** `from_xml` has **NO None/empty short-circuit** (docx delta — LOAD-BEARING: `WD_COLOR_INDEX.INHERITED`/`WD_UNDERLINE.INHERITED` carry `xml_value=None`; `from_xml(None)`→the None member, `from_xml("")`→the `""` member); **(2)** `to_xml` guard is `if not xml_value` (Python falsy — rejects None **and** `""`) → `ismissing(xv)\|\|strlength(xv)==0`; **(3)** **NO `validate()`** classmethod (pptx has one); **(4)** message forms are str-interpolated (docx-authored) under `mat2doc:ValueError` (**D-005**), the stdlib `cls(value)` message keeps CPython `%r` quotes (Gate-2 **F1**). **H3 tri-state** central: Python None→`<missing>`, `""`→real `""`, distinct (`asXmlVal_`/`xmlEq_`). `int(member)`→`double(member.value)` (no `==int` override, VERIFY-P3-1b→P4+). `DocsPageFormatter` **NOT ported** (docs-tooling, VERIFY-E4). **Gate-3 34/34 probe facts byte-identical, 0 new D#.** |
| `enum/text.py` — `WD_PARAGRAPH_ALIGNMENT` / `WD_BREAK_TYPE` / `WD_COLOR_INDEX` / `WD_LINE_SPACING` / `WD_TAB_ALIGNMENT` / `WD_TAB_LEADER` / `WD_UNDERLINE` + module aliases `WD_ALIGN_PARAGRAPH` / `WD_BREAK` / `WD_COLOR` | `+mat2doc\+enum\+text\` (10 files) | P3-3 | Concrete enums on the P3-1 base — **per-module subpackage** (`mat2doc.enum.text.*`), differs from Mat2Ppt's flat `+enum\`. 8 `BaseXmlEnum` (62 members) + **`WD_BREAK_TYPE`** = a **plain value classdef** (Python `enum.Enum`, no `xml_value`; PROG_ID precedent) whose **`TEXT_WRAPPING` is a `Constant` pointing at the canonical `LINE_CLEAR_ALL` member** (both value 11 — member-alias idiom, see dunder row). `WD_COLOR_INDEX.INHERITED` / `WD_UNDERLINE.INHERITED` carry `xml_value=None` (`<missing>`, H3) → reachable via `from_xml([])` on the P3-1 no-None-guard scan; `to_xml(INHERITED)` raises `…has no XML representation`. `WD_LINE_SPACING.{SINGLE,ONE_POINT_FIVE,DOUBLE}` carry the truthy placeholder `"UNMAPPED"` (not None, not `""`). |
| `enum/section.py` — `WD_HEADER_FOOTER_INDEX` / `WD_ORIENTATION` / `WD_SECTION_START` + module aliases `WD_HEADER_FOOTER` / `WD_ORIENT` / `WD_SECTION` | `+mat2doc\+enum\+section\` (6 files) | P3-3 | 3 `BaseXmlEnum` (10 members) + 3 module aliases. `mat2doc.enum.section.*`. Straightforward token round-trips (`default`/`first`/`even`; `portrait`/`landscape`; `continuous`/`nextColumn`/`nextPage`/`evenPage`/`oddPage`). |
| `enum/dml.py` — `MSO_COLOR_TYPE` (`BaseEnum`, no XML) + `MSO_THEME_COLOR_INDEX` (`BaseXmlEnum`) + module alias `MSO_THEME_COLOR` | `+mat2doc\+enum\+dml\` (3 files) | P3-3 | `mat2doc.enum.dml.*`. `MSO_COLOR_TYPE` = `BaseEnum` (`RGB` 1 / `THEME` 2 / `AUTO` 101, no `from_xml`/`to_xml`). **`MSO_THEME_COLOR_INDEX` is the docx≠pptx delta** (do NOT copy the Mat2Ppt version): 17 members, **full-word** xml_values (`accent1`/`background1`/`dark1`/`hyperlink`/`light1`/`text1`) vs pptx abbreviations, `NOT_THEME_COLOR` xml_value `"UNMAPPED"` (truthy — `to_xml`→`"UNMAPPED"`) vs pptx `""`, and **no `MIXED`** member. |
| `enum/style.py` — `WD_BUILTIN_STYLE` (`BaseEnum`, **132 members**) + `WD_STYLE_TYPE` (`BaseXmlEnum`) + module alias `WD_STYLE` | `+mat2doc\+enum\+style\` (3 files) | P3-4 | `mat2doc.enum.style.*`. **`WD_BUILTIN_STYLE`** = the tier's largest enum: 132 `WdBuiltinStyle` MS-API constants (all **negative**, all **distinct** — oracle iterates 132 canonical members, so NO internal member-alias `Constant`, unlike `WD_BREAK_TYPE`); `BaseEnum` (no XML mapping, no `from_xml`/`to_xml`); feeds the **P4-7** styles tier; alias `WD_STYLE` re-exports all 132 (no statics — `BaseEnum` has no XML). `WD_STYLE_TYPE` (4): `paragraph` 1 / `character` 2 / `numbering`(=LIST) 4 / `table` 3. Period-less `WD_BUILTIN_STYLE.INDEX_HEADING` docstring `"Index Heading"` reproduced verbatim (doclen-proven). |
| `enum/table.py` — `WD_CELL_VERTICAL_ALIGNMENT` / `WD_ROW_HEIGHT_RULE` / `WD_TABLE_ALIGNMENT` (`BaseXmlEnum`) + `WD_TABLE_DIRECTION` (`BaseEnum`) + module aliases `WD_ALIGN_VERTICAL` / `WD_ROW_HEIGHT` | `+mat2doc\+enum\+table\` (6 files) | P3-4 | `mat2doc.enum.table.*`. 3 `BaseXmlEnum` (10 members): `WD_CELL_VERTICAL_ALIGNMENT` `top`/`center`/`bottom`/`both` (alias `WD_ALIGN_VERTICAL`), `WD_ROW_HEIGHT_RULE` `auto`/`atLeast`/`exact`(=EXACTLY) (alias `WD_ROW_HEIGHT`), `WD_TABLE_ALIGNMENT` `left`/`center`/`right`. `WD_TABLE_DIRECTION` (`BaseEnum`, 2): `LTR` 0 / `RTL` 1 (no XML). No member carries a `None`/`""` xml_value (H3 tri-state present but unexercised). `WD_TABLE_ALIGNMENT.LEFT` docstring `"Left-aligned"` (no trailing period, faithful); `WD_CELL_VERTICAL_ALIGNMENT.BOTH` = the long 4-fragment concatenated docstring (doclen-proven). |
| `enum/shape.py` — `WD_INLINE_SHAPE_TYPE` (plain `enum.Enum`) + module alias `WD_INLINE_SHAPE` | `+mat2doc\+enum\+shape\` (2 files) | P3-4 | `mat2doc.enum.shape.*` — the boundary-audit **C2** fold-in (`shape.py` had been orphaned; P3-4 closes C2). `WD_INLINE_SHAPE_TYPE` = a **plain value classdef** (NOT `Base(Xml)Enum` — the `WD_BREAK_TYPE` precedent): immutable `value (1,1) int32`, no `xml_value`/docstr/`from_xml`. 5 members `CHART` 12 / `LINKED_PICTURE` 4 / `PICTURE` 3 / `SMART_ART` 15 / `NOT_IMPLEMENTED` **−6** — all distinct, so **no internal alias** (unlike `WD_BREAK_TYPE`, which needed a `Constant` for its duplicate value 11). Alias `WD_INLINE_SHAPE` re-exports all 5 (no statics). |

| `oxml/text/font.py` — `CT_RPr` (`w:rPr`, 27 `ZeroOrOne` children over a 39-entry `_tag_seq` + 8 `@property` helpers + `_new_color` override) + the 6 child element classes `CT_Color`/`CT_Fonts`/`CT_Highlight`/`CT_HpsMeasure`/`CT_Underline`/`CT_VerticalAlignRun` | `+mat2doc\+oxml\+text\` (7 files) + **21 of the 28** `registry.m` main-map rows (`w:rPr`→`CT_RPr` + the 6 typed children + the shared rows below; `oxml/__init__.py:198-225`) | P4-1a | **THE first real `w:`-content element tier + the M2 byte-critical path.** All 7 `< mat2doc.oxml.BaseOxmlElement`; `text/font.py`→`+text` (module→subpackage convention, no clash with top-level `mat2doc.shared`). **`CT_RPr`**: 27 `ZeroOrOne` descriptors, each generating the docx family `get.x`/`get_or_add_x`/`new_x_`/`insert_x_`/`add_x_`/`remove_x_`, with `successors=_tag_seq[N:]` → **`TAG_SEQ(N+1:end)`** (H1 base shift once at the slice; engine has no arithmetic). The `_tag_seq` is **39 tags** (`font.py:79-119`, not the docstring's "40") stored verbatim as Constant `TAG_SEQ`; several descriptors carry **non-contiguous** slices (`sz=[24:]`, `highlight=[26:]`, `u=[27:]`, `vertAlign=[32:]`, `specVanish=[38:]`) because intervening `_tag_seq` tags have no descriptor — **kept, not collapsed**, so a scrambled/parsed insert re-sorts to canonical schema order byte-exact (H11 — the M2 crux; wrong slice → Word repair = FAIL). `w:color` overrides its creator with **`new_color_`** (`_new_color`, `font.py:149-151`, design.md §2 OVERRIDE alert) seeding `<w:color w:val="000000"/>` (RGB black) instead of the generic empty creator. 8 `@property` helpers live (`highlight_val`/`rFonts_ascii`/`rFonts_hAnsi`/`style`/`subscript`/`superscript`/`sz_val`/`u_val`); **`rFonts_hAnsi` asymmetry** — `=None` keeps `w:rFonts` (removes only `@w:hAnsi`), unlike `rFonts_ascii=None` which removes `w:rFonts` (`font.py:201-206`, verbatim). The 6 children delegate to P3-2 simpletypes / P3-3 enums: `CT_Color` `val`=`ST_HexColor`(Req)+`themeColor`=`MSO_THEME_COLOR`(Opt, fully-qualified for `resolveTypeCls_`), `CT_Fonts` `ascii`/`hAnsi`=`ST_String`(Opt), `CT_Highlight` `val`=`WD_COLOR_INDEX`(Req), `CT_HpsMeasure` `val`=`ST_HpsMeasure`(Req, `Pt(12)`→`"24"`), `CT_Underline` `val`=`WD_UNDERLINE`(Opt, remove-all-then-add), `CT_VerticalAlignRun` `val`=`ST_VerticalAlignRun`(Req). **M1 preserved:** registering the tags changes only the parsed node CLASS of the 464 `w:rPr` blocks in `styles.xml` (349458 B), never bytes — 17/17 byte-neutral. **0 new D#** (adopt-only D-001/D-serializer-nsdecl/D-STYPE-3/4/D-delta-1/2/3/D-005). |
| `oxml/shared.py` — `CT_OnOff` (bool run/para props) + `CT_String` (single-required-string, `+ static new`) — the **C3 fold-in** (2 of the 3 `shared.py` leaves; `CT_DecimalNumber` waits for P4-2) | `+mat2doc\+oxml\+shared\` (2 files) + **7 of the 28** `registry.m` main-map rows (20→`CT_OnOff` `w:b`/`w:i`/…, 1→`CT_String` `w:rStyle`; the count-total is 28 with the +text rows above) | P4-1a | `shared.py`→`+shared` (`mat2doc.oxml.shared.CT_*`; **distinct** from top-level `mat2doc.shared` RGBColor/Length). **`CT_OnOff`**: `val`=`OptionalAttribute("w:val", ST_OnOff, **default=True**)` — a **non-None default**, so the **D-delta-1 tri-state** fires via `setAttrTyped`: absent-get→`True`; set `False`→`ST_OnOff.to_xml`→`<w:b w:val="0"/>`; set `True`(==default) or `[]`(None)→`@w:val` **removed** (bare `<w:b/>`). Target of **20** registry rows. **`CT_String`**: `val`=`RequiredAttribute("w:val", ST_String)` + static `new(nsptagname, val)` (`elm=OxmlElement; elm.val=val`; the Python `cast(...)` type hint dropped); target of the `w:rStyle` row. **0 new D#**. |
| `oxml/text/run.py` — `CT_R` (`w:r`, the run element: 1 `ZeroOrOne` `rPr` + 5 `ZeroOrMore` content + explicit `add_t`/`add_drawing`/`clear_content`/`style`/`text`-shadow/comment helpers) + the run-content leaves `CT_Br`/`CT_Cr`/`CT_NoBreakHyphen`/`CT_PTab` + `CT_Text` (`w:t`) + the module-private `_RunContentAppender` | `+mat2doc\+oxml\+text\` (7 files: `CT_R`/`CT_Text`/`CT_Br`/`CT_Cr`/`CT_NoBreakHyphen`/`CT_PTab`/`RunContentAppender_`) + **6** `registry.m` run-block rows (`w:br`/`w:cr`/`w:noBreakHyphen`/`w:ptab`/`w:r`/`w:t`, `oxml/__init__.py:72-78`) | P4-1b | **THE second half of the M2 byte-critical path — `document.xml` body is `w:p`/`w:r`/`w:t`.** `run.py`→`+text` (same subpackage as `font.py`; distinct class names, no clash). **`CT_R`**: `rPr`=`ZeroOrOne(successors=())` forced to the FRONT via the **`insert_rPr_` OVERRIDE** (`run.py:145-147` `self.insert(0,rPr)`→`obj.insert(1,rPr)`, H1/H11), `br`/`cr`/`drawing`/`t`/`tab`=`ZeroOrMore(successors=())`→append (docx public `add_x`), so scrambled adds keep rPr-first + content-in-insertion-order. Explicit `add_t`/`add_drawing` WIN over the generated adders (xmlchemy skips an already-defined name). **`add_t` BYTE-CRITICAL** (`run.py:40-45`): sets `@xml:space="preserve"` iff `len(text.strip())<len(text)` — ported via the **`PY_STRIP_WS` 29-code-point Constant** (the exact CPython `str.strip()` set), **NOT** MATLAB `strip()`, which misses U+0085/U+00A0/U+2007/U+202F/U+2028 (the **H16 hazard**, AUD-1 fix); reserved `xml` prefix never `xmlns`-declared. **`CT_R.text`** shadows lxml `.text` (D10) → OVERRIDE protected `getText_`/`setText_`; serializer reads `text_raw_` (bypass), M1 bytes unchanged. **`_RunContentAppender`** (`RunContentAppender_`, private→trailing-`_`): text→content FSM `\t`→`w:tab`(`add_tab`), `\r`/`\n`→`w:br`(`add_br`; the docstring's "w:cr" is upstream's own error), else buffer→`w:t` (H2 UTF-16 surrogate-safe, H4 non-empty flush). **`CT_Text`** (`w:t`): `__str__`→`str_()` = text or `""` (H3/H4, never None); does NOT shadow `.text`. **`CT_Br`/`CT_Cr`/`CT_NoBreakHyphen`/`CT_PTab`** `str_()`→`"\n"`(text-wrapping)/`""`(page-column) / `"\n"` / `"-"` / `"\t"` (H2 via `newline`/`char(9)`). `CT_Br.type` non-None default `"textWrapping"` (D-delta-1). **VERIFY (parfmt WP):** `w:tab`→`CT_TabStop` unported → `CT_R.text` over a run holding `<w:tab>` errors until parfmt adds `CT_TabStop.str_()→"\t"` (off the M2 **write** path; `add_tab` write is live). **Latent P1-2 engine defect exposed + FIXED** (`XmlParser.m`): registering `w:r`→`CT_R` moved the `a03` `{wURI}r` child (ancestor-declared non-fixed prefix `q`) onto the registered-class ctor path, which did NOT forward the parser-resolved URI → `mat2doc:KeyError 'q'`; fix forwards `uri` to `feval(cls,name,ownDecls,uri)` exactly as the generic fallback (line 216) already did (drops the redundant post-ctor `setResolvedNsuri_`); a03 D-nsprefix-rewrite bytes proven UNCHANGED pre/post. **M1 preserved** (17/17 byte-neutral; registering the 6 tags changes only the parsed node CLASS). **0 new D#** (adopt-only D-001/D-serializer-nsdecl/D-delta-1/D-STYPE-\*/D-zip-time; AUD-1 + engine fix are fixes-to-match-Python, not deviations). |
| `oxml/text/parfmt.py` — `CT_PPr` (`w:pPr`, 12 `ZeroOrOne` children over a 36-entry `_tag_seq` + 13 `@property` helpers) + the para-property children `CT_Ind`/`CT_Jc`/`CT_Spacing`/`CT_TabStop`/`CT_TabStops` | `+mat2doc\+oxml\+text\` (6 files) + **11 of the 12** `registry.m` main-map rows (`w:pPr`→`CT_PPr`, `w:ind`→`CT_Ind`, `w:jc`→`CT_Jc`, `w:spacing`→`CT_Spacing`, `w:tab`→`CT_TabStop`, `w:tabs`→`CT_TabStops`, + the shared rows `w:pStyle`→`CT_String`, `w:keepNext`/`w:keepLines`/`w:pageBreakBefore`/`w:widowControl`→`CT_OnOff`; `oxml/__init__.py:231-251`) | P4-2 | **THE M2 `add_heading` write target — `add_paragraph`/`add_heading` write `w:p`/`w:pPr`/`w:pStyle` into `document.xml`.** All 6 `< mat2doc.oxml.BaseOxmlElement`; `text/parfmt.py`→`+text` (same subpackage as `font.py`/`run.py`; distinct class names). **`CT_PPr`**: 12 `ZeroOrOne` descriptors, each generating the docx family `get.x`/`get_or_add_x`/`new_x_`/`insert_x_`/`add_x_`/`remove_x_`, with `successors=_tag_seq[N:]` → **`TAG_SEQ(N+1:end)`** (H1 base shift once at the slice). The `_tag_seq` is **36 tags** (`parfmt.py:64-101`) stored verbatim as Constant `TAG_SEQ`; several descriptors carry **non-contiguous** slices (`widowControl=[6:]` skips framePr@5, `tabs=[11:]`, `spacing=[22:]`, `ind=[23:]`, `jc=[27:]`, `outlineLvl=[31:]`, `sectPr=[35:]`) because intervening `_tag_seq` tags have no descriptor — **kept, not collapsed**, so a scrambled/parsed insert re-sorts to canonical schema order byte-exact (H11 — the M2 crux; **`pStyle`=`_tag_seq[1:]` sorts before EVERYTHING**, the `add_heading` case). ALL 12 use the generic engine (no `_new_x`/`_insert_x` override). 13 `@property` helpers live (`first_line_indent`/`ind_left`/`ind_right`/`jc_val`/`keepLines_val`/`keepNext_val`/`pageBreakBefore_val`/`spacing_after`/`spacing_before`/`spacing_line`/`spacing_lineRule`/`style`/`widowControl_val`); `spacing_lineRule` maps present-`@w:line`+absent-`@w:lineRule`→`MULTIPLE` (`parfmt.py:296-298`); `first_line_indent` splits negative→`w:hanging`/positive→`w:firstLine` (`Length(-hanging)`, H6). The 5 children delegate to P3-2 simpletypes / P3-3 enums: **`CT_Ind`** `left`/`right`=`ST_SignedTwipsMeasure`(signed) + `firstLine`/`hanging`=`ST_TwipsMeasure`(unsigned), all Opt-default-None; **`CT_Jc`** `val`=`WD_ALIGN_PARAGRAPH`(Req); **`CT_Spacing`** `after`/`before`=`ST_TwipsMeasure` + `line`=`ST_SignedTwipsMeasure` + `lineRule`=`WD_LINE_SPACING`, all Opt; **`CT_TabStop`** `val`=`WD_TAB_ALIGNMENT`(Req) + `pos`=`ST_SignedTwipsMeasure`(Req) + `leader`=`WD_TAB_LEADER`(Opt, **non-None default `SPACES`** → D-delta-1/-2) + **`str_()`→`"\t"`** (`char(9)`, H2) which **CLOSES the P4-1b `w:tab` carry-forward gap** (`CT_R.text`/`CT_P.text` over a run holding `<w:tab>` now returns the tab char); **`CT_TabStops`** `tab`=`OneOrMore(successors=())` + `insert_tab_in_order(pos,align,leader)` sorted-by-pos. **`w:outlineLvl`→`CT_DecimalNumber` DEFERRED** (lives in `oxml/shared.py`, unported; byte-neutral — `outlineLvl`-bearing `pPr` in the 204-`w:pPr` `styles.xml` round-trips L1; a future shared/numbering WP adds the row); `w:numPr`/`w:sectPr` left generic (P8/P5). **M1 preserved:** registering the tags changes only the parsed node CLASS of the **204** `w:pPr` blocks in `styles.xml` (349458 B), never bytes — 17/17 byte-neutral. **0 new D#** (adopt-only D-001/D-serializer-nsdecl/D-STYPE-\*/D-delta-1/2/4). |
| `oxml/text/paragraph.py` — `CT_P` (`w:p`, the paragraph element: 1 `ZeroOrOne` `pPr` + 2 `ZeroOrMore` content `hyperlink`/`r` + `add_p_before`/`alignment`/`clear_content`/`inner_content_elements`/`lastRenderedPageBreaks`/`set_sectPr`/`style`/`text`-shadow) | `+mat2doc\+oxml\+text\CT_P.m` + **1** `registry.m` run-block row (`w:p`→`CT_P`, `oxml/__init__.py:229`) | P4-2 | **THE M2 `add_paragraph` target — `document.xml` body is a sequence of `w:p`, each `w:pPr` + `w:r`.** `paragraph.py`→`+text`. **`CT_P`**: `pPr`=`ZeroOrOne(successors=())` forced to the FRONT via the **`insert_pPr_` OVERRIDE** (`paragraph.py:104-106` `self.insert(0,pPr)`→`obj.insert(1,pPr)`, H1/H11 — same shape as `CT_R._insert_rPr`), `hyperlink`/`r`=`ZeroOrMore(successors=())`→append (docx public `add_x`), so appending a run then setting `style` (adds pPr) still yields `[pPr, r]`. **`CT_P.text`** shadows lxml `.text` (D10) — the concatenated inner-content text over `w:r|w:hyperlink` → OVERRIDE protected `getText_`; **read-only** → `setText_` RAISES `mat2doc:AttributeError`. `clear_content` (`./*[not(self::w:pPr)]`, keeps only pPr), `inner_content_elements` (`./w:r \| ./w:hyperlink`), `lastRenderedPageBreaks`, `alignment` (via `pPr.jc_val`), `set_sectPr` (get_or_add_pPr; remove_sectPr_; insert_sectPr_), `style` (via `pPr.style`), `add_p_before` all live. **VERIFY (P4-3 hyperlink WP):** `w:hyperlink`→`CT_Hyperlink` unported → resolves generic; `CT_P.text`/`inner_content_elements`/`lastRenderedPageBreaks` over a paragraph CONTAINING a hyperlink return the generic element's char data (only the element CLASS differs) until P4-3 registers `w:hyperlink` and re-probes — off the M2 write path (default body is one empty `w:p`; `add_paragraph`/`add_heading` write `w:pPr`+`w:r` only). Same dependency-order shape as the `w:tab` gap this WP closed. **M1 preserved** (17/17 byte-neutral; `document.xml`'s `w:p` now parses to `CT_P`). **0 new D#** (adopt-only D-001/D-serializer-nsdecl/D-delta-4/D10). |
| `oxml/text/hyperlink.py` — `CT_Hyperlink` (`w:hyperlink`, the run container: `r`=`ZeroOrMore` + `rId`/`anchor`/`history` `OptionalAttribute`s + `.text`-shadow + `lastRenderedPageBreaks`) | `+mat2doc\+oxml\+text\CT_Hyperlink.m` + **1** `registry.m` main-map row (`w:hyperlink`→`CT_Hyperlink`, `oxml/__init__.py:67`) | P4-3 | **The hyperlink element — the second thing (besides `w:r`) a paragraph can hold.** `hyperlink.py`→`+text`. **`CT_Hyperlink`**: `r`=`ZeroOrMore("w:r", successors=())`→append (`r_lst`/`new_r_`/`insert_r_`/`add_r_`/public `add_r`). Three `OptionalAttribute`s: `rId`=`XsdString`(`r:id`, default None→[]), `anchor`=`ST_String`(`w:anchor`, default None→[]), **`history`=`ST_OnOff`(`w:history`, `default=True` — NON-None)** driving the **D-delta-1 tri-state** (absent→`true`; `false`→`w:history="0"`; `true`(==default)/`[]`→`@w:history` removed; `ST_OnOff.from_xml` `1/true/on`→true, `0/false/off`→false). **`.text`** shadows lxml `.text` (D10) — `"".join(r.text for r in xpath("w:r"))` over DIRECT `w:r` children via `getText_` OVERRIDE; **read-only** → `setText_` RAISES `mat2doc:AttributeError`; serializer reads `text_raw_` (bypass), so the hyperlink's own char data serializes unchanged. `lastRenderedPageBreaks`=`./w:r/w:lastRenderedPageBreak` (no-arg method). **CLOSES the P4-2 VERIFY-hyperlink** — registering this class flips `CT_P.text`/`inner_content_elements`/`lastRenderedPageBreaks` over a hyperlink-bearing paragraph from generic char data to the concatenated run text, **registry row alone, no CT_P edit** (`p.text`="Before link after" MATLAB==python-docx). **M1 preserved** (17/17 byte-neutral; template carries 0 hyperlinks → pure lookup addition). **DEVIATION — s7 created-element nsdecl residual (D-serializer-nsdecl manifestation, ACCEPTED, NO new D#):** on a LOOSE created `w:hyperlink` serialized standalone, set `rId`→mints `xmlns:ns0`(relationships URI)→clear `rId`→lxml keeps the orphaned `xmlns:ns0`, the port drops it (element/attrs/text/expanded-names identical, exclusive-C14N-equal; L3 only under `pkgcompare`'s full-in-scope-nsmap canonicalizer). D-serializer-nsdecl's decl-emission fix covered PARSED verbatim nodes only, so this created-element minted-then-orphaned sub-case is a residual; UNREACHABLE on every real path (real hyperlinks keep `rId`; document-tree hyperlinks use the root's `xmlns:r`, never mint `ns0`). ACCEPTED/deferred, P4-5b/P7 reopen-check (`decision_2026-07-28_nsdecl_created_element_orphan.md`). **0 new D#** (adopt-only D-001/D-serializer-nsdecl/D-delta-1/D10). |
| `oxml/text/pagebreak.py` — `CT_LastRenderedPageBreak` (`w:lastRenderedPageBreak`, the renderer page-break: precedes/follows detection + preceding/following fragment split; 6 `@lazyproperty` + 3 private helpers) | `+mat2doc\+oxml\+text\CT_LastRenderedPageBreak.m` + **1** `registry.m` main-map row (`w:lastRenderedPageBreak`→`CT_LastRenderedPageBreak`, `oxml/__init__.py:74`) | P4-3 | **The rendered page-break element — the split machinery `Paragraph.rendered_page_breaks` (a later WP) is built on.** `pagebreak.py`→`+text`. An **empty** element (schema `CT_Empty`; the distinguished type-name only attaches behaviour). 4 public accessors ported as **no-arg methods** (so display never evaluates them + the ValueError guards fire only on explicit call): **`precedes_all_content`** (common — `false` in a hyperlink, else non-empty `./w:r[1]/w:lastRenderedPageBreak[not(preceding-sibling::*[<run-inner-content>])]`), **`follows_all_content`** (rare — last-run mirror `(./w:r)[last()]/…[not(following-sibling::…)]`), **`preceding_fragment_p`**/**`following_fragment_p`** (loose `CT_P` deepcopy clone split at the break; first-break guard `self==_first_lrpb_in_p(_enclosing_p)` H5, else `mat2doc:ValueError` "only defined on first rendered page-break in paragraph"). Split differs **bare-run** (remove inner-content on the far side of the break's run/within-run, keep `w:pPr`/`w:rPr`, remove break) vs **atomic-hyperlink** (whole `w:hyperlink`→preceding frag, break removed from inside — NOT split). `RUN_INNER_CONTENT_XPATH` = verbatim 7-term `self::` union Constant. 6 `@lazyproperty` (`_is_in_hyperlink` + 4 frag builders + `_run_inner_content_xpath`→Constant) cached **value + logical computed-flag** (design.md §2, never `isempty` sentinel); caching is FAITHFUL (both langs return the STALE frag after a mutation). Private helpers `_enclosing_hyperlink`/`_enclosing_p`/`_first_lrpb_in_p` (underscore rotation). **H1** XPath `[1]`/`[last()]` already 1-based (never shifted); `res(1)` for `[0]`. **VERIFY-deepcopy RATIFIED unobservable** (a cloned lrpb's lazyproperties are never accessed). **M1 preserved** (17/17 byte-neutral; template carries 0 lrpb → pure lookup addition). Gate-3: 8-tree detection + 6-tree fragment-split serhex byte-pins + second-break ValueError guard all MATLAB==python-docx. **0 new D#** (adopt-only D-001/D-serializer-nsdecl). |

| `text/font.py` — `Font(ElementProxy)` (the run's character properties: 20 boolean tri-state toggles + color/highlight_color/name/size/subscript/superscript/underline; `_get_bool_prop`/`_set_bool_prop`) | `+mat2doc\+text\Font.m` | P4-4a | **OPENS the P4 API tier** — the first user-facing proxy, sitting on the now-complete text-oxml element layer. `text/font.py`→`+text` (`mat2doc.text.Font`; distinct from the oxml `mat2doc.oxml.text.CT_*`). `< mat2doc.shared.ElementProxy` (reference semantics + H5 element-identity `eq`/`ne` inherited). Wraps a **`w:r`** (NOT its `w:rPr`); every accessor reaches `self._element.rPr` (get, `[]` if absent) / `get_or_add_rPr()` (set) and **delegates to the P4-1a `CT_RPr` helpers** — **NO oxml logic, NO `register_element_cls` row, NO serialization code** (API/proxy tier → behavioral equivalence, not byte-fixture; **byte-neutral**, M1 17/17 trivially unchanged). Ctor faithful incl. two dead members: the `self._element=r` re-assign (redundant with the super) + `self._r`→`r_` (set, **never read** in `font.py`). **20 boolean tri-states** (H3 `[]`=inherited): each `_get_bool_prop(name)`/`_set_bool_prop(name)` passes the **CT_RPr property name** (the `w:` local tag minus prefix) → `CT_RPr.get_bool_val_`/`set_bool_val_`; the map (verified vs `font.py`): `all_caps`→`w:caps`, `bold`→`w:b`, `complex_script`→`w:cs`, `cs_bold`→`w:bCs`, `cs_italic`→`w:iCs`, `double_strike`→`w:dstrike`, `emboss`→`w:emboss`, `hidden`→`w:vanish`, `imprint`→`w:imprint`, `italic`→`w:i`, `math`→`w:oMath`, `no_proof`→`w:noProof`, `outline`→`w:outline`, `rtl`→`w:rtl`, `shadow`→`w:shadow`, `small_caps`→`w:smallCaps`, `snap_to_grid`→`w:snapToGrid`, `spec_vanish`→`w:specVanish`, `strike`→`w:strike`, `web_hidden`→`w:webHidden` (tri-state fires through `CT_OnOff` D-delta-1: True→bare `<w:b/>`, False→`<w:b w:val="0"/>`, `[]`→removed). **Non-bool props**: `color` (read-only)→a **fresh** `ColorFormat(self._element)` each access (not cached); `highlight_color`→`CT_RPr.highlight_val` (`WD_COLOR_INDEX`); `name`→get `rFonts_ascii` / set BOTH `rFonts_ascii`+`rFonts_hAnsi` (the ascii/hAnsi asymmetry lives in `CT_RPr`); `size`→`CT_RPr.sz_val` (a `Length` in EMU; `<w:sz>` is **half-points** via `ST_HpsMeasure`, `Pt(12)`→`"24"`), set wraps `Emu()` (**D-STYPE-1** adopt-only, non-engaged); `subscript`/`superscript`→`CT_RPr.subscript`/`.superscript` (the `w:vertAlign` truth table lives in `CT_RPr`). **`underline`** = tri-state-**plus-enum** (`WD_UNDERLINE`): get maps `u_val` None/INHERITED→`[]`, SINGLE→`true`, NONE→`false`, else the member (explicit `isequal(val,[])` first branch == Python `==`-fall-through, VERIFY-1 closed); set uses strict `islogical&&isscalar&&value` guards (Python `is True`/`is False`) so True→SINGLE, False→NONE, member/`[]` pass through. **0 new D#** (adopt-only D-STYPE-1 [non-engaged], D-serializer-nsdecl [non-engaged, `w:`-only runs mint no `ns0`]). |
| `dml/color.py` — `ColorFormat(ElementProxy)` (`rgb`/`theme_color`/`type` over `w:rPr/w:color`; private `_color`) | `+mat2doc\+dml\ColorFormat.m` | P4-4a | The first `+dml` class (`mat2doc.dml.ColorFormat`). `< mat2doc.shared.ElementProxy`; wraps the **`w:rPr` PARENT** (a `w:r`), NOT the `<w:color>` itself — reaches the color via `self._element.rPr.color` (the private `_color`→`color_`, underscore rotation). Ctor faithful incl. the redundant `self._element=rPr_parent` re-assign. **API/proxy tier** — delegates to the P4-1a `CT_Color`/`CT_RPr` helpers, no registry row / no serialization (byte-neutral). **`type` (read-only) detection precedence THEME > AUTO > RGB** (`color.py:94-101`): no color→`[]`; `@w:themeColor` present→**THEME** (checked FIRST); else `@w:val=="auto"`→**AUTO**; else **RGB** — so a color with BOTH a themeColor and an RGB `@w:val` reports THEME (Word's RGB "good guess" retained but ignored; the val is NOT cleared). **`rgb`** get: `color.val` (an `RGBColor` OR the string `"auto"`); `== ST_HexColorAuto.AUTO`→`isequal(color.val, AUTO)` (H4 mixed-type: `isequal(RGBColor,"auto")` false, `isequal("auto","auto")` true — reproduces Python `==`); AUTO→`[]`. set: no-op when clearing an already-absent color; else remove any color then (unless clearing) add fresh + set `@w:val`. **REQUIRED-ATTR EDGE:** `rgb` reads the **required** `@w:val`, so on a parsed `<w:color w:themeColor="accent1"/>` (themeColor present, `@w:val` ABSENT) it **RAISES `mat2doc:InvalidXmlError`** — byte-identical message to python-docx; `type`/`theme_color` on the same element are SAFE (type checks themeColor before val). **`theme_color`**: get→`color.themeColor` (an `MSO_THEME_COLOR` member or `[]`); set→force a color + set `@w:themeColor`; `[]`→remove color only when a color AND an rPr both exist. H3 `[]` throughout. **0 new D#** (adopt-only). |
| `text/run.py` — `Run(StoryChild)` (the `<w:r>` proxy: `text` / `bold` / `italic` / `underline` / `font` / `add_break` / `add_text` / `add_tab` / `clear` / `contains_page_break` / `mark_comment_range` + `add_picture`·`iter_inner_content`·`style` stubs) | `+mat2doc\+text\Run.m` | P4-4b | The SECOND P4 API-tier proxy (`mat2doc.text.Run`; distinct from the oxml `mat2doc.oxml.text.CT_R` — `Run` **wraps** a `CT_R`, is not one). `< mat2doc.shared.StoryChild` (NOT `ElementProxy` — docx's run/block-item base: only `parent_` + `part` delegation; `Run(r,parent).part`→`parent.part`, `_PartStub`→"STUBPART" verified). Ctor `self._r = self._element = self.element = r` → **three names, one `CT_R` handle**: `r_` (private, working) / `element_` (private, what `font` wraps) / `element` (public, set-never-read, kept for fidelity). **API/proxy tier — NO registry row, NO oxml class, NO serialization change (byte-neutral, M1 17/17 trivially unchanged, behavioral equivalence).** **LIVE:** `text` get/set (→`r_.text`, D10; set replaces content, `\t`→`w:tab`, `\n`/`\r`→`w:br`, `\r` reads back `\n`); `bold`/`italic`/`underline` get/set delegate to a **fresh** `Font(self._element)` each access (Python does not cache → `run.font==run.font` true, mutation persists on the shared element); `font` read-only fresh `Font`; **`add_break`** maps `WD_BREAK`→`(type_, clear)` then sets `br.type`/`br.clear` iff non-`[]` — LINE→`<w:br/>`, PAGE→`@w:type="page"`, COLUMN→`@w:type="column"`, **LINE_CLEAR_{LEFT,RIGHT,ALL}→ONLY `@w:clear` (the `type_="textWrapping"` DEFAULT is removed by the OptionalAttribute setter, D-delta-1 byte-confirmed)**, `TEXT_WRAPPING` alias==LINE_CLEAR_ALL; any other member (`SECTION_*`) → Python `{…}[m]` **KeyError** (`error("mat2doc:KeyError",…)`, fires BEFORE `add_br` → children=0, no partial mutation); `add_text`→appends `<w:t>` (leading/trailing ws→`xml:space="preserve"`), returns a `Text_`; `add_tab`→`<w:tab/>`; `clear`→drops content, **keeps `<w:rPr>`, returns self**; `contains_page_break`→`~isempty(r_.lastRenderedPageBreaks())` (rendered only — a hard `<w:br w:type="page"/>` is NOT counted, H4); `mark_comment_range(last_run,comment_id)`→commentRangeStart/End + a `CommentReference` run. **STUB (`mat2doc:notYetPorted`, naming the owner):** `add_picture`→**P7** (`InlineShape`/`StoryPart.new_pic_inline`); `iter_inner_content`→**P4-5b + P7** (whole-method stub — `CT_R.inner_content_items` is itself stubbed at P4-1b, no faithful partial path); `style` get/set→**P4-7** (`CharacterStyle`/`get_style`·`get_style_id`) ported as a **Dependent read/write property** (true Python shape → both `run.style` and `run.style=x` raise the SAME stub; auto-DISPLAY is safe — MATLAB omits the erroring `style` row, Gate-2 Flag-2 RATIFIED). **0 new D#** (adopt-only D10 upstream, D-delta-1 engaged+byte-confirmed by LINE_CLEAR_*, D-serializer-nsdecl non-engaged: `w:`-only runs mint no `ns0`). |
| `text/run.py` — `_Text` (the inert `<w:t>` wrapper `Run.add_text` returns) | `+mat2doc\+text\Text_.m` | P4-4b | `_Text`→`Text_`, `_t`→`t_` (underscore rotation, design.md §2). A bare `handle` holding only the private `t_` (a `CT_Text`). **v1.2.0 `_Text` is a bare `object` subclass with NO `text` property or any other public member** (verified vs run.py 252-257 + the runtime oracle `hasattr(t,"text")`→False; MATLAB public-member count 0) — ported faithfully with **NO `text` accessor added** (adding one would be an unported feature, design.md §7; Gate-2 Flag-1 RATIFIED — the task brief's "has a text property" claim was wrong). `super(_Text,self).__init__()` is just `object.__init__()`, no ported effect. **0 new D#.** |
| `text/parfmt.py` — `ParagraphFormat(ElementProxy)` (the paragraph-format proxy: `alignment` / `first_line_indent` / `keep_together` / `keep_with_next` / `left_indent` / `line_spacing` / `line_spacing_rule` / `page_break_before` / `right_indent` / `space_after` / `space_before` / `tab_stops` / `widow_control` + the `_line_spacing`/`_line_spacing_rule` statics) | `+mat2doc\+text\ParagraphFormat.m` | P4-5a | The THIRD P4 API-tier proxy (`mat2doc.text.ParagraphFormat`; distinct from the oxml `mat2doc.oxml.text.CT_PPr` — it **wraps** a pPr owner, is not one). `< mat2doc.shared.ElementProxy` (reference semantics + H5 element-identity `eq`/`ne` inherited). **Wraps the pPr's OWNER** — a `w:p` (`CT_P`) or a style pPr owner, NOT the `<w:pPr>` — and reaches its properties through `self._element.pPr` (get, `[]` if absent) / `get_or_add_pPr()` (set), **delegating to the P4-2 `CT_PPr` helpers** (`jc_val`/`first_line_indent`/`ind_left`/`ind_right`/`keepLines_val`/`keepNext_val`/`pageBreakBefore_val`/`widowControl_val`/`spacing_after`/`spacing_before`/`spacing_line`/`spacing_lineRule`) — **NO oxml logic, NO `register_element_cls` row, NO serialization code** (API/proxy tier → behavioral equivalence, byte-neutral; M1 17/17 trivially unchanged). All 13 properties H3 tri-states (`[]`=inherited via inline `isequal(x,[])`; the removal logic lives in the P4-2 CT_PPr/CT_Ind/CT_Spacing setters). **`alignment`** `WD_ALIGN_PARAGRAPH`/`[]`→`w:jc`; **4 bool tri-states** `keep_together`→`w:keepLines`, `keep_with_next`→`w:keepNext`, `page_break_before`→`w:pageBreakBefore`, `widow_control`→`w:widowControl` (CT_OnOff D-delta-1). **Indents SIGN both ways** (via CT_PPr/CT_Ind): `first_line_indent` positive→`w:firstLine`/negative→`w:hanging` (`Length(-hanging)` on get); `left_indent`/`right_indent` signed `@w:left`/`@w:right`; `space_before`/`space_after`→`@w:before`/`@w:after`. **`line_spacing` float-vs-`Length` detection** (the meaty logic, `parfmt.py:102-160,256-286`): GET returns a **float** multiple when `@w:lineRule="auto"` (MULTIPLE, `line/Pt(12)`, **D-STYPE-1**) else the absolute `Length`; SET dispatches on the ARGUMENT TYPE — a `Length`→absolute (`EXACTLY` unless already `AT_LEAST`), a non-`Length` numeric→`Emu(value·Twips(240))`+MULTIPLE, `[]`→attrs removed but empty `<w:spacing/>` retained. **`line_spacing_rule`** maps the MULTIPLE special members `Twips(240)`→SINGLE / 360→ONE_POINT_FIVE / 480→DOUBLE (SET writes the matching Twips line + MULTIPLE; AT_LEAST/EXACTLY write lineRule only; `[]` removes). Note `Pt(12)==Twips(240)==152400 EMU` (parfmt.py uses `Pt(12)` on get, `Twips(240)` on set — both verbatim). **`tab_stops`** = a `@lazyproperty` (cache + logical computed-flag, never `isempty` sentinel) returning a read-only `TabStops` view (no `set.tab_stops` → Python `AttributeError`). **0 new D#** (adopt-only D-STYPE-1 [engaged, value-exact/output-invisible], D-serializer-nsdecl [non-engaged, `w:`-only]). |
| `text/tabstops.py` — `TabStops(ElementProxy)` (the tab-stop **sequence**: `add_tab_stop` / `clear_all` / `__getitem__` / `__delitem__` / `__iter__` / `__len__`) | `+mat2doc\+text\TabStops.m` | P4-5a | **The FIRST proxy *sequence* in Mat2Doc.** `mat2doc.text.TabStops`; `< mat2doc.shared.ElementProxy`. Wraps the **`w:pPr`** (`CT_PPr`, `self._pPr`→`pPr_`), reaching the `<w:tabs>` on demand via `pPr.tabs`/`get_or_add_tabs()`/`_remove_tabs()` (P4-2). **API/proxy tier** — no registry row, no serialization (byte-neutral). **VERIFY-COLLECTION (accepted at Gate 2):** the shared `RedefinesParen` collection base (design.md §2: `x[i]→x(i+1)`, `len→numel`, `for s in x→x.to_array()`) is a **future WP** and does not exist yet; per the Mat2Ppt `_GradientStops`→`GradientStops_` precedent, the Python sequence surface is ported as **explicit methods** — `__getitem__`→`getitem_`, `__delitem__`→`delitem_`, `__len__`→`len_`, `__iter__`→`to_array` — with `getitem_`/`delitem_` keeping the **Python 0-based** argument (H1: `+1` at the child-list index, negative index counts from the end, out-of-range→`mat2doc:IndexError`). **Hand-off:** when the collection base lands, `TabStops` derives from it and exposes native 1-based `tab_stops(1)` (≡ `getitem_(0)`); do NOT retrofit now. **`add_tab_stop(position, alignment=LEFT, leader=SPACES)`** inserts in **position order** (`insert_tab_in_order` → `<w:tabs>` stays pos-sorted). **`getitem_`** verbatim messages: OOR→`"list index out of range"`, empty→`"TabStops object is empty"`; **`delitem_`** OOR→`"tab index out of range"`, delete-down-to-empty **removes `<w:tabs>`**; **`clear_all`** drops `<w:tabs>`; **`to_array`** mints a FRESH `TabStop` per `<w:tab>` (not cached, H5/H9 materialized). **0 new D#.** |
| `text/tabstops.py` — `TabStop(ElementProxy)` (an individual tab stop: `position` / `alignment` / `leader` over `<w:tab>`) | `+mat2doc\+text\TabStop.m` | P4-5a | `mat2doc.text.TabStop`; `< mat2doc.shared.ElementProxy`. Wraps a single `<w:tab>` (`CT_TabStop`, `self._tab`→`tab_`). **API/proxy tier** — delegates to the P4-2 `CT_TabStop` descriptors, no registry/serialization (byte-neutral). All three read/write: **`position`**→`@w:pos` (`Length`, signed twips, RequiredAttribute); **`alignment`**→`@w:val` (`WD_TAB_ALIGNMENT`, RequiredAttribute); **`leader`**→`@w:leader` (`WD_TAB_LEADER`, OptionalAttribute, **default `SPACES` written attribute-absent** — `add_tab_stop(Inches(1))` → `<w:tab w:pos="1440" w:val="left"/>` with NO `@w:leader`, reads back SPACES, D-delta-1/-2; explicit DOTS→`w:leader="dot"`). **POSITION-SETTER IDENTITY QUIRK (faithful, `tabstops.py:118-123`):** the setter RE-INSERTS a fresh `<w:tab>` at the new pos-order slot and removes the old element, but reassigns **only `self._tab`** — `self._element`/`eq()` still track the **OLD detached** element while the three properties reflect the NEW one. Ported verbatim: after `t.position=Inches(1.5)`, a fresh-proxy-over-same-element `t2` keeps `t==t2` True (both pinned to the old element) while `t.position` reads the new value and `to_array` re-sorts — genuine python-docx behavior, correctly NOT "corrected" (Gate-2 confirmed). **0 new D#.** |
| `text/paragraph.py` — `Paragraph(StoryChild)` (the `<w:p>` proxy: `add_run` / `alignment` / `clear` / `contains_page_break` / `hyperlinks` / `insert_paragraph_before` / `iter_inner_content` / `paragraph_format` / `rendered_page_breaks` / `runs` / `style` / `text` + protected `_insert_paragraph_before`) | `+mat2doc\+text\Paragraph.m` | P4-5b | The **FIFTH P4 API-tier proxy and the LAST API-tier WP of P4** (`mat2doc.text.Paragraph`; distinct from the oxml `mat2doc.oxml.text.CT_P` — **wraps** a `CT_P`, is not one). `< mat2doc.shared.StoryChild` (NOT `ElementProxy` — so **instance identity**, no `eq`/`ne`; see dunder row). Ctor `self._p = self._element = p` → `p_` (working) / `element_` (what `paragraph_format` wraps), one `CT_P` handle. **API/proxy tier — NO registry row, NO oxml class, NO serialization change → behavioral equivalence, byte-neutral (M1 17/17 trivially unchanged).** **List properties** `runs`/`hyperlinks`/`rendered_page_breaks` → homogeneous `1×N` object arrays seeded `Class.empty(1,0)` (plain-list surface, see dunder row); **`iter_inner_content`** → heterogeneous `1×N` **cell** (`Run`/`Hyperlink` in document order; H9 precomputed generator, H10 `isa(e,"mat2doc.oxml.text.CT_R")` dispatch, H1 `./w:r \| ./w:hyperlink` doc-order). **`add_run(text,style)`** `if text:` (non-empty-string, H4) + `if style:` (truthy via `truthy_` helper); **`insert_paragraph_before(text,style)`** uses the DISTINCT `if text:` (truthy) vs **`if style is not None`** (identity, `~isequal(style,[])`) — ported separately. **`clear`** returns SELF (H5, `p.clear()==p`), preserves `<w:pPr>`. **`style`** get AND set delegate through `part().get_style`/`get_style_id` → the **P4-7 DocumentPart STUBS** (`mat2doc:notYetPorted`) — faithful propagation (Dependent read/write property, both raise; auto-display omits the row); also reached via `add_run(text,style)`/`insert_paragraph_before(text,style)`. `text` get→`p_.text` (D10), set→`clear();add_run(text)` (safe — no style). `paragraph_format`→a fresh `ParagraphFormat(element_)` each access. `_insert_paragraph_before`→`insert_paragraph_before_` (protected; parent = `obj.parent_`, NOT obj). **0 new D#** (adopt-only D-serializer-nsdecl [non-engaged, s7 still unreachable] + D10 [read-only]). |
| `text/hyperlink.py` — `Hyperlink(Parented)` (the `<w:hyperlink>` read proxy: `address` / `contains_page_break` / `fragment` / `runs` / `text` / `url`) | `+mat2doc\+text\Hyperlink.m` | P4-5b | `mat2doc.text.Hyperlink`; `< mat2doc.shared.Parented` (parent-ONLY tier — holds no element, does NOT define `eq`/`ne` → **instance identity**, distinct from an `ElementProxy` subclass). Because `Parented` holds no `element_`, this class declares its OWN private handles (`hyperlink_`/`element_`, both = the `CT_Hyperlink`; `element_` set-but-unread per hyperlink.py). **API/proxy tier** — delegates to the P4-3 `CT_Hyperlink` helpers, no registry/serialization (byte-neutral). **Entire surface read-only.** **`address`** LIVE — `if rId` (non-empty-string, H4) → `parent_.part().rels().getitem(rId).target_ref`; internal jump / absent rId → `""` (a STRING, never `[]`). **`fragment`** `anchor or ""` (H3/H4) → `""` for None/empty anchor. **`url`** `not address`→`""`; `if fragment`→`address#fragment` else `address`. **`runs`** homogeneous `1×N` `Run` array, and IMPORTANT: the runs get **`self._parent`** (the hyperlink's parent, a `ProvidesStoryPart`), NOT the hyperlink — ported exactly. `text`→`hyperlink_.text` (D10); `contains_page_break`→`~isempty(hyperlink_.lastRenderedPageBreaks())`. **NO `rId`-setting/-clearing path exists** → s7 Family-2 orphan unreachable (reopen-check CONCUR, re-booked P7). **0 new D#**. |
| `oxml/styles.py` — `styleId_from_name` (module fn) + `CT_LatentStyles` / `CT_LsdException` / `CT_Style` / `CT_Styles` (4 element classes) | `+mat2doc\+oxml\+styles\` (5 files) + **12 styles-block `registry.m` main-map rows** (`w:basedOn`/`w:latentStyles`/`w:locked`/`w:lsdException`/`w:name`/`w:next`/`w:qFormat`/`w:semiHidden`/`w:style`/`w:styles`/`w:uiPriority`/`w:unhideWhenUsed`; `oxml/__init__.py:138-149`) | P4-6 | **The styles-oxml element tier — the FIRST WP of the styles chain (P4-6 → P4-7a/b → M2), registry-adding + byte-critical parse path.** All 4 CT `< mat2doc.oxml.BaseOxmlElement`; `styleId_from_name` is a **module fn** → a package function (design.md §1, one per file), used by `CT_Styles.add_style_of_type`. **`CT_Styles`** (root of `styles.xml`): `get_by_id` (`w:style[@w:styleId="X"]`) / `get_by_name` (`w:style[w:name/@w:val="X"]`, predicate attr sub-path) → `res(1)` (H1) or `[]` (H3); `default_for` = **last** `@w:type`-matching `@w:default` style in doc order (`matches{end}`, H4 triad `isequal(s.type,style_type) && ~isequal(d,[]) && d`); `add_style_of_type` (public `add_style` D-delta-4). **`CT_Style`**: 4 optional attrs (type→`WD_STYLE_TYPE` FQCN / styleId→ST_String / default,customStyle→ST_OnOff, all default None) + **10 `ZeroOrOne`** over the 22-tag `_tag_seq` (`successors=_tag_seq[N:]`→`TAG_SEQ(N+1:end)`, several non-contiguous — `w:aliases` at index 2 / `w:link`,`autoRedefine`,`hidden` at 5-7 / `personal*`,`rsid` at 13-16 skipped); **NO `link` descriptor in v1.2.0** (`w:link` is `_tag_seq`-only → generic `XmlElement`); the H3 tri-state `.._val` split (basedOn/name/uiPriority→`[]`; locked/qFormat/semiHidden/unhideWhenUsed→logical `false`); `base_style`/`next_style` sibling chains via `getparent().get_by_id`. **`CT_LatentStyles`**: 6 optional attrs (count/defUIPriority ST_DecimalNumber; 4 def* ST_OnOff) + `lsdException` `ZeroOrMore` (public `add_lsdException`); `bool_prop`→`false` when absent; `set_bool_prop` **F-1** (see idiom row). **`CT_LsdException`**: required `name` (ST_String, InvalidXmlError) + 5 optional attrs; NO children; `on_off_prop`/`set_on_off_prop`. **`delete()`** on `CT_Style`/`CT_LsdException` = the guarded-destructor **H17** override (see idiom row). Registering the 12 tags is **byte-neutral** — the shipped 349 KB `styles.xml` (164 `w:style` / 137 `w:lsdException`) now transits these CT_* with M1 17/17 preserved. **0 new D#**. |
| `oxml/shared.py::CT_DecimalNumber` (the third `shared.py` leaf) | `+mat2doc\+oxml\+shared\CT_DecimalNumber.m` + **2 `registry.m` rows** (`w:uiPriority` [styles] + `w:outlineLvl` [closed P4-2 parfmt deferral]; `oxml/__init__.py:148`/`:244`) | P4-6 | `mat2doc.oxml.shared.CT_DecimalNumber`; `< mat2doc.oxml.BaseOxmlElement` — the `<w:val>`-bearing decimal-number leaf, sibling of the P4-1a `CT_OnOff`/`CT_String`. `val` = `RequiredAttribute` of `ST_DecimalNumber` (`xsd:int`): `getAttrRequired` raises `mat2doc:InvalidXmlError` when absent, setter always writes + range-validates, `from_xml` = Python `int(str)` (H6). Static **`new(nsptagname, val)`** sets `@w:val = str(val)` **directly** via `mat2doc.shared.pyStr(val,"int")` (H14) — NOT through the descriptor setter, so (like Python `str()`) it does **not** range-validate. **FIRST registration** — at `w:uiPriority` (styles) + `w:outlineLvl` (closes the P4-2 deferral); numbering/table registrations (`w:numId`/`w:ilvl`/`w:gridSpan`/…) stay **DEFERRED to P6/P8** (F-3 forward flag: those WPs must re-run the M1 `styles.xml` byte gate, since `w:numId`(6×)/`w:ilvl`(1×) appear inside `w:pPr/w:numPr` in the template and flip parse-class then). **0 new D#**. |
| `text/pagebreak.py` — `RenderedPageBreak(Parented)` (the `<w:lastRenderedPageBreak>` read proxy: `preceding_paragraph_fragment` / `following_paragraph_fragment`) | `+mat2doc\+text\RenderedPageBreak.m` | P4-5b | `mat2doc.text.RenderedPageBreak`; `< mat2doc.shared.Parented` (**instance identity**; declares its own `element_`/`lastRenderedPageBreak_`, both = the `CT_LastRenderedPageBreak`, `element_` set-but-unread). **API/proxy tier** — the heavy split machinery (H1 xpath positional indexing, deepcopy/remove, the atomic break-inside-hyperlink case) lives in the P4-3 `CT_LastRenderedPageBreak`; these two getters only guard + wrap. **`preceding_paragraph_fragment`** — `precedes_all_content()` true → `[]` (Python None); else `Paragraph(preceding_fragment_p(), parent_)` (a loose fragment divorced from the body; contains the WHOLE hyperlink when the break is inside one). **`following_paragraph_fragment`** — `follows_all_content()` true → `[]`; else `Paragraph(following_fragment_p(), parent_)` (excludes the hyperlink). Both getters CAN raise `mat2doc:ValueError` (non-first break in the paragraph) — faithful; MATLAB auto-display catches the erroring getter and omits the row. `precedes_all_content`/`follows_all_content` are no-arg oxml methods used as booleans (H4). **Completes the P4 API tier → styles (P4-6/P4-7) next → M2.** **0 new D#**. |
| `styles/style.py` — `StyleFactory` (module fn) + `BaseStyle` / `CharacterStyle` / `ParagraphStyle` / `_TableStyle` / `_NumberingStyle` (5 classes) | `+mat2doc\+styles\` (`StyleFactory.m` + `BaseStyle.m` / `CharacterStyle.m` / `ParagraphStyle.m` / `TableStyle_.m` / `NumberingStyle_.m`) | P4-7a | **The styles API/proxy tier — SECOND WP of the styles chain (P4-6 → P4-7a → P4-7b → M2); API/proxy tier so NO registry row, NO oxml logic, NO serialization change → behavioral equivalence, byte-neutral (M1 17/17).** `StyleFactory` is a **module fn** → a package function (design.md §1) dispatching `WD_STYLE_TYPE` → leaf class via an explicit `isequal`/elseif chain (H10), reproducing the Python dict `{...}[style_elm.type]` incl. the **KeyError on None** edge (absent `@w:type` → `mat2doc:KeyError` msg `None`). `BaseStyle < mat2doc.shared.ElementProxy & matlab.mixin.Heterogeneous` — the **heterogeneous root** so `Styles.to_array` can hold a mixed `1×N` array of the leaf subclasses (design.md §2; `eq`/`ne` left UNSEALED — only scalar `eq` is used). All properties reach the P4-6 `CT_Style` `.._val` helpers: `builtin` (`not customStyle`, H4), `name` (via BabelFish `internal2ui`, the faithful write-verbatim/read-translate quirk), `priority`/`style_id`/`type` (None→PARAGRAPH), the 4 CT_OnOff booleans (getters return logical `false` when absent, H3). **`delete()`→`delete_()`** (H17 proxy-layer resolution — see idiom row). `CharacterStyle` adds `base_style`/`font`; `ParagraphStyle` adds `next_paragraph_style` (self-fallback matrix)/`paragraph_format`; **`_TableStyle`→`TableStyle_`**, **`_NumberingStyle`→`NumberingStyle_`** (FLAG-3 private-class rename — see idiom row). **0 new D#**. |
| `styles/styles.py` — `Styles(ElementProxy)` (the styles collection: `__contains__`/`__getitem__`/`__iter__`/`__len__` + `add_style`/`default`/`get_by_id`/`get_style_id`/`latent_styles` + the private `_get_by_id`/`_get_style_id_from_name`/`_get_style_id_from_style`) | `+mat2doc\+styles\Styles.m` | P4-7a | `mat2doc.styles.Styles`; `< mat2doc.shared.ElementProxy` (H5 element-identity `eq`/`ne` inherited). Wraps the `<w:styles>` root (a P4-6 `CT_Styles`). **VERIFY-COLLECTION** (accepted at Gate 2): the shared 1-based `RedefinesParen` `()` base is a **future WP**, so per the TabStops precedent the Python sequence dunders port as explicit methods — `__contains__`→`contains_`, `__getitem__`→`getitem_`, `__iter__`→`to_array`, `__len__`→`len_`. **H1 none** (keys are style names/ids, no integer shift). `getitem_` looks up by UI name (via BabelFish) then falls back to the **deprecated by-id** path emitting a `mat2doc:UserWarning` (the project's FIRST ported `warnings.warn`; house convention `mat2doc:<PyWarningCategory>`), else `mat2doc:KeyError`. `get_by_id`/`get_style_id`/`default`/`add_style` (dup→`mat2doc:ValueError`, wrong-type→`ValueError` with the `NAME (value)` enum-str) all H3/H4-faithful. **`latent_styles` stays a clean `mat2doc:notYetPorted` stub naming P4-7b** (the LatentStyles proxy). **0 new D#**. |
| `styles/__init__.py` — `BabelFish` (UI↔internal style-name translation) | `+mat2doc\+styles\BabelFish.m` | P4-7a | `mat2doc.styles.BabelFish` — an all-static class. The **module-vs-class flattening** (FLAG-3): BabelFish lives in Python's `docx.styles` package `__init__`, `StyleFactory`/`BaseStyle`/… in `docx.styles.style`, and `Styles` in `docx.styles.styles`, but the Mat2Doc port **flattens all three modules into the single `+mat2doc\+styles` package** (no module-mirroring sub-packages). The 12 special-case alias pairs (`Caption`/`Footer`/`Header`/`Heading 1`…`Heading 9`) are held as a Constant Nx2 `[UI, internal]` string array; Python's two dicts (`internal_style_names` = `dict(style_aliases)`, `ui_style_names` = the inverse) map to a column-1 scan (`ui2internal`) / column-2 scan (`internal2ui`). **H15**: lookups are EXACT, case-sensitive `==` (Python `dict.get`); unknown names pass through verbatim both directions. **0 new D#**. |
| `styles/latent.py` — `LatentStyles(ElementProxy)` (the latent-styles collection: `__getitem__`/`__iter__`/`__len__` + `add_latent_style`/`default_priority`/`default_to_hidden`/`default_to_locked`/`default_to_quick_style`/`default_to_unhide_when_used`/`load_count`) + `_LatentStyle(ElementProxy)` (a `w:lsdException` override: `name`/`priority`/`hidden`/`locked`/`quick_style`/`unhide_when_used` + `delete`) | `+mat2doc\+styles\LatentStyles.m` + `+mat2doc\+styles\LatentStyle_.m` | P4-7b | **The FINAL styles-chain WP — un-stubs `Styles.latent_styles` → reaches ★ M2.** Both `< mat2doc.shared.ElementProxy` (H5 element-identity `eq`/`ne` inherited); wrap the P4-6 `CT_LatentStyles` (`<w:latentStyles>`) / `CT_LsdException` (`<w:lsdException>`). **API/proxy tier — NO registry row, NO oxml logic, NO serialization change → behavioral + serialized-bytes parity, byte-neutral** (M1 17/17; latent WRITE path `s0034` 17/17, only `styles.xml` changes, SHA `3981d463…ab53`). **VERIFY-COLLECTION** (same TabStops/Styles precedent): the sequence dunders port as explicit methods — `__getitem__`→`getitem_` (by style NAME, so **no H1 shift**; miss→`mat2doc:KeyError` `no latent style with name '<key>'` verbatim), `__iter__`→`to_array`, `__len__`→`len_`; `add_latent_style` appends a `w:lsdException` (public `add_lsdException`, D-delta-4) + sets `@w:name` via `BabelFish.ui2internal`. **H3 tri-state SPLIT:** `LatentStyles.default_to_*` read `CT_LatentStyles.bool_prop`→logical **`false`** when absent (effective default; setters via `set_bool_prop`, **F-1** `bool(None)→False→"0"`), whereas `LatentStyle_.hidden`/`locked`/`quick_style`/`unhide_when_used` read `CT_LsdException.on_off_prop`→the **RAW** `{[]/true/false}` (`[]`=inherit the parent default). `default_priority`/`load_count`/`priority`→`int`|`[]`; `name` reads the REQUIRED `@w:name` (no guard) through `internal2ui`. **`_LatentStyle`→`LatentStyle_`** FLAG-3 rename; **`delete()`→`delete_()`** H17 proxy-layer rename (see idiom row) — byte-identical to python-docx `_LatentStyle.delete()`. **VERIFY-2:** the WP-brief `styles`/`count`/`element`/`default_to_builtin` members do NOT exist in v1.2.0 — ported to the SOURCE (10 `LatentStyles` + 7 `LatentStyle_` members). **0 new D#**. |
| `document.py::Document.add_heading`/`add_paragraph` + `blkcntnr.py::BlockItemContainer.add_paragraph`/`_add_paragraph`/`paragraphs` (the paragraph-authoring adder chain — **un-stubbed**) | `+mat2doc\+document\Document.m` (`add_heading`/`add_paragraph`) + `+mat2doc\BlockItemContainer.m` (`add_paragraph`/`add_paragraph_`/`paragraphs`) | P4-7b | **The ★ M2 authoring path — un-stub of the adders stubbed since P1-8/P2-3.** `Document.add_heading(text="", level=1)` (H13 defaults) maps `level`→style (`0`→"Title", `1`→"Heading 1", `2..9`→"Heading N") with the **H1 no-shift** (level is a DATA value, used verbatim in the `0-9` bound test AND `"Heading %d"`), raises `mat2doc:ValueError` `level must be in range 0-9, got %d` (verbatim) outside `0-9`, then delegates `add_paragraph(text, style)`. `Document.add_paragraph(text="", style=[])` → `body_().add_paragraph(...)`. `BlockItemContainer.add_paragraph` is the real worker: fresh `<w:p>` via the LIVE `CT_Body.add_p()` (P2-3) wrapped as a P4-5b `Paragraph`, then **`if text`** (H4 non-empty → `add_run(text)`) and **`if style is not None`** (identity, `~isequal(style,[])` → `paragraph.style = style`, resolved by NAME through the P4-7a un-stubbed chain); `paragraphs` mints a fresh `Paragraph` per `CT_Body.p_lst` (homogeneous `1×N`). `_add_paragraph`→`add_paragraph_` (design.md §2 rotation). **M2 byte proof:** `Document(); add_heading(_,0/1/2); add_paragraph(); save()` → `document.xml` **1865 B `a71e5502…f7c2c` 17/17 L1** (only `document.xml` differs from M1; `styles.xml` UNCHANGED — styles resolved by name), Word-COM clean. `Document.paragraphs`/`add_page_break` stay honest stubs (deps live; out of P4-7b's named scope). **0 new D#**. |
| `oxml/settings.py::CT_Settings` (the `<w:settings>` root element: the 98-tag `_tag_seq`, the `evenAndOddHeaders` `ZeroOrOne` descriptor + the derived `evenAndOddHeaders_val` boolean `@property`) | `+mat2doc\+oxml\+settings\CT_Settings.m` + **2 registry rows** (`w:settings`→`CT_Settings` `oxml/__init__.py:134`; `w:evenAndOddHeaders`→`shared.CT_OnOff` `:83`) | P5-1 | The **first Phase-5 WP** — registry-adding + M1-byte-path-touching. `_tag_seq` (98 tags) → the Constant `TAG_SEQ`; the H11 slice `successors=_tag_seq[48:]` → `TAG_SEQ(49:end)` (own tag `w:evenAndOddHeaders` at Python idx 47 = `TAG_SEQ(48)`, first successor `w:bookFoldRevPrinting` at `TAG_SEQ(49)`) — proven byte-for-byte adversarially. `evenAndOddHeaders_val` is the **H3 tri-state** over `CT_OnOff` presence (absent→`false`; set `true`→empty `<w:evenAndOddHeaders/>` per **D-delta-1**; set `false`/`[]`→remove), with the **H4 identity** guard `isequal(value,[]) \|\| (islogical(value) && isscalar(value) && ~value)` (Font.m:315-317 idiom) so a double `0` CREATES `w:val="0"` rather than removing. The **second registry row is a hard functional dependency** (`.val` on a generic `XmlElement` does not exist), byte-neutral on M1 (default `settings.xml` has no `w:evenAndOddHeaders`); sibling `w:titlePg` (`:84`) DEFERRED to the P5 section tier. Registering `w:settings` is **byte-neutral** — `word/settings.xml` (2535 B, SHA `51a0d348…d73689`) transits `CT_Settings` on load, M1 17/17 held; the write/remove paths are byte-identical to python-docx (`s0037`/`s0038`). **0 new D#** [adopt-only D-001/D-serializer-nsdecl/D-delta-1/D-STYPE-*/D-zip-time]. |
| `settings.py::Settings` (the document-settings proxy — the one member `odd_and_even_pages_header_footer` read/write) | `+mat2doc\+settings\Settings.m` + **un-stubs** `SettingsPart.settings`/`DocumentPart.settings`/`Document.settings` | P5-1 | A thin `Settings < mat2doc.shared.ElementProxy` over the `<w:settings>` root (a `CT_Settings`); H5 element-identity `eq`/`ne` + the `part` accessor inherited from `ElementProxy`. `odd_and_even_pages_header_footer` is a **Dependent `get.`/`set.` property** (house convention for a read/write proxy `@property`, e.g. `Font.bold`) delegating straight to the element's `evenAndOddHeaders_val` — no local state; `Settings(element, parent=[])` mirrors `__init__(element, parent=None)`. The three un-stubs resolve the settings delegation end-to-end (`SettingsPart.settings`→`Settings(settings_)`, `DocumentPart.settings`→`settings_part_().settings`, `Document.settings`→`part_.settings`), all wrapping the **same** element (H5 eq-chain: `d.settings == d.part.settings`). **Behavioral equivalence, byte-neutral. 0 new D#**. |
| `oxml/section.py` — `CT_SectPr` / `CT_PageSz` / `CT_PageMar` / `CT_SectType` / `CT_HdrFtrRef` (the section-properties element core: page geometry, section start, title-page, header/footer references) | `+mat2doc\+oxml\+section\` (5 files) + **7 `registry.m` main-map rows** (`w:titlePg`→`shared.CT_OnOff` `__init__.py:84` [P5-1 deferral CLOSED]; `w:footerReference`/`w:headerReference`→`CT_HdrFtrRef` `:123`/`:126`; `w:pgMar`→`CT_PageMar` `:127`; `w:pgSz`→`CT_PageSz` `:128`; `w:sectPr`→`CT_SectPr` `:129`; `w:type`→`CT_SectType` `:130`) | P5-2a | **The section-oxml element core — SECOND Phase-5 WP; registry-adding + byte-critical on the M1-central `word/document.xml`.** All 5 CT `< mat2doc.oxml.BaseOxmlElement`. **`CT_SectPr`** (`<w:sectPr>` root): `_tag_seq` (20 tags) → the Constant `TAG_SEQ`; the H11 slices `successors=_tag_seq[N:]`→`TAG_SEQ(N+1:end)` (own-tag 1-based idx 3 `type`→`(4:end)`, idx 4 `pgSz`→`(5:end)`, idx 5 `pgMar`→`(6:end)`, idx 14 `titlePg`→`(15:end)`; `headerReference`/`footerReference` are `ZeroOrMore` NOT in `_tag_seq`→ successors span the whole sequence, so refs sort before `type`/`pgSz`/…). Page-geometry accessors read/write `w:pgSz`/`w:pgMar` children (`page_width`/`page_height` + 7 margins) with the faithful WRAP-vs-DIRECT setter ASYMMETRY (`bottom_margin`/`footer`/`gutter`/`header`/`left_margin` WRAP `int|Length|None` via `asLengthOrNone_`; `top_margin`/`right_margin`/`page_height`/`page_width` DIRECT `Length|None`). `orientation` H4 (**NO w/h swap** — appends `@w:orient` on the portrait dims), `start_type` IDENTITY setter (`is None or is NEW_PAGE`→remove), `titlePg_val` `[None,False]` `==`-membership breadth (D-delta-1 empty `<w:titlePg/>`), the six add_/get_/remove_ header/footer methods (PUBLIC 2-arg adders SHADOW the generated `add_x` per xmlchemy hasattr guard) + two `_lst` getters, `clone` (deepcopy+attrib.clear), `preceding_sectPr` (H1); `iter_inner_content` = clean `mat2doc:notYetPorted` stub (owner P5-2b). **`CT_PageSz`** (w/h `ST_TwipsMeasure` default None; `orient` `WD_ORIENTATION` **NON-None default PORTRAIT** → set==PORTRAIT removes `@w:orient`). **`CT_PageMar`** (7 margins; **top/bottom `ST_SignedTwipsMeasure` signed** vs right/left/header/footer/gutter `ST_TwipsMeasure` unsigned — transcribed exactly). **`CT_SectType`** (`val` `WD_SECTION_START` default None; the `<w:sectType>` docstring but `w:type` tag). **`CT_HdrFtrRef`** (RequiredAttribute `type_` `WD_HEADER_FOOTER` + `rId` `XsdString` `@r:id` — plain string, NO relationship wiring [P5-3b]; `type_` verbatim, no rotation). Registering the 7 tags is **byte-neutral** — `word/document.xml` (1548 B, SHA `0e4dd503…`) transits `CT_SectPr` on load, M1 17/17 preserved; the geometry-write/LANDSCAPE paths are byte-identical to python-docx (`s0040`/`s0041`). The loose-element multi-ref `r:` auto-prefix is the pre-existing SIGNED **D-nsprefix-rewrite** (dead-on-generation, rooted path byte-clean). **0 new D#**. |
| `oxml/section.py` — `CT_HdrFtr` (the `<w:hdr>`/`<w:ftr>` header/footer PART root) + `_SectBlockElementIterator` (the section block-element walk) | `+mat2doc\+oxml\+section\CT_HdrFtr.m` + `+mat2doc\+oxml\+section\SectBlockElementIterator_.m` + **2 `registry.m` rows** (`w:ftr`→`CT_HdrFtr` `oxml/__init__.py:124`; `w:hdr`→`CT_HdrFtr` `:125`) + `CT_SectPr.iter_inner_content` **un-stubbed** | P5-2b | **Completes the section-oxml layer — registry-adding but M1-NEUTRAL** (`w:hdr`/`w:ftr` are SEPARATE-PART roots absent from `default.docx`, so the rows touch no M1 parse path — byte-neutral AND flip-neutral, no exact-class pin sees a w:hdr/w:ftr class; M1 17/17 unchanged, ZERO re-pins). **`CT_HdrFtr`** (`< mat2doc.oxml.BaseOxmlElement`): the docx-form `ZeroOrMore` p/tbl descriptor family (`p_lst`/`new_p_`/`insert_p_`/`add_p_`/`add_p` + tbl analogues, `successors=()`→`NO_SUCCESSORS` append-at-end, D-delta-4 public adder, NO bare getter/get-or-add/remover), and `inner_content_elements` = `xpath("./w:p | ./w:tbl")` — a **TAG-BASED** child union (NOT isinstance): an unregistered `w:tbl` resolves to a **generic `XmlElement`** (the CT_Body.tbl_lst precedent, LIVE since P2-3) and is **INCLUDED in document order** (never dropped), a `w:p` nested in `w:ins` is EXCLUDED (fixed child union, not a descendant scan); H9 materialized 1×N heterogeneous array (typed-empty when none), H5 live child handles. Ports COMPLETE with **zero stubs** (the only table-branch decision is `Section.iter_inner_content`'s isinstance dispatch → P5-3a, owner P6-4a). **`_SectBlockElementIterator`→`SectBlockElementIterator_`** (FLAG-3, leading-underscore methods rotate: `_iter_sect_block_elements`→`iter_sect_block_elements_`, etc.): the H9 **generator→materialized array** (laziness unobservable — consumers don't mutate the tree during iteration); the boundary strategy (all blocks in-and-above this section, skip the prior sections' count) over the mutually-exclusive p-sect (`./parent::w:pPr/parent::w:p`) / body-sect (`self::w:sectPr[parent::w:body]`) xpath shapes + `preceding-sibling::*[self::w:p \| self::w:tbl]`, doc-ordered (docSortDedupe==lxml); **H1** (`sectPr_idx==0`→`idx==1`, `sectPrs[idx-1]` predecessor, `[n_blks_to_skip:]`→`(n_blks_to_skip+1:end)`, empty `1×0` at the boundary), **H5** identity `find(sectPrs==sectPr,1)` (not-found→`mat2doc:ValueError` faithful to `list.index()`); the two compiled-xpath caches + the separate `count(...)` xpath DROPPED as unobservable (`numel` of the same node-set). Un-stubs **`CT_SectPr.iter_inner_content`** COMPLETELY (one-line delegation to the static entry). Gate-3: header-part round-trip **byte-identical** (`s0043` real header, extends D-001 to a header PART), 4-doc/11-section partition corpus == the python-docx oracle (`s0044`, concat==body no-drop/no-dup, all edges). **0 new D#**. |
| `section.py` — `Section` (the section- and page-setup proxy) + `Sections` (the section sequence) | `+mat2doc\+section\` (`Section.m`, `Sections.m`) | P5-3a | **The section API/proxy tier — no registry row, no oxml logic, no serialization change → behavioral + serialized-bytes parity, byte-neutral (M1 17/17).** Both are **plain-object** proxies (`class Section:` / `class Sections(Sequence[Section])` — NOT `ElementProxy`), so `classdef … < handle` with **default handle identity and NO `eq`/`ne`** (Python default object identity). **`Section`**: all 12 geometry/type accessors are `Dependent` `get.`/`set.` delegating **one-to-one** to the same-named `CT_SectPr` member (the two distance aliases `footer_distance`→`sectPr.footer`, `header_distance`→`sectPr.header`, and `different_first_page_header_footer`→`titlePg_val`) — every H6/H3/H4/H10 hazard (orientation NO-w/h-swap, start_type identity, titlePg breadth) lives in `CT_SectPr`, this tier is a transparent pass-through; `part` returns `document_part_` (the `ProvidesStoryPart` hook — `iter_inner_content` mints `Paragraph(element, self)`, so the `Section` is the block item's parent); `iter_inner_content` (C2) wraps each `CT_P` as a `Paragraph` (`1×N` cell, H9 materialized) and **RAISES `mat2doc:notYetPorted` owner P6-4a** on the `w:tbl`→`Table` branch (never a silent drop). The **six** `_Header`/`_Footer`-returning members (`even_page_*`/`first_page_*`/`footer`/`header`) are clean `mat2doc:notYetPorted` stubs → **P5-3b**. **`Sections`**: the Python `Sequence` dunders → the **interim explicit methods** `getitem_` (`__getitem__`), `to_array` (`__iter__`), `len_` (`__len__`) — see the dunder row; H5 mints a FRESH `Section` per access; `document_elm_`/`document_part_` (underscore rotation). **0 new D#**. |
| `document.py::Document.sections`/`add_section` + `oxml/document.py::CT_Body.add_section_break` — the section-authoring **un-stubs** (`Document._block_width` goes transitively live) | `+mat2doc\+document\Document.m` (`sections`/`add_section`/`block_width_`) + `+mat2doc\+oxml\+document\CT_Body.m` (`add_section_break`) | P5-3a | **C1 un-stub — NO registry rows; the `add_section` novel authoring byte path.** `Document.sections`→`Sections(element_, part_)`; `Document.add_section(start_type=WD_SECTION.NEW_PAGE)` (H13) = `body.add_section_break()` → `new_sectPr.start_type = start_type` → `Section(new_sectPr, part_)` (faithful 3-liner). **`CT_Body.add_section_break`** (`oxml/document.py:51-71`): `get_or_add_sectPr()` fetches the sentinel; `add_p().set_sectPr(sentinel.clone())` nests the CLONE (carrying the prior section's geometry + hdr/ftr refs) in a **new trailing `w:p`** inserted BEFORE the body sentinel (`p` successors=`("w:sectPr",)`, H11) — the intermediate break; then the hdr/ftr refs are removed from the **sentinel only** (xpath union materialized before the removal loop, H9), and the SAME sentinel handle is returned (H5) — clone-before-removal is load-bearing (the intermediate keeps the refs). Deps all LIVE: `CT_SectPr.clone` (P5-2a), `CT_P.set_sectPr` (P4). **`Document.block_width_`** goes transitively live — `self.sections[-1]` now reaches the real collection (`s.getitem_(-1)` replaces the stub-era `sections()(end)` placeholder that assumed a plain array); reached only via `add_table` (P6 stub), byte-neutral, unpinned. The default `Document().save()` fires ZERO stubs → M1 17/17 (`document.xml` `0e4dd503…` unchanged); `add_section` (5 start types + 2/3-section chains) + property-write byte-identical to python-docx (`s0046`–`s0051`). **0 new D#**. |
| `parts/hdrftr.py` — `HeaderPart` / `FooterPart` (the `word/headerN.xml` / `word/footerN.xml` PART classes + their `new` factory / `_default_header_xml` / `_default_footer_xml` template readers) + `templates/default-header.xml` / `default-footer.xml` | `+mat2doc\+parts\HeaderPart.m` + `FooterPart.m` + `+mat2doc\templates\default-header.xml` / `default-footer.xml` (byte-identical copies) | P5-3b | **The FIRST runtime-added parts in Mat2Doc** (risk-register #4). Both `< mat2doc.parts.StoryPart` (`< XmlPart`): PARSE on load, RE-SERIALIZE on save through `serialize_part_xml` — **inherit `blob`/`element`/`paragraphs`/`add_paragraph` unchanged**, adding only `new(package)` (`next_partname("/word/header%d.xml")` — the `%d` is **DATA**, H1-safe; `content_type = CT.WML_HEADER/WML_FOOTER`; `element = parse_xml(default_header_xml_())`) and their **OWN static `load`** (the **inherited-static trap** — MATLAB does not dispatch an inherited static to the calling subclass; without it the PartFactory flip silently builds a base `XmlPart`, `opc/part.py:229-232` `cls=HeaderPart`). **PartFactory flip `WML_HEADER→HeaderPart` / `WML_FOOTER→FooterPart` is BYTE-NEUTRAL** (inherited `XmlPart.blob` → identical bytes; no M1 fixture loads a hdr/ftr part) — inert on the M1 sweep, `0` re-pins from the flip itself. Template read BINARY (`fopen(path,"rb")` + `fread(*uint8)` — CRLF preserved, the `StylesPart._default_styles_xml` precedent). docx ctor arg order `(partname, content_type, element, package)` — element third. **C4:** the two templates copied byte-identical (SHA vs clone `edced8ac…`/`3d7551c7…`, 1422 B each) + named rows in `+mat2doc\templates\.gitattributes` (`* binary` pin). Gate-3: header G-scenario 18/18 L1 (`header1.xml` `3c75200f`), reload round-trip `s0058` 19/19 L1 (HeaderPart/FooterPart.**load** byte-proven). **0 new D#**. |
| `section.py` — `_BaseHeaderFooter` / `_Header` / `_Footer` (the section-side header/footer proxies) + the SIX `Section` hdr/ftr members un-stubbed + the C3 `BlockItemContainer` element-seam + five `parts/document.py::DocumentPart` un-stubs (`add_header_part`/`add_footer_part`/`drop_header_part`/`header_part`/`footer_part`) + the `PartFactory` WML_HEADER/WML_FOOTER flip | `+mat2doc\+section\BaseHeaderFooter_.m` + `Header_.m` + `Footer_.m` (new) + `+mat2doc\+section\Section.m` (6 un-stubs) + `+mat2doc\BlockItemContainer.m` (C3 seam) + `+mat2doc\+parts\DocumentPart.m` (5 un-stubs) + `+mat2doc\+opc\PartFactory.m` (flip) | P5-3b | **FINAL P5 WP — completes the sections tier; behavioral + the FIRST runtime-added-part byte paths.** `_BaseHeaderFooter`→**`BaseHeaderFooter_`**, `_Header`→**`Header_`**, `_Footer`→**`Footer_`** (FLAG-3 leading-underscore rotation), all `< mat2doc.BlockItemContainer`. Python `__init__` stores `_sectPr`/`_document_part`/`_hdrftr_index` (→ `sectPr_`/`document_part_`/`hdrftr_index_`) and **does NOT call `super().__init__()`** — the MATLAB ctor MUST chain `obj@mat2doc.BlockItemContainer([], [])`, both placeholders **dead** (`element_()` and `part()` are overridden). `is_linked_to_previous` get (`not _has_definition`) / set (`bool(value)`→no-change guard→`_drop_definition` [inherit] / `_add_definition` [add empty def]) — the AUTHORING trigger. `_get_or_add_definition` = the **3-case inherit-walk** recursion (H9 plain recursion; H4 `if prior_headerfooter:`→`~isequal(prior,[])` object-truthy): case-1 own def / case-2 `preceding_sectPr` recurse / case-3 add-on-first-section. **Faithful hdr/ftr ASYMMETRY preserved verbatim:** `Header_.drop_definition_`→`document_part_.drop_header_part(rId)` but `Footer_.drop_definition_`→`document_part_.drop_rel(rId)` **DIRECT** (there is NO `drop_footer_part`, `section.py:414-417` vs `:460-463`). The six `Section` members return the right proxy + `WD_HEADER_FOOTER` index: `header`/`footer`→PRIMARY (`@lazyproperty` cached handle), `even_page_*`→EVEN_PAGE, `first_page_*`→FIRST_PAGE (plain `@property` fresh proxy each call). The **C3 element-accessor seam** (see the idiom row) makes `_element` a lazy override. Gate-3: full-package byte-identical across header/footer/both/even+first/add→drop/reload (`s0052`–`s0058`); **C3 M1 17/17 + M2 17/17 byte-neutral**; C5 Word COM sweep PASS (all 4 packages open clean, `DifferentFirstPage` rendered). Two by-design stub-battery re-pins at Gate-4 (`Test_p5_3a::test_p5_3b_stubs`, `Test_p2_2::twelve_feature_stubs`). **0 new D#**. |
| `oxml/table.py` — the 7 table LEAF classes: `CT_TblWidth` (`w:tcW` width union) / `CT_Height` (`w:trHeight`) / `CT_TblGrid` (`w:tblGrid`) / `CT_TblGridCol` (`w:gridCol`) / `CT_TblLayoutType` (`w:tblLayout`) / `CT_VerticalJc` (`w:vAlign`) / `CT_VMerge` (`w:vMerge`) | `+mat2doc\+oxml\+table\` (7 files, one per class) + **7 `registry.m` rows** (`w:gridCol`→`CT_TblGridCol` `__init__.py:171`, `w:tblGrid`→`CT_TblGrid` `:174`, `w:tblLayout`→`CT_TblLayoutType` `:175`, `w:tcW`→`CT_TblWidth` `:181`, `w:trHeight`→`CT_Height` `:183`, `w:vAlign`→`CT_VerticalJc` `:185`, `w:vMerge`→`CT_VMerge` `:186`) | P6-1 | **FIRST WP of Phase 6 (tables) — the LEAF classes only; container CTs (CT_Tbl/CT_Row/CT_Tc/CT_TblPr/CT_TblPrEx/CT_TrPr/CT_TcPr) DEFERRED to P6-2/P6-3.** All `< mat2doc.oxml.BaseOxmlElement`, delegating to the P1-3b typed-attr / child engine (no new engine code). **Registry-adding but M1-NEUTRAL + flip-neutral** (`default.docx` has no table — 0 of the 7 tags in any of the 17 M1 parts; M1 17/17 `0e4dd503…` unchanged, **zero re-pins**). **`CT_TblWidth`** — 2 RequiredAttributes (`w` XsdInt, `type` ST_TblWidth) + the `width` UNION `@property` (see the idiom row): `type!="dxa"`→None, else `Twips(w)`; the setter forces `type="dxa"`, `w=Emu(value).twips`. **`CT_Height`** — 2 OptionalAttributes (`val` ST_TwipsMeasure→Length, `hRule` WD_ROW_HEIGHT_RULE), H3 tri-state. **`CT_TblGrid`** — `ZeroOrMore("w:gridCol", successors=("w:tblGridChange",))` → docx-form members + public `add_gridCol` (D-delta-4); H11 insert-before-tblGridChange. **`CT_TblGridCol`** — OptionalAttribute `w` + `gridCol_idx` `@property` (0-based, H1 `find-1`, H5 identity, `mat2doc:ValueError` on absence). **`CT_TblLayoutType`** — OptionalAttribute `type` (ST_TblLayoutType fixed/autofit). **`CT_VerticalJc`** — RequiredAttribute `val` (WD_CELL_VERTICAL_ALIGNMENT, InvalidXmlError if `@w:val` absent). **`CT_VMerge`** — OptionalAttribute `val` (ST_Merge, **default="continue"** — the sole non-None default; `[]`/`"continue"`→remove, `"restart"`→write, `""`→ST_Merge ValueError). **C4 brief-correction:** register ONLY `w:tcW`→`CT_TblWidth`; upstream has NO `w:tblW` row (a `<w:tblW>` stays a plain element). Gate-3: M1 17/17 byte-neutral; `probe_diff` s0059 MATCH exit 0 (values + serhex + verbatim error msgs); table-leaf round-trip `references\s0060` 10/10 byte-identical (real 3×2 table subtrees + 4 loose width-union `w:tcW`); width union byte-proven both ways; regression 132/132 0 flips. **0 new D#** (adopt-only D-001/D-delta-4/D-zip-time). |
| `oxml/table.py` — the 4 table PROPERTY-CONTAINER classes: `CT_TblPr` (`w:tblPr` — alignment/autofit/style) / `CT_TblPrEx` (`w:tblPrEx` — bare exceptions container) / `CT_TrPr` (`w:trPr` — grid skips + row height) / `CT_Row` (`w:tr` — the row element) | `+mat2doc\+oxml\+table\` (4 files: `CT_TblPr.m`/`CT_TblPrEx.m`/`CT_TrPr.m`/`CT_Row.m`) + **7 `registry.m` rows** (`w:gridAfter`→`shared.CT_DecimalNumber` `__init__.py:169`, `w:gridBefore`→`shared.CT_DecimalNumber` `:170`, `w:tblPr`→`CT_TblPr` `:176`, `w:tblPrEx`→`CT_TblPrEx` `:177`, `w:tblStyle`→`shared.CT_String` `:178`, `w:tr`→`CT_Row` `:182`, `w:trPr`→`CT_TrPr` `:184`) | P6-2 | **SECOND WP of Phase 6 — the property containers; the FIRST NON-neutral table WP** (registering `w:tblPr` puts the 100 `<w:tblPr>` nodes in `word/styles.xml`/`stylesWithEffects.xml` [inside the shipped table styles] on the LIVE M1 parse path — a registry-flip byte-critical WP, the P4-6 pattern; proven byte-neutral by the M1 17/17 `styles.xml` `02d71a68…bc30e384`/349 458 B match — registering changes only a parsed node's CLASS). All `< mat2doc.oxml.BaseOxmlElement`. **`CT_TblPr`** (`_tag_seq` 18 tags → `TAG_SEQ`; 4 `ZeroOrOne` on the H11 slices tblStyle `[1:]`→`(2:end)`/bidiVisual `[4:]`→`(5:end)`/jc `[8:]`→`(9:end)`/tblLayout `[13:]`→`(14:end)`): `alignment` **★ A2 cross-enum** (see idiom row — reuses the single P4-2 `CT_Jc`, returns a `WD_PARAGRAPH_ALIGNMENT` member, `cast` is a runtime no-op; **no second `w:jc` row**), `autofit` (H4: false iff `<w:tblLayout @type="fixed">`, absent→true), `style` (`./w:tblStyle/@val` via the PRIVATE `_add_tblStyle` adder). **`CT_TblPrEx`** — bare pass-through container (no descriptors/attrs/properties; exists so a parsed `<w:tblPrEx>` resolves to a named class; byte-neutral). **`CT_TrPr`** (`_tag_seq` 15 tags; 3 `ZeroOrOne` — the gridBefore-declared-after-gridAfter-but-schema-ordered-BEFORE trap): `grid_after`/`grid_before` read-only (0 when absent, else `CT_DecimalNumber.val`), `trHeight_hRule`/`trHeight_val` get/set with the **None-guard** (`value is None and self.trHeight is None → return` — no empty `<w:trHeight>`). **`CT_Row`** (`tblPrEx`/`trPr` `ZeroOrOne` **CUSTOM inserters** — `_insert_tblPrEx`=`insert(0)`→`insert(1,…)` H1, `_insert_trPr`=`tblPrEx.addnext` else `insert(0)`; `tc` `ZeroOrMore`): `grid_after`/`grid_before`/`trHeight_*` delegate to `trPr`; **`tr_idx`** 0-based among sibling `<w:tr>` (Python `tbl.tr_lst.index(self)`; `CT_Tbl.tr_lst`≡`findall(qn("w:tr"))` so read the parent's `w:tr` directly — tag-based, no CT_Tbl/no stub, H1 `find-1`/H5 identity); the **CT_Tc BOUNDARY** (design.md §4): `_new_tc`=`CT_Tc.new()`→`new_tc_` raises `mat2doc:notYetPorted` owner **P6-3a** (the `tc` adders route through it → also raise, faithful), `tc_at_grid_offset` ported VERBATIM with the `tc.grid_span` access **isa-guarded** (→notYetPorted P6-3a; the `==grid_before`/`<grid_before` fast paths work now; runs unchanged once CT_Tc registers). **Deferral closures:** `w:tblStyle`→`CT_String` (P6-1 deferred; hard functional dep of `CT_TblPr.style`; M1-neutral — 0 `<w:tblStyle>` in default.docx) + `w:gridAfter`/`w:gridBefore`→`CT_DecimalNumber` (2 P4-6 deferrals). Gate-3: **M1 17/17 byte-neutral** (`styles.xml` `02d71a68…`, `document.xml` `0e4dd503…`); `probe_diff` s0061 MATCH exit 0 (A2 by-name/value compares); table-props round-trip `references\s0062` **6/6 byte-identical** (incl. the `<w:jc w:val="both"/>`→JUSTIFY edge `620dea7a…`); alignment cross-enum bytes byte-proven; registry 7 rows resolve, `w:jc` NOT re-registered; regression 107/107 0 flips. **0 new D#** (A2 cross-class `==` gap ruled no-D, design.md §2). |
| `oxml/table.py` — the table-cell tier: `CT_Tc` (`w:tc` — the cell MERGE engine) + `CT_TcPr` (`w:tcPr` — cell properties: grid span / vMerge / width / vAlign) | `+mat2doc\+oxml\+table\` (2 files: `CT_Tc.m` / `CT_TcPr.m`) + **3 `registry.m` rows** (`w:gridSpan`→`shared.CT_DecimalNumber` `__init__.py:172`, `w:tc`→`CT_Tc` `:179`, `w:tcPr`→`CT_TcPr` `:180`) + the `CT_Row` **tc un-stub** (`new_tc_`→`CT_Tc.new()`; `tc_at_grid_offset` grid_span guard removed) | P6-3a | **THIRD Phase-6 WP — the table-cell MERGE ENGINE, the single hardest WP of the port** (its own dedicated pre-launch plan-audit, risk-register #2; the re-scoped split = **P6-3a is CT_Tc + FULL CT_TcPr**, P6-3b is CT_Tbl only + the un-defer sweep). Both `< mat2doc.oxml.BaseOxmlElement`. **The SECOND NON-neutral table WP** — registering `w:tcPr` puts the **595 `<w:tcPr>` nodes** in `word/styles.xml` (+595 in `stylesWithEffects.xml`, inside the shipped `<w:tblStylePr>` overrides) on the LIVE M1 parse path (the P4-6/P6-2 pattern; proven byte-neutral by the M1 17/17 `styles.xml` `02d71a68…bc30e384`/349 458 B + `stylesWithEffects.xml` `463ae092…`/438 131 B match); `w:tc`/`w:gridSpan` have 0 occurrences in default.docx. **`CT_Tc`** (`table.py` 421-783): `tcPr` `ZeroOrOne` **custom inserter** (`_insert_tcPr`=`insert(0)`→`insert(1,…)` H1, tcPr ALWAYS the first child), `p`/`tbl` `ZeroOrMore` (`_new_tbl` override raises `NotImplementedError` — faithful); the read tier `grid_span`/`vMerge`/`width` (tcPr delegators), `grid_offset`/`left`/`right` (0-based grid, RAW), **`top`/`bottom` ZERO-ARG METHODS not Dependent props (V1** — see idiom row); and the MERGE write tier `merge`→`_span_dimensions`(raise_on_inverted_L/tee → `mat2doc:InvalidSpanError` "requested span not rectangular")→`_grow_to`(the vMerge_val closure)→`_span_to_width`→`_swallow_next_tc`("not enough grid columns"/"span is not rectangular") + `_move_content_to`(H9 snapshot MOVE)/`_add_width_of`(H4 falsy-zero)/`_remove_trailing_empty_p`/`_remove`. **The Tc→Tbl `tr_lst` shim (V2 — RESOLVED at P6-3b):** through P6-3a `CT_Tbl` was unregistered, so `_tbl` was a generic `w:tbl` ancestor and `self._tbl.tr_lst` was realized by the private `trLstOfTbl_`=`tbl.xpath("./w:tr")` — the identical CT_Row handle list; **P6-3b registers `w:tbl`→`CT_Tbl` and REMOVES the shim** (the 3 consumers `merge`/`tr_below_`/`tr_idx_` now call `self._tbl.tr_lst` directly, byte-neutral, identity-safe, nearest-ancestor-correct incl. nested; merge byte-matrix 10/10 byte-identical after the swap). **`CT_TcPr`** (`table.py` 784-891): `_tag_seq` 18 tags → `TAG_SEQ`; 4 `ZeroOrOne` on the H11 slices tcW `[2:]`→`(3:end)`/gridSpan `[3:]`→`(4:end)`/vMerge `[5:]`→`(6:end)`/vAlign `[12:]`→`(13:end)`; `grid_span` (H4 strict — writes `@w:val` only `if >1`), `vMerge_val` (the **bare-`<w:vMerge/>`** mechanism — see idiom row), `width`, `vAlign_val`. Whole class ported — nothing deferred (upstream CT_TcPr is already minimal). Gate-3: **M1 17/17 byte-neutral**; **the MERGE byte-matrix `references\s0063` 10/10 byte-identical** (horizontal / vertical-bare-vMerge / block / rowspan / into-hspan / into-vspan / width-sum 1440+1440→2880 / H4-zero-skip / sdt-passenger / nested-table; frozen as the permanent table-merge oracle); `probe_diff` s0064 MATCH exit 0 (geometry / handle-identity `_tr_idx`=`[0 1 2]` / get-set incl bare vMerge + H4 strict / H11 tcPr child order byte-identical / un-stub); 9/9 invalid-span raises verbatim; regression 69/69 0 flips (the 4 P6-2 tc-boundary stub-throws fixed in-WP now live). **0 new D#** (adopt-only D-001 own parser/serializer, D-delta-1 bare-vMerge via CT_VMerge default, D-zip-time envelope). |
| `oxml/table.py` — the table ROOT `CT_Tbl` (`w:tbl` — the `new_tbl` constructor + the whole-table accessors) + the **tbl UN-DEFER sweep** | `+mat2doc\+oxml\+table\CT_Tbl.m` (1 file) + **2 `registry.m` rows** (`w:bidiVisual`→`shared.CT_OnOff` `__init__.py:168`, `w:tbl`→`CT_Tbl` `:173`) + the `CT_Tc` **V2 shim resolution** (`trLstOfTbl_`→`self._tbl.tr_lst`) | P6-3b | **FOURTH & FINAL table-oxml WP — the table ROOT `CT_Tbl`; COMPLETES the table oxml layer (all 14 `table.py` element classes) and the registry (no more table rows).** `< mat2doc.oxml.BaseOxmlElement`. **Registry-adding but M1-NEUTRAL** — `default.docx` has no `<w:tbl>` root (styles.xml table-STYLE defs use `tblPr`/`tcPr`, not a `w:tbl` root), so `w:tbl`/`w:bidiVisual` transit nothing on M1 (M1 17/17 `styles.xml` `02d71a68…`/`document.xml` `0e4dd503…` unchanged). **`CT_Tbl`** (`table.py` 146-256): 2 `OneAndOnlyOne` (`tblPr`/`tblGrid` → `getRequiredChild`, getter-only, `InvalidXmlError` on absent) + `tr` `ZeroOrMore` (docx-form `tr_lst`/`new_tr_`/`insert_tr_`/`add_tr_` + PUBLIC `add_tr` D-delta-4; default creator → `CT_Row`); the whole-table accessors `bidiVisual_val` (get/set — `WD_TABLE_DIRECTION` int-enum `bool(value)`→`logical(double(value.value)~=0)` H10/H4; via `CT_OnOff.val` True default D-delta-1: RTL→**bare** `<w:bidiVisual/>`, LTR→`w:val="0"`, `[]`→removed), `col_count` (`numel(tblGrid.gridCol_lst)`), `iter_tcs` (Python generator → materialized `1×N` `CT_Tc`, **row-major** H9), `tblStyle_val` (get/set — `_remove_tblStyle()` UNCONDITIONALLY first, then the PRIVATE `_add_tblStyle` adder `add_tblStyle_`, not get-or-add); and the **★ `new_tbl(rows,cols,width)` constructor** — see the idiom row (verbatim `_tbl_xml`/`_tblGrid_xml`/`_trs_xml`/`_tcs_xml` builders, H6 EMU-floor col-width, H14 twips via `pyStr`, NO `<w:tblStyle>` — style is API-tier P6-4a). **The UN-DEFER sweep:** registering `w:tbl`→`CT_Tbl` auto-upgrades every prior tag-based `w:tbl`→generic site (`CT_Body`/`CT_HdrFtr`/`CT_SectPr`/`SectBlockElementIterator_`/`CT_Tc` — node-set unchanged, only the element CLASS flips generic→`CT_Tbl`, no code change) and **resolves the P6-3a `CT_Tc` `trLstOfTbl_` shim** to the real `self._tbl.tr_lst` (V2); the proxy layer does NOT flip (keyed on the absent `Table` proxy, P6-4a). Gate-4 re-pins `Test_p2_3_document_shell:291` + `Test_p5_2b_hdrftr_oxml:132` (3 assertions / 2 methods — the registry-flip stale-pin pattern). Gate-3: **M1 17/17 byte-neutral**; the **new_tbl byte oracle `references\s0065` 7/7 byte-identical** (1×1, the headline 2×3/Inches(6) `10211e87…`/835 B, 3×2 raw-EMU, 5×4, the **non-even** 2×7/Inches(6.5) `5943600//7=849085` EMU→1337 twips H6 floor, cols==0→Emu(0), negative floor — frozen as the permanent table-authoring oracle); **the merge byte-matrix `references\s0063` re-proven 10/10 byte-identical** with the V2 swap live (incl. m10 nested nearest-ancestor); `probe_diff` s0066 MATCH exit 0 (descriptors / add_tr / iter_tcs row-major / bidiVisual RTL-bare/LTR-`val=0`/None / tblStyle set/reset/None / VERIFY-1 no-tblStyle); targeted regression FOREGROUND 83/81 with only the 2 expected flips. **0 new D#** (adopt-only D-001, D-delta-1, D-zip-time). |
| `table.py` — the table API tier: `Table` + `_Columns`/`_Column`/`_Rows`/`_Row` (the table proxy + the row/column collections and single-row/column proxies reading `CT_Tbl`/`CT_Row`/`CT_TblGridCol`) | `+mat2doc\+table\` (5 files: `Table.m`, `Columns_.m`, `Column_.m`, `Rows_.m`, `Row_.m`) | P6-4a | **The table API/proxy tier — NO registry row, NO oxml logic, NO serialization change → behavioral parity, byte-neutral (M1 17/17).** The four leading-underscore proxy classes take the **FLAG-3 trailing-underscore rotation** `_X`→`X_` (`_Columns`→`Columns_`, `_Column`→`Column_`, `_Rows`→`Rows_`, `_Row`→`Row_` — same convention as `_TableStyle`→`TableStyle_`, `_Relationship`→`Relationship_`); public `Table` keeps its name. **`Table`** (`class Table(StoryChild)`→`< mat2doc.shared.StoryChild`, default handle identity, NO `eq`/`ne`; stores the `<w:tbl>` twice — `element_`/`tbl_`, the `Paragraph` `p_`/`element_` precedent): `alignment` **★ A2 cross-enum** (delegates `_tblPr.alignment` → a `WD_PARAGRAPH_ALIGNMENT` member VERBATIM, compare by name/`.value` — see the A2 idiom row; the setter takes a `WD_TABLE_ALIGNMENT` value, cross-enum `to_xml` resolves by int), `autofit`/`style`/`table_direction` (delegate `_tblPr.autofit` / `_tbl.tblStyle_val`+`part.get_style` / `_tbl.bidiVisual_val` — the last returns `[]` or a **logical**, python's `cast` a no-op), `rows`/`columns` (`@lazyproperty` cached, computed-flag idiom — NEVER `isempty` sentinel), `table` (returns `obj`, the child→parent terminus), `_column_count`/`_tblPr` (public zero-arg methods `column_count_`/`tblPr_` — upstream tests read them). **`Columns_`/`Rows_`** (`< Parented`): the Python `Sequence` dunders → the interim explicit `getitem_`/`to_array`/`len_` (the `RedefinesParen` base is a future WP, VERIFY-COLLECTION); **0-based `getitem_`** (H1 `+1`; negative wrap) — `Columns_` INT-ONLY with the custom `"column index [%d] is out of range"` on the ORIGINAL idx, `Rows_` INT+SLICE (`list(self)[idx]`, standard `"list index out of range"`; slice=`struct(start,stop,step)`, `sliceIndices_` byte-identical to `Sections.sliceIndices_`). **`Column_`/`Row_`** (`< Parented`): `width`/`height` (`Length\|[]`, H6/H3), `height_rule` (`WD_ROW_HEIGHT_RULE\|[]`, H10/H3), `grid_cols_after`/`grid_cols_before` (int RO), `_index`→`index_()` (0-based DATA, H1, public). **P6-4b STUBS** (need `_Cell`): `Table.add_column`/`add_row`/`cell`/`column_cells`/`row_cells`/`_cells`, `Row_.cells`, `Column_.cells` — each raises `mat2doc:notYetPorted` owner P6-4b; no `Cell_.m` exists. **0 new D#**. |
| `document.py::Document.add_table`/`iter_inner_content` + `blkcntnr.py::BlockItemContainer.add_table`/`iter_inner_content` + `section.py::Section.iter_inner_content` (`w:tbl` branch) — the table-authoring **un-stubs** (the FIRST end-to-end table) | `+mat2doc\+document\Document.m` (`add_table`/`iter_inner_content`) + `+mat2doc\BlockItemContainer.m` (`add_table`/`iter_inner_content`) + `+mat2doc\+section\Section.m` (`iter_inner_content` `w:tbl` branch) | P6-4a | **The authoring un-stub — NO registry rows; the `add_table` novel authoring byte path (deps all live: `CT_Tbl.new_tbl` byte-proven P6-3b, `CT_Body._insert_tbl` live P2-3, the `Table` proxy now live).** `BlockItemContainer.add_table(rows,cols,width)` = `tbl = CT_Tbl.new_tbl(rows,cols,width); element_().insert_tbl_(tbl); Table(tbl,obj)` (the C3 element-seam). `Document.add_table(rows,cols,style=[])` = `t = body_().add_table(rows,cols,block_width_()); t.style = style` (**the signature difference — see the add_table idiom row**; H13 `style=[]`≡Python `None`). The three `iter_inner_content` sites (`Document`→delegates `body_()`; `BlockItemContainer`→`1×N` cell of `Paragraph`(CT_P)/`Table`(else) over `element_().inner_content_elements`; `Section`→the `w:tbl`→`Table(element,obj)` branch) are all un-stubbed — **discharges the P5-3a C2 debt** (a `<w:tbl>` in a section now yields a `Table`, never silently dropped). H9 materialized, H10 `isa` CT_P dispatch. `Document().save()` fires ZERO table stubs → M1 17/17 (`document.xml` unchanged); `document.add_table(2,3)`→`document.xml` byte-identical to python-docx (`a1eda043…`, `styles.xml` unchanged; a styled table → `<w:tblStyle>` + `styles.xml` gains the style), full-package L1 17/17 across a 12-scenario matrix. **0 new D#**. |
| `table.py::_Cell` (the table cell — `text` get/set, `add_paragraph`, nested `add_table`, `grid_span`, `paragraphs`, `tables`, `vertical_alignment`, `width`, **`merge`**) | `+mat2doc\+table\Cell_.m` (**FLAG-3** `_Cell`→`Cell_`) | P6-4b | **FINAL Phase-6 WP — closes the table tier.** `class _Cell(BlockItemContainer)`→`< mat2doc.BlockItemContainer` — a block-item container (inherits `add_paragraph`/`paragraphs`/`add_table`/`tables`) plus the cell members. The `_tc`/`_element` dual name (`self._tc = self._element = tc`, `table.py:198`) → the private `tc_` field (self._tc sites) + the inherited `element_()` C3 seam (self._element sites), both the ONE `CT_Tc`; unlike `_BaseHeaderFooter`, `_Cell` stores a **concrete** element so the base `element_()` (returns `element_store_=tc`) is exactly right (NOT the lazy hdr/ftr override). `text` get = `"\n".join(p.text …)` (**H16 no strip** — latent, not triggered), set = `tc.clear_content()`+`add_p`+`add_r`; nested `add_table` defaults width to the cell's width or `Inches(1)` (H3) + appends a trailing `<w:p>`; `vertical_alignment` reads the RAW `tcPr` (H3/H10, a read never creates it); `width`→`tc.width` (H6/H3); `grid_span`→`tc.grid_span`. `_Cell.tables` = `super().tables` **required** un-stubbing `BlockItemContainer.tables` (see the row below). Gate-3: the full cell-authoring + merge matrix `s0073`–`s0082` all part-level 17/17 L1; probe_diff `s0081` MATCH exit 0; **0 new D#**. |
| `document.py::Document.tables`/`paragraphs` (the Document-level content stubs — **NOT** un-stubbed at P6-4b; owner re-assigned) | `+mat2doc\+document\Document.m` (`tables`/`paragraphs`) | **→ P8-3** | Left as clean `mat2doc:notYetPorted` stubs at P6-4b (out of the 8-stub P6-4b scope — P6-4b un-stubs the SHARED `BlockItemContainer.tables`, which `Body_.tables` inherits, but NOT the `Document`-level one-line delegators). Their in-source owner tag read "P6 table tier"; because P6-4b is the FINAL Phase-6 WP, the tag would go stale at merge → **re-owned to P8-3** (the blkcntnr-remainder WP, the `Document.paragraphs`/`add_page_break`→P8-3 precedent). Deps already live; a trivial P8-3 rider. |
| `blkcntnr.py::BlockItemContainer.tables` + `table.py::Table.add_row`/`add_column`/`cell`/`row_cells`/`column_cells`/`_cells` + `_Row.cells` + `_Column.cells` (the cells-grid + grow API — **un-stubbed**) | `+mat2doc\BlockItemContainer.m` (`tables`) + `+mat2doc\+table\Table.m` (`add_row`/`add_column`/`cell`/`row_cells`/`column_cells`/`cells_`) + `Row_.m` (`cells` + private `iter_tc_cells_`) + `Column_.m` (`cells`) | P6-4b | **The P6-4b un-stub set — closes the 8 P6-4a table stubs + the required `BlockItemContainer.tables` dependency.** `BlockItemContainer.tables` (stubbed since P2-3, all deps live once `Table` landed at P6-4a) goes live — the single shared impl `_Cell.tables`/`Body_.tables` route through (`Document.tables` stays its own stub → P8-3). `add_row()`→`Row_` (append `<w:tr>`, one `<w:tc>` per grid column with its width); `add_column(width)`→`Column_` (append `<w:gridCol>` + a `<w:tc>` to every row); `cell(r,c)`→the `_cells[c + r*_column_count]` `Cell_` (H1 `+1`); `row_cells`/`column_cells`/`_cells` walk the grid. `_Row.cells` / `_Column.cells` build `Cell_` handles with **span identity** (see the cells-grid-walk idiom row). Byte-proven end-to-end (`s0074` add_row `3f7009e1…`, `s0075` add_column `52b20778…`); **0 new D#**. |
| `image/image.py` — `Image` (value object), `_ImageHeaderFactory` (`SIGNATURES` dispatch), `BaseImageHeader` | `+mat2doc\+image\` (`Image.m`, `ImageHeaderFactory_.m`, `BaseImageHeader.m`) + `+mat2doc\+opc\sha1_hexdigest.m` | P7-1a | **[R] accelerated re-port — same-source RE-HOME.** python-pptx v1.0.2 has NO image module (it uses PIL); docx `image/` is the **HOME** of these parsers, and Mat2Ppt's `+image` was itself already the docx port — so P7-1a **re-ports from Mat2Ppt's `+image`** (oracle = python-docx). Three transforms: **T1** namespace `mat2ppt.image.*`→`mat2doc.image.*` + Length family → **`mat2doc.shared.Inches`/`Emu`** (docx's home) + `sha1_hexdigest`→`mat2doc.opc`; **T2** inline None-idiom (`isNone(x)`→`isequal(x,[])`, 3 sites); **T3** **WMF-exclusion** (see idiom row). `Image` is a **handle** class (`ext`/`sha1` are `@lazyproperty`); `horz_dpi`/`vert_dpi` are **pure header delegation — NO dpi math in the core**, and `BaseImageHeader` has **no `default_dpi`** member (the "72" default lives in the parsers, P7-1b/P7-2). `content_type`/`default_ext` abstract-`@property` → overridable **methods** (MATLAB can't redefine an inherited property getter). `ImageHeaderFactory_` lands **whole** (8 call-time-resolved `@Class.from_stream` handles; the 6 parser classes are `notYetPorted` stubs owned by P7-1b/P7-2). **Pure-parsing — no registry row, nothing on open/save → M1 17/17, 0 new D#** (`s0083` probe 195/195; sha1 end-to-end `ca11f589…`/`4040957e…`). |
| `image/helpers.py` — `StreamReader` (+ `BIG_ENDIAN`/`LITTLE_ENDIAN`) + `io.BytesIO` (stdlib use) | `+mat2doc\+image\StreamReader.m` + `+mat2doc\+image\BytesIO.m` (Mat2Doc infra) | P7-1a | `StreamReader` — fixed-width **unsigned** reads (`read_byte`/`read_short`/`read_long` = `struct` `B`/`H`/`L`) at `base_offset+base+offset`, configurable byte-order, `read_str` UTF-8 (H2), past-EOF → `mat2doc:UnexpectedEndOfFileError`. `BytesIO` is **Mat2Doc infrastructure** (no python-docx counterpart; substitutes `io.BytesIO`): a 0-based seekable cursor over a `1×N uint8`, `read(n)`/`read()`/`seek`/`tell`, short read returns fewer bytes (never raises — the `StreamReader` layer raises on a truncated field). Re-homed copies (no shared code with Mat2Ppt). |
| `image/constants.py` — `MIME_TYPE`, `JPEG_MARKER_CODE`, `PNG_CHUNK_TYPE`, `TIFF_FLD_TYPE`/`TIFF_FLD`, `TIFF_TAG` | `+mat2doc\+image\` (`MIME_TYPE.m`, `JPEG_MARKER_CODE.m`, `PNG_CHUNK_TYPE.m`, `TIFF_FLD.m`, `TIFF_TAG.m`) | P7-1a | Five `classdef … properties (Constant)` value classes, byte-identical to docx. `MIME_TYPE` = the 5 image types (**`X_WMF` dropped**, T3). `JPEG_MARKER_CODE` = 44 single-byte codes held as `uint8` (a 1-byte read compares directly) + `STANDALONE_MARKERS`/`SOF_MARKER_CODES` `uint8` tuples + `is_standalone`/`marker_name`. `PNG_CHUNK_TYPE` `IHDR`/`pHYs`/`IEND` — **case-sensitive** (H15, `pHYs` keeps mixed case). `TIFF_FLD` mirrors the `TIFF_FLD = TIFF_FLD_TYPE` alias (5 field-type codes). `TIFF_TAG` = 5 IFD tag codes (doubles) + an order-inert `tag_name` lookup (H11). |
| `image/png.py` — `Png` (`BaseImageHeader` subclass) + the module-private `_PngParser`/`_Chunks`/`_ChunkParser`/`_ChunkFactory`/`_Chunk`/`_IHDRChunk`/`_pHYsChunk` | `+mat2doc\+image\` (`Png.m`, `PngParser_.m`, `Chunks_.m`, `ChunkParser_.m`, `ChunkFactory_.m`, `Chunk_.m`, `IHDRChunk_.m`, `pHYsChunk_.m`) | P7-1b | **[R] re-port; un-stubs the P7-1a `Png` placeholder.** `Png.from_stream` walks the PNG chunk stream (big-endian) via `PngParser_`: `px_width`/`px_height` from the **IHDR** chunk, `horz_dpi`/`vert_dpi` from the **pHYs** chunk when present + `units_specifier == 1` (px-per-metre), else **72**. The seven chunk helpers get the underscore rotation (`_ChunkFactory`→`ChunkFactory_` etc., design.md §2). `ChunkFactory_` is a `switch` on `PNG_CHUNK_TYPE` mirroring `chunk_cls_map` (IHDR→`IHDRChunk_`, pHYs→`pHYsChunk_`, else `Chunk_`; H10). pHYs-absent → inline `isequal(pHYs, [])` None-idiom → 72 (H3, no shared `isNone`); IHDR-absent → `mat2doc:InvalidImageStreamError` "no IHDR chunk in PNG image" verbatim (single-string rule). Chunk-type compare **case-sensitive** (H15). Generators realized eagerly into cell arrays (H9). Chunk-walk arithmetic (`chunk_offset += 4+4+len+4`, `data_offset = off+8`) is byte-count DATA, no index shift (H1). **Pure-parsing — no registry row, nothing on open/save → M1 17/17, 0 new D#.** `PngParser_.dpi_` was already the docx `0.0254`/default-72 formula in Mat2Ppt (never PIL-oracled) — namespace change only. |
| `image/gif.py` — `Gif` (`BaseImageHeader` subclass) | `+mat2doc\+image\Gif.m` | P7-1b | **[R] re-port; un-stubs the P7-1a `Gif` placeholder.** Reads `px_width`/`px_height` from the Logical Screen Descriptor — two little-endian unsigned shorts (`"<HH"`) at byte offset 6, read directly off the stream (NOT via `StreamReader`) with explicit 1-based indexing. **dpi unconditionally 72/72** — GIF carries no resolution, so there is no dpi computation (matches python-docx exactly). `content_type` `image/gif`, `default_ext` `"gif"`. |
| `image/bmp.py` — `Bmp` (`BaseImageHeader` subclass) + `Bmp._dpi` | `+mat2doc\+image\Bmp.m` (`Bmp.dpi_` static) | P7-1b | **[R] re-port; un-stubs the P7-1a `Bmp` placeholder — the site of the ★ PIL→docx dpi reversion (see idiom row below).** Parses the BITMAPINFOHEADER (little-endian unsigned longs): `px_width` @ `0x12`, `px_height` @ `0x16`, horz px/m @ `0x26`, vert px/m @ `0x2A`, via `StreamReader`. `dpi_` = the docx formula `int(round(px_per_meter * 0.0254))` with `px_per_meter == 0 → 96` (NOT Mat2Ppt/PIL's 72), half-to-even (`fix(pyRound(..))`, H6/H14). `content_type` `image/bmp`, `default_ext` `"bmp"`. |

**All of `shared.py` is now ported** — the `Length` family + `RGBColor` at P1-1,
the proxy tier (`ElementProxy`/`Parented`/`StoryChild`/`TextAccumulator`/
`lazyproperty`/`write_only_property`) at P2-1. **Phase 2 is complete** at P2-3
(proxy tier P2-1 + object-graph un-stub P2-2 + document shell P2-3); the document
object graph is fully wired and M1 is preserved throughout (17/17 byte-neutral,
0 new D-numbers across all of P2).

## Dunder / idiom

| Python idiom | MATLAB rendering | first WP |
|---|---|---|
| `class Length(int)` (int subclass; arithmetic degrades to a plain `int`) | `classdef Length < double` — defining properties forces every op to return a plain `double`; `isa(x,'double')` true; EMU held exactly (magnitudes ≪ 2^53, design.md §8) | P1-1 |
| `int.__new__(cls, emu)` | `obj@double(fix(emu)+0)` after `pyIntArg(emu,"parse")`; the `+0` normalizes IEEE `-0.0 → +0.0` | P1-1 |
| `int(x)` on a constructor argument | `fix(x)` (truncate toward zero — H6) after domain coercion by `pyIntArg` | P1-1 |
| `self / float(_EMUS_PER_*)` (true division) | `double(obj) / <Const>` (dependent-property getter) | P1-1 |
| `int(round(self / float(635)))` (Python-3 `round()` = half-to-even, returns int) | static `pyRoundHalfToEven_` replicating CPython `float.__round__` (`r=round(x); if abs(x-r)==0.5, r=2*round(x/2)`) + `+0` signed-zero normalize — H6/H14 | P1-1 |
| `@property` `emu` `return self` | `value = obj` (the getter returns the `Length` instance itself) | P1-1 |
| private class attrs `_EMUS_PER_*` | `Constant` properties `EMUS_PER_*_` (leading underscore rotated to trailing, design.md §2) | P1-1 |
| `class RGBColor(Tuple[int,int,int])` (immutable tuple, equality-by-value) | value `classdef` with immutable `r/g/b` properties + by-value `eq`/`ne`; comparison to a non-`RGBColor` is `false` | P1-1 |
| `__str__` / `__repr__` = `"%02X..."` / `"RGBColor(0x%02x...)"` | `str_()` / `repr_()` returning a `string` via `sprintf` (dunders rotate to trailing-underscore methods) — direct `%X`/`%x` hex, **not** routed through `pyStr` (H14) | P1-1 |
| `int(rgb_hex_str[:2], 16)` etc. | `parseHexByte_(s(1:2))` → validated `hex2dec` (base MATLAB, case-insensitive) | P1-1 |
| `raise TypeError(msg)` / `raise ValueError(msg)` | `error("mat2doc:TypeError", "%s", msg)` / `error("mat2doc:ValueError", "%s", msg)` — one identifier per Python exception type (D-004 namespace) | P1-1 |
| `isinstance(val, int)` where a MATLAB `double` cannot distinguish int from float | integer-**valued** real numeric scalar accepted as the Python int (D-STYPE-1); non-integral → `TypeError` | P1-1 |
| ordered `dict` prefix→URI (`nsmap`, `nspfxmap`) | scalar `struct`, insertion-ordered fields (H11); iterate `fieldnames`; never `containers.Map` | P1-2 |
| `dict` keyed by a URI or Clark name (not a valid struct field name) — `pfxmap`, `OxmlElement` `attrs` | **Nx2 `string` array** `[key, value]`, document order (H11) | P1-2 |
| `nsmap[pfx]` / dict `KeyError` | `isfield` guard → `error("mat2doc:KeyError", "%s", …)` | P1-2 |
| `etree.fromstring(xml, oxml_parser)` | `mat2doc.oxml.parse_xml(xml)` driving `XmlParser.parse` — own OOXML-subset reader (D-001), order-preserving | P1-2 |
| `oxml_parser.makeelement(clark, attrib, nsmap)` | `createElement` (+ `.set` per attr) with registry-then-`XmlElement` fallback | P1-2 |
| lxml `_Element` identity / `el1 is el2` (H5) | `handle` identity; `Sealed` `==`/`~=` on the Heterogeneous `XmlElement` root | P1-2 |
| `el.insert(i, c)` / `parent.index(child)` (0-based) | 1-based on the MATLAB surface (H1): `insert(i+1, c)`, `index(child)-1` for data | P1-2 |
| `copy.deepcopy(el)` / `__deepcopy__` | `XmlElement.deepcopy` (Sealed, structural copy of own decls + verbatim tag URI) | P1-2 |
| `etree.tostring(el, encoding="UTF-8", standalone=True)` | `mat2doc.oxml.serialize_part_xml(el)` — own writer, byte-matched to lxml (single `unicode2native` at the boundary, H2/H7) | P1-2 |
| bytes ↔ text at the XML boundary | `native2unicode` / `unicode2native('UTF-8')` only (H2); astral chars via surrogate pairs → 4-byte UTF-8 | P1-2 |
| `MetaOxmlElement` generating per-attribute get/set properties from `OptionalAttribute`/`RequiredAttribute` descriptors | explicit engine methods `getAttrTyped`/`setAttrTyped`/`getAttrRequired`/`setAttrRequired` on `BaseOxmlElement`, each `CT_*` delegates (design.md §2 — no MATLAB metaclass hook) | P1-3a |
| descriptor `simple_type` object / `_clark_name` (`BaseAttribute`) | `resolveTypeCls_` (bare `"ST_String"`→`+oxml\+simpletypes.<name>`; dotted `"mat2doc.enum.*"` verbatim) + `attrClarkName_` (prefixed→`qn`, plain→verbatim) | P1-3a |
| `attr is None` / `del obj.attrib[clark]` in a descriptor setter | `isequal(value,[])` (H3: `""`≠None) + `has_attrib`/`remove_attrib` guard; the three docx setter deltas (D-delta-1/2/3) live in `setAttrTyped`/`setAttrRequired` | P1-3a |
| `raise InvalidXmlError(...)` (required attr absent) / required-setter `ValueError` | `error("mat2doc:InvalidXmlError", ...)` (msg uses element Clark `tag`) / `error("mat2doc:ValueError", ...)` with `valueRepr_` best-effort `str(value)` (D-005 message-token class) | P1-3a |
| lxml `_Element.xpath(expr, namespaces=nsmap)` (docx injects the **public** `nsmap`) | `BaseOxmlElement.xpath(expr[, ns])` → `mat2doc.oxml.evaluate_xpath` (own mini-engine, D-001); default `ns = nsmap()` (public map, no `_nsmap`); typed-empty node-set on no match (H3), never `[]` | P1-3a |
| positional XPath predicate `[1]` (XPath is 1-based) | 1-based verbatim (H1 — never shift): `w:p[1]` selects the first match | P1-3a |
| lxml `text()` node-set (C-level text nodes) | `text_raw_()` self-text + each child's `.tail`, doc-ordered with tail-after-subtree (WPC-F1); bypasses a `CT_*` `getText_` shadow (D10, F1) | P1-3a |
| lxml bare union `A \| B \| …` (node-**set** — merged, doc-ordered, de-duplicated) | union AST node → `pathNodeset` per operand → `cellToElemArray(docSortDedupe(...))`: document order + identity-dedup (H11 union trap; `[p,p,tbl,p,p]` interleaved, never operand-grouped) | P1-3x |
| lxml `not(path)` predicate (keep where the sub-path is an empty node-set) | `notExists` predicate → `isempty(subPathNodeset(...))`; sub-path may itself be a relative union (`[not(self::w:br \| self::w:cr)]`) | P1-3x |
| lxml reverse axes `preceding-sibling::`/`preceding::` (`position()` counts nearest-first) | matches emitted **nearest-first** for the positional predicate, then `docSortDedupe` restores document order for the final node-set (H1 — position by axis direction, not doc order) | P1-3x |
| lxml `position()=n` / `(…)[last()]` / grouped `(…)[1]` | `pos`/`last` predicate kinds; a `(…)` group re-numbers positions over the whole doc-ordered grouped node-set (1-based, H1) | P1-3x |
| lxml predicate attribute sub-path `[w:name/@w:val="…"]` (existential node-set `=`) | `pathEq` predicate → `subPathStringValues` (relative sub-path terminating in `@attr`/`text()`) → `any(vals == literal)` | P1-3x |
| lxml `//@attr[n]` — positional predicate on an attribute-terminal step (one attr per element) | applied **per context element** to that element's own attr node-set (position 2 never exists → empty); result is a **typed-empty `string`** (`string.empty(1,0)`), lxml encode() convention (H3) — the only member removed from the F2 raise-set | P1-3x |
| `MetaOxmlElement` generating child accessors from `ZeroOrOne`/`ZeroOrMore`/`OneAndOnlyOne`/`OneOrMore`/`Choice`/`ZeroOrOneChoice` descriptors | 11 explicit engine methods on `BaseOxmlElement` (`getChild` … `getOrChangeToChild`); each `CT_*` generated member delegates (design.md §2 — no MATLAB metaclass hook) | P1-3b |
| successor slice `_tag_seq[N:]` (child sorts before every schema-later tag) → `obj.insert_element_before(child, *successors)` | `insertChildInSequence(child, successors)` expanding the class `SUCCESSORS` Constant via `num2cell`; **H1 0→1 base shift applied ONCE at the slice declaration** (`_tag_seq[N:]`→`tagSeq(N+1:end)`), never in the engine (H11) | P1-3b |
| `get_or_add_x`: `child = getter; if child is None: child = _add_x()` (returns the live present child, `is`-identity) | `getOrAddChild(tag, successors)`: `getChild` → `addChild` when `isequal(child,[])`; a present child returns the **same handle** back-to-back (H5) | P1-3b |
| `x_lst` `findall` **list** vs single `get.x` `find` **None** vs `OneAndOnlyOne` **raise** | `getChildList` → materialized `(1,N)`/`1x0` typed `XmlElement` (H9, snapshot-safe remove-during-iter); `getChild` → `[]` (None); `getRequiredChild` → `mat2doc:InvalidXmlError` (byte-exact RST-backtick msg) — the **tri-state** absent encoding (H3) | P1-3b |
| `ZeroOrMore` public `add_x()` (docx `_add_public_adder`, xmlchemy.py 536; pptx: `OneOrMore` only) | routes through the same `addChild` primitive as `_add_x` — **D-delta-4**, engine-neutral (**no new D#**); genoxml scaffolder emits the public delegator at each of the 24 docx `ZeroOrMore` sites | P1-3b |
| `etree.tostring(el, encoding="unicode", pretty_print=True)` (libxml2 pretty printer) | `mat2doc.oxml.serialize_for_reading(el)` — own pretty printer: 2-space indent per level, format-off on mixed content propagates through the whole subtree, one trailing LF incl. root tail; **test-only** (off the L1 byte path), verified live vs lxml (no golden freeze) | P1-4 |
| Python `class X:` with class-level string constants (`CONTENT_TYPE`, `RELATIONSHIP_TYPE`, `NAMESPACE`, `RELATIONSHIP_TARGET_MODE`) | `classdef X … properties (Constant)` — values transcribed verbatim, implicit-string-concat entries joined into the single literal | P1-4 |
| `etree.ElementBase` subclass using raw `get`/`set`/`find`/`findall`/`append` (the OPC `CT_*`, NO xmlchemy descriptors) | `classdef CT_* < mat2doc.opc.oxml.BaseOxmlElement < mat2doc.oxml.XmlElement` (the `ElementBase` analogue, **not** the xmlchemy `BaseOxmlElement`); one-line `obj.get(...)` getters, `.new` factory `set`s attrs in insertion=serialized order (H11); missing attr → `[]` (None, H3), never raises | P1-4 |
| `self.set("TargetMode", "External")` **only** `if target_mode == EXTERNAL` (attribute conditionally written) | write `TargetMode` inside `if target_mode == "External"` (string equality); internal rel **omits** the attribute; getter is two-arg `get("TargetMode", RTM.INTERNAL)` → defaults `"Internal"` (H3 tri-state) | P1-4 |
| duplicate-key tuple list `(("bin",…),("bin",…),…)` consumed by `(k, v) in list` membership | Nx2 `string` array; membership is a **row test** `any(pairs(:,1)==k & pairs(:,2)==v)` over BOTH columns (never a dict/`containers.Map` — would collapse duplicate keys) | P1-4 |
| `class CaseInsensitiveDict(dict)` folding keys with `.lower()` on `__setitem__`/`__getitem__`/`__contains__` | `handle` class over a backing `dictionary`; the fold is confined to `set`/`get`/`isKey`; stored key = lowercased form (so `keys()` returns lowercased), values verbatim (H15); no-arg constructor (created empty) | P1-4 |
| `getattr(cls, method_name)` (bound class/static method) | `str2func(cls + "." + method_name)` where `cls` is a fully-qualified class-name **string**; `@classmethod`→Static (design.md §2) so no implicit `cls` arg — a caller needing the class passes it explicitly | P1-4 |
| Python `@property` `CT_Relationships.xml` **overriding** the base `.xml` to return part BYTES | member **rotated** to `xml_file_bytes` (MATLAB cannot override an inherited property); the base pretty `.xml` is inherited unchanged (condition B4) | P1-4 |
| `class PackURI(str)` (immutable str subclass used as dict key / path / format arg) | **value `classdef`** wrapping the URI in a private `string`, with `string()`/`char()` conversions + by-value `eq`/`ne`; every Python "use as str" site maps to `string(obj)` (H2) | P1-5 |
| `re.compile("([a-zA-Z]+)([1-9][0-9]*)?").match(name)` then `int(m.group(2))` or `None` | `regexp(name,'^([a-zA-Z]+)([1-9][0-9]*)?','tokens','once')` (anchored `^` = `re.match`); present group → `str2double`, else `[]` (None); docx `[1-9][0-9]*` (leading 1-9) so `image01`→`[]` (H12); value is data, never index-shifted (H1) | P1-5 |
| `posixpath.split`/`splitext`/`join`/`abspath`/`normpath`/`relpath` on `/`-separated pack URIs | pure-string private statics `pp*_` (`ppSplit_`/`ppSplitext_`/`ppJoin_`/`ppAbspath_`/`ppNormpath_`/`ppRelpath_`), matching posixpath exactly on the absolute inputs docx passes | P1-5 |
| `class X.__new__` returning a **different subtype** instance by dispatch (factory `__new__`) | static `factory(arg)` method (a MATLAB ctor can't return another class) returning the concrete subclass; the concrete classes subclass the factory base (H10) | P1-5 |
| distinct `except IOError:` vs `except KeyError:` clauses guarding the same shape (per-reader failure mode) | each `blob_for` raises its own id (`mat2doc:IOError` dir / `mat2doc:KeyError` zip); the matching `rels_xml_for` catches ITS OWN id → `[]` and rethrows others (H3 error-id fidelity) | P1-5 |
| `zipfile.ZipFile(pkg,"r").read(member)` (lazy random-access read) | preload the whole archive to an ordered `{membername→uint8 blob}` map via `java.io.ByteArrayInputStream`→`java.util.zip.ZipInputStream`, drained by `InterruptibleStreamCopier` (MATLAB can't observe `read(byte[],off,len)` mutation); `blob_for` = lookup (design.md §4, D-001-family realization) | P1-5 |
| `ZipFile(pkg,"w",ZIP_DEFLATED).writestr(name, blob)` (batch, wall-clock stamp) | **stateful** `ZipOutputStream`: ctor opens, `write` = per-entry `ZipEntry`+`setTime(1980)`+`putNextEntry`+`write`+`closeEntry`, `close` = `finish`/flush; `setTime` pinned `1980-01-01` (**D-zip-time**, byte-reproducible, adopt-only) | P1-5 |
| signed Java `byte[]` ↔ `uint8` at the `java.util.zip` boundary | `typecast(uint8,'int8')` / `typecast(int8,'uint8')` (NOT `cast` — bit pattern kept, `128↔-128`), confined to `bytesToJava`/`bytesFromJava` (the sole audited Java boundary, H2); re-ported per-toolbox (design.md §7) | P1-5 |
| `class Relationships(Dict[str,_Relationship])` iterated in dict **insertion** order (docx does NOT sort `.rels` by rId) | insertion-ordered parallel arrays `rIds_`/`rels_`; `xml` emits in that order (**H11 — no rId sort**, the docx-vs-pptx divergence: pptx `_Relationships.xml` sorts numerically); `self[rId]=rel` = replace-in-place keep-position; never `containers.Map` | P1-5 |
| `_next_rId`: first unused `"rId%d"` scanning `range(1, len+2)` (gap-reusing) | `for n=1:len+1: candidate="rId"+n; if ~any(rIds_==candidate) return` — reuses the lowest gap (`{rId3,rId1}`→`rId2`); unreachable fall-through assigned `""` (no defensive `error`, H13) | P1-5 |
| `@property target_ref` **recomputed every access** (plain property, NOT `@lazyproperty`) | plain dependent getter `get.target_ref` recomputing each read (no cache field) — faithful to docx v1.2.0 `_Relationship`; adding a cache would be behavior-adding (the pptx `@lazyproperty` precedent does NOT carry, design.md §7) | P1-5 |
| `_target_parts_by_rId` live `dict` returned by `related_parts` (internal targets only) | `dictionary(string→cell)` (insertion-preserving) built by `add_relationship` for `~is_external` only; `rp(rId){1}` unwraps the Part; a **value snapshot** on read (V-1: consumers must read transiently, not cache) | P1-5 |
| generator `def iter_sparts(self): … yield (partname, content_type, reltype, blob)` consumed by `for partname, content_type, reltype, blob in reader.iter_sparts()` | precomputed `1xN` **struct array** with named fields (H9 — no mutation-during-iteration, materialize eagerly); consumer does `for s = reader.iter_sparts()` then reads `s.partname`/`s.content_type`/… (mirrors tuple-unpack) | P1-6a |
| recursive generator `_walk_phys_parts` sharing a mutable `visited_partnames` list across the recursion | **threaded** in/out arg `[results, visited] = walk_phys_parts_(…, visited)`: the mutated `visited` is returned and re-passed so a part reached by two rel paths is emitted once, globally (the shared-list semantics without a shared handle) | P1-6a |
| `hasattr(self, "_target_partname")` lazy-cache guard (`_SerializedRelationship.target_partname`) | private cache property `target_partname_` + a **logical** `has_target_partname_` flag as the sentinel — **never** `isempty` (an empty value could be a legitimate cache hit); computed once via `PackURI.from_rel_ref`, returned unchanged after (design.md §2 lazyproperty) | P1-6a |
| `if rels_item_xml is not None:` (the `.rels` item may be absent) | `~isempty(rels_item_xml)` — the phys reader returns `[]` (None) when absent and a present `.rels` blob is non-empty `uint8`, so `isempty` is an exact None-test here (H3); absent → an **empty** `SerializedRelationships_`, not `[]` | P1-6a |
| `_ContentTypeMap.__getitem__`: override (exact partname) then default (by ext), both `CaseInsensitiveDict`, else `raise KeyError` | `getitem`: `isa(partname,"…PackURI")` guard → `overrides_.isKey` → `defaults_.isKey(partname.ext)` → `error("mat2doc:KeyError", …)` verbatim; BOTH maps case-insensitive (H15) — Override-beats-Default with CI partname AND ext | P1-6a |
| `(ext.lower(), content_type) in default_content_types` (writer Default/Override classify) vs `_overrides = {}` plain-dict case-**preserved** partname keys | `any(pairs(:,1)==lower(ext) & pairs(:,2)==content_type)` row-test → `<Default>` else `<Override>`; writer `_overrides` = plain `dictionary` keyed by the **case-preserved** `string(partname)` — the deliberate reader(CI)/writer(preserving) asymmetry (H15) | P1-6a |
| `_element`: `for ext in sorted(self._defaults.keys())` then `for partname in sorted(self._overrides.keys())` (sort at emit) | `sort(defaults_.keys())` then `sort(reshape(keys(overrides_),1,[]))`, `add_default`/`add_override` in that order (sort-at-EMIT not at insertion; MATLAB `sort`==Python `sorted` for the all-ASCII partname corpus, H11) | P1-6a |
| `PackageWriter.write` static, then `if len(part.rels):` write the part's `.rels` after the part | `mat2doc.opc.PackageWriter.write(...)` (Static, class never instantiated); `if part.rels.len > 0` (H4) — part-then-its-`.rels` interleave, no-rels parts emit none; `write_parts_` preserves the given `parts` order, no sort/reorder/dedup | P1-6a |
| generator `walk_parts`/`walk_rels` w/ a shared mutable `visited=[]` default (recreated per call because defined inside the method) | **nested function** + closure `visited = mat2doc.opc.Part.empty(1,0)` (fresh per top-level call), appending to the shared `parts`/`rels` array (H9 — no mutation-during-iteration, eager materialize) | P1-6b |
| `for part in self.iter_parts()` returning a **list of proxy objects** (`Part`/`XmlPart` mix) consumed by index / attribute-read | a **heterogeneous `mat2doc.opc.Part` object array** (`Part < handle & matlab.mixin.Heterogeneous`, `XmlPart < Part`), NOT a cell — `iter_parts` builds `mat2doc.opc.Part.empty(1,0)` and `parts(end+1)=part`; the P1-6a writer indexes `parts(k)` and reads named members (VERIFY-1b) | P1-6b |
| `part in visited` (identity membership on a graph node) | `any(visited == part)` — requires `eq`/`ne` **Sealed** in the heterogeneous root (MATLAB refuses unsealed dispatch on a heterogeneous array), forwarding to `eq@handle`/`ne@handle` (H5) | P1-6b |
| `walk_rels` yields EVERY rel but recurses only into not-yet-visited parts (visited gates recursion, not the yield) vs `walk_parts` yields each part once | two traversals over the same `visited` set: `iter_rels` appends every `rel` then skips recursion when `any(visited==part)`; `iter_parts` skips both — the diamond dedup (shared part once, both its rels) | P1-6b |
| Python `@property` overridden in a subclass (`XmlPart.blob` overrides `Part.blob`) | port EVERY `Part` `@property` as a **zero-arg method** (`blob()`/`partname()`/`rels()`…) so `XmlPart` can override `blob` — MATLAB cannot override an inherited property accessor, but can override a method; field-access `part.blob` still invokes the zero-arg method | P1-6b |
| `Part.__init__(self, partname, content_type, blob=None, package=None)` — **docx** arg order, blob before package (pptx was `(…, package, blob)`) | `Part(partname, content_type, blob, package)` with `arguments` defaults `blob=[]`, `package=[]` (H3 None); order preserved in `load`/`XmlPart`/`PartFactory.create`; no `set_blob` (docx `blob` is getter-only) | P1-6b |
| `self._blob or b""` (bytes falsy only when empty) | `if isempty(obj.blob_): b = uint8.empty(1,0); else b = obj.blob_` (H4 — `b""`/None → empty `uint8`) | P1-6b |
| `XmlPart._element` parsed once at `load` (`parse_xml`), `blob` = `serialize_part_xml(self._element)` recomputed each access | element parsed ONCE in `XmlPart.load` (`mat2doc.oxml.parse_xml`, stored in `element_`), `blob()` re-serializes on demand via `mat2doc.opc.oxml.serialize_part_xml` — the whitespace-collapse path (H9 lazy/eager currency; the P1-4 byte-critical writer) | P1-6b |
| `PartFactory.__new__` dispatch (class-as-callable) + `getattr(cls, "part_class_selector")` | static `create(...)` (design.md §2: factories are explicit switch/registry tables); registry `part_type_for_` = ordered Nx2 `[content_type, class-name]` mirroring `docx/__init__.py` 44-51; selector/`load` resolved via `cls_method_fn(cls, name)` (`cls` a fully-qualified class-name STRING) — resolves the P1-5 `cls_method_fn` VERIFY (H10) | P1-6b |
| registry "None → fall through" sentinel (`if PartClass is None`) | `PartClass == ""` (empty string can never be a class name, so `""` is the None sentinel here, H3); `""`→content-type map→default base `Part` | P1-6b |
| `@classmethod` `cls()` / `cls(...).load(...)` constructing the calling subclass | MATLAB statics carry no `cls` → hard-construct the concrete class (`OpcPackage.open`→`OpcPackage()`; `Part.load`/`XmlPart.load`→own class). **P2/P1-8 obligation:** each Part subclass must define its OWN static `load` and `Package` its own `open` (statics ARE inherited — a forgotten `load` silently constructs the base) | P1-6b |
| `template % n` partname formatting (`"/word/header%d.xml" % n`) in `next_partname`, scanning `range(1, len+2)` | `sprintf(char(template), n)` for `n = 1:(numel(partnames)+1)`; the partname NUMBER is DATA (H1, never index-shifted); fall-through → `PackURI.empty(1,0)` (Python implicit None, H3) | P1-6b |
| `str(value)` at an XML serialization site (`revision.text = str(int)`; `value = str(value)` in a text setter) — Python's shortest-round-trip float repr, `str(bool)`→`"True"`/`"False"` | `mat2doc.shared.pyStr(value[, kind])` — the ONLY sanctioned numeric→text conversion (H14, design.md §8); NEVER raw `num2str`/`sprintf('%g')`/`string(double)`. `kind="int"`/`"float"`/`"auto"`; float uses correctly-rounded shortest-repr search (`2.0`→`"2.0"`, `1/3`→`"0.3333333333333333"`, `inf`/`nan`); MATLAB can't tell `2.0` from `2`, so a Python-float call site must pass `kind="float"`. First site: the `<cp:revision>` int + F2 string setters | P1-7 |
| `getattr(self, name)` (descriptor read) / `getattr(self, "get_or_add_"+name)()` (reflected generated adder) | `obj.(name)` dynamic property access / `feval("get_or_add_"+name, obj)` — the faithful analogue of Python `getattr` reflection over the generated descriptor members (design.md §2) | P1-7 |
| `datetime.strptime(part, tmpl)` loop over W3CDTF templates, last successful match wins; `_offset_dt` INVERTED offset sign | four anchored regexes replicating CPython `_strptime` field widths, last-match-wins, whole-part must match, then `datetime(...)` component round-trip rejects rollover + `year>=1` floor (CPython `MINYEAR`); `sign_factor = -1` for `+` (inverted) — `-08:00`→`+8h` | P1-7 |
| lxml xsi-nsdecl reconciliation trick (throwaway `xsi:foo` set/del on the root to hoist `xmlns:xsi`) | translated to its DOCUMENTED intent: `XmlElement.set_nsdecl_("xsi", nsmap().xsi)` declares `xsi` once on the root; the serializer then renders the child as `xsi:type="dcterms:W3CDTF"` (trailing decl order `cp,dc,dcterms,xsi`, D-serializer-nsdecl) | P1-7 |
| `x is None` in the coreprops element (docx oxml convention) | inline `isequal(x, [])` — matching the established Mat2Doc oxml siblings (`XmlElement`/`BaseOxmlElement`/`CT_Relationship`); Mat2Doc `+shared` has no `isNone` helper. `""` is NOT None (H3 tri-state) | P1-7 |
| `docx is None` at a **public entry** that accepts a user path/stream (`api.py::Document`) | **strict** double-`0×0` sentinel `strcmp(class(docx),'double') && isequal(size(docx),[0,0])` — NOT a bare `isequal(docx,[])`, which is TRUE for the `0×0` char `''`; python-docx treats `''` as a real path (H3, `'' is not None`) → `PackageNotFoundError`. The ratified public-entry form (`decision_2026-07-26_mat2doc_none_idiom.md`; internal sites keep the bare inline form) | P1-8 |
| `Package.open(docx).main_document_part` (method chained on a static-call result) | split into two statements `pkg = Package.open(docx); dp = pkg.main_document_part();` — MATLAB cannot chain a method call on a static-call result; behavior identical (Mat2Ppt api precedent) | P1-8 |
| `os.path.split(__file__)[0]` + `templates/default.docx` (`_default_docx_path`, package-relative) | `fullfile(fileparts(mfilename('fullpath')), "templates", "default.docx")` — resolves relative to the `+mat2doc` package dir, **not** pwd, so `mat2doc.Document()` works from any working directory | P1-8 |
| `@classmethod cls()` / inherited-but-`cls`-bound `cls.load(...)` where `cls` is the calling **subclass** (`Package.open`, `DocumentPart.load`) | each subclass declares its **OWN** static `open`/`load` hard-constructing the concrete class — MATLAB statics ARE inherited but carry no `cls`, so a forgotten override silently constructs the **base** (the inherited-static trap: `Package.open` would build a base `OpcPackage`, `DocumentPart.load` a base `XmlPart`, and `.document` would be missing) | P1-8 |
| plain `@property` returning a **fresh** proxy each access (`DocumentPart.document` = `Document(self._element, self)`, NOT `@lazyproperty`) | a plain zero-arg method constructing a new `mat2doc.document.Document` on every call — two reads are **distinct handles** (non-cached), faithful to python-docx (H5); contrast a cached `@lazyproperty` which would return the same handle | P1-8 |
| subclass adds methods only over an inherited `blob`/`load`/`element` (`DocumentPart`/`CorePropertiesPart` over `XmlPart`; later `StoryPart`/`ElementProxy` retrofits) | a **byte-neutral** PartFactory row flip / superclass insertion — the reloaded part's **type** changes but the emitted bytes do not (same inherited `blob`), so the flip and the P2-1/P2-2 base retrofits (VERIFY-M1-DOC-BASE / VERIFY-M1-DOCPART-BASE) emit identical bytes | P1-8 |
| `def __eq__(self, other): if not isinstance(other, ElementProxy): return False; return self._element is other._element` (element-identity, H5) | `function tf = eq(a,b)` guarded by `isa(_, "mat2doc.shared.ElementProxy")` on BOTH operands (reflected order too) → `tf = a.element_ == b.element_` delegating to `XmlElement`'s **Sealed** handle `eq` (≡ lxml `is`); `ne` is the complement (non-proxy → `true`). **Distinct byte-identical elements are NOT equal** (identity, never content) | P2-1 |
| `ElementProxy.__init__(element, parent=None)` + `part` raising `ValueError` when `self._parent is None` | `arguments … parent = []` (None sentinel, H3); `part()` inline typed-empty guard `isa(parent_,"double") && isempty(parent_)` (`''` can never spoof None) → `error("mat2doc:ValueError","%s","part is not accessible from this element")`, else `parent_.part()` | P2-1 |
| class with NO `__eq__` override (`Parented`/`StoryChild`) — Python default object identity | plain `handle` class, NO `eq`/`ne` defined → MATLAB default handle (instance) identity, the exact analogue; structurally distinct `Parented` vs `StoryChild` classes retained (block items derive `StoryChild`) | P2-1 |
| generator method yielding 0-or-1 (`TextAccumulator.pop`, `yield from accum.pop()`) | returns a `(1,:) string` of length 0 or 1 (H9, design.md §2); consumer iterates `for t = acc.pop()` (nothing on `1×0`); buffer `strings(1,0)` = Python `[]` (H4 empty-list falsy, NOT `""`) | P2-1 |
| `@lazyproperty` (cache on first access, read-only) | **idiom**, not a callable: Dependent read-only + private cache + **logical computed-flag** (`fget_isComputed_`), NEVER `isempty`-as-sentinel (`[]` is a legal cached value = None, H3); read-only via defining NO `set.fget`. Central guide `mat2doc.shared.lazyproperty` (non-callable, raises `:notCallable`) | P2-1 |
| `@write_only_property` (set-only property) | Dependent property with `set.x` and **no** `get.x` — reads error, writes flow; central idiom guide `mat2doc.shared.write_only_property` (non-callable) | P2-1 |
| `class X(Exception)` / `class Y(X)` hierarchy (`exceptions.py`) | function raiser `error("mat2doc:<Name>","%s",message)` — one flat identifier per Python class; the `issubclass` chain flattens to INDEPENDENT identifiers (VERIFY-2, inert — zero internal `except` sites) | P2-1 |
| `class P(Protocol)` structural type (`types.py`) | `classdef (Abstract) < handle` with an Abstract `part` method, **documentation-only**: nothing inherits or `isa`-gates on it; satisfied structurally by any object exposing `part()` (VERIFY-3) | P2-1 |
| lxml `_Element.xpath` universal on every element class (docx overrides `BaseOxmlElement.xpath` only to inject the fixed `nsmap`) | **hoist** `xpath` to `mat2doc.oxml.XmlElement` (the `_Element` analogue), `BaseOxmlElement` **inherits** it (single `DefiningClass`) — a plain unregistered parsed root supports `xpath`; a pure relocation of the P1-3 body (only the `arguments` context type widened), **byte-neutral** (task #60) | P2-2 |
| subclass **inserted above** an existing class adding methods only (`DocumentPart < StoryPart < XmlPart`; `StoryPart` supplies `next_id`/`_document_part` over inherited `blob`/`load`/`element`) | a **byte-neutral** superclass insertion — the reparented part emits identical bytes (same inherited `XmlPart.blob`); the reloaded TYPE changes, not one byte (discharges VERIFY-M1-DOCPART-BASE; the 17/17 M1 sweep is unchanged) | P2-2 |
| thin part subclass whose **own static `load`** is what makes a PartFactory row flip land (`StylesPart`/`SettingsPart`/`NumberingPart` shells) | each shell declares its own `static load` constructing the subclass — the inherited-static trap: without it, `PartFactory` builds a base `XmlPart` and the `WML_STYLES`/`SETTINGS`/`NUMBERING` flip is **inert**; with it, the reloaded part's TYPE changes but the bytes do not | P2-2 |
| `used_ids = [int(s) for s in xpath("//@id") if s.isdigit()]; max(used_ids)+1 or 1` (`next_id`, story.py 84-88) | `for` over `xpath("//@id")` filtering `strlength(s)>0 && all(char(s)>='0' & char(s)<='9')` (ASCII `isdigit`, D-002-family — docx ids are ASCII integers), `str2double`, `max(used_ids)+1` else `1`; **H1 — data arithmetic on id VALUES, no index shift**; `if not used_ids`→`isempty` (H4) | P2-2 |
| `del self.rels[rId]` — plain `dict.__delitem__` on a `Dict` subclass that does NOT override it (`Relationships`) | `delitem(rId)` prunes the insertion-ordered `rIds_`/`rels_` only; **KeyError if absent**; **does NOT touch** the parallel `target_parts_by_rId_` map, so a stale `related_parts` entry survives a drop exactly as upstream (H3/H11 faithful quirk) | P2-2 |
| `if self._rel_ref_count(rId) < 2: del self.rels[rId]` (`Part.drop_rel`, part.py 81; ref-count 0 = implicit rel) | `if rel_ref_count_(rId) < 2: rels().delitem(rId)`; threshold **`< 2`** → refcount 2 KEPT / 1 dropped / 0-present dropped / absent `mat2doc:KeyError`; `rel_ref_count_` = `sum(element.xpath("//@r:id") == rId)` (reachable via the #60 hoist) | P2-2 |
| `def new(cls): raise NotImplementedError` **in python-docx itself** (`numbering.py` 11-14) | `error("mat2doc:NotImplementedError", "%s", …)` — **faithful upstream behavior, NOT a port stub**; explanatory text supplied because MATLAB `error(id,'')` does not throw (the identifier is the faithful element); distinct from the `mat2doc:notYetPorted` port stubs | P2-2 |
| plain `@property` (NOT `@lazyproperty`) returning a sibling part via `part_related_by` (`_styles_part`/`_settings_part`) | a plain zero-arg method re-running `part_related_by` each call — returns the **same** loaded handle for a package that HAS the part (H5/H9 identity via the live rels), and only creates+relates a `default()` when the part is ABSENT (materialize-once); **KeyError-only** catch lets a duplicate-rel `ValueError` propagate | P2-2 |

| manual `self.__body is None` cache in a plain `@property` (`Document._body`, NOT `@lazyproperty`) | a private property `body__ = []` (None literal) + `if isequal(obj.body__,[]) … end` populate-once; the sentinel is `[]` (a `Body_` handle is never `[]`, so the cache-empty test is sound, H3) — distinct from the `lazyproperty` logical-computed-flag idiom; faithful to document.py 242-246 | P2-3 |
| `ZeroOrMore("w:p", successors=("w:sectPr",))` — insert a new child BEFORE the schema-later sentinel, which itself has `successors=()` (append last) | `PTBL_SUCCESSORS = "w:sectPr"` Constant fed to `insertChildInSequence` (H11, riding the P1-3a `insert_element_before`): `add_p`/`add_tbl` land before any `w:sectPr`; `sectPr` (`NO_SUCCESSORS = string.empty(1,0)`) appends last | P2-3 |
| `page_width or Inches(8.5)` (a `Length(0)` is falsy in Python) | explicit `if isequal(pw,[]) \|\| pw==0; pw = mat2doc.shared.Inches(8.5); end` (H4 — the `[]`/None and the `Length(0)`-zero cases both fall through to the default) | P2-3 |
| top-level module import `from docx.blkcntnr import BlockItemContainer` (module lives at the package root, not a sub-package) | the class lands at the `+mat2doc` package **root** (`mat2doc.BlockItemContainer`), design.md §1; verified not to collide with the sibling `+mat2doc\Document.m` entry function (distinct names) | P2-3 |
| stub raised at the **item-construction boundary** even though its first primitive is live (`_add_paragraph` = `Paragraph(self._element.add_p(), self)`; `add_p()` live, `Paragraph` not) | the WHOLE method raises `mat2doc:notYetPorted` up front (naming its P4/P6 owner) rather than calling the live primitive first — otherwise a half-applied mutation would leave a stray `<w:p>`/`<w:tbl>` in the tree | P2-3 |

| `class BaseEnum(int, enum.Enum)` — an int subclass whose members carry `_value_` + a docstring (and `BaseXmlEnum` adds `xml_value: str \| None`) | a **plain value `classdef`** (NOT handle — enum members are value objects) owning immutable `value (1,1) int32` / `doc (1,1) string` (+ `xml_value (1,1) string`) props + a pass-through ctor; concrete enums subclass it with an `enumeration` block passing `(ms_api_value,[xml_value,]docstr)` — a MATLAB `enumeration` subclassing a numeric builtin cannot add properties, so the associated data lives on the value-classdef base (design.md §2) | P3-1 |
| `@classmethod from_xml`/`to_xml` bound to the concrete `cls` (per-enum) | shared **Hidden static** helpers `from_xml_(fullClassName,…)`/`to_xml_(fullClassName,…)` on the base (MATLAB statics have no `cls` binding); each concrete enum exposes a thin `from_xml`/`to_xml` forwarding with its own class name **hardcoded** — the explicit no-metaclass, no-runtime-dispatch pattern | P3-1 |
| `int(member)` / `member == ms_api_value` (int-subclass value use) | `double(member.value)` at each site (MATLAB enum members are NOT numeric; no `eq`-with-int override on the base — the Mat2Ppt precedent, VERIFY-P3-1b carried to P4+ consumers) | P3-1 |
| **★ A2 cross-enum API getter** — `cast("WD_TABLE_ALIGNMENT", jc.val)` where `jc.val` is a `WD_ALIGN_PARAGRAPH` member (`CT_TblPr.alignment`; `cast` is a runtime **no-op**, and `WD_TABLE_ALIGNMENT.from_xml("both")` would RAISE so a *converting* getter is provably wrong) | getter returns `obj.jc.val` **verbatim** — a `mat2doc.enum.text.WD_PARAGRAPH_ALIGNMENT` member (exactly Python's runtime object; the legal `<w:jc w:val="both">` reads back as `JUSTIFY`); setter accepts a `WD_TABLE_ALIGNMENT` value and writes it through `CT_Jc.val` (cross-enum `to_xml` resolves by INT VALUE → correct bytes). **Compare a returned alignment by NAME (`== "CENTER"`) or `.value`, NEVER cross-class `==`** — MATLAB enum `==` is class-scoped (`WD_PARAGRAPH_ALIGNMENT.CENTER == WD_TABLE_ALIGNMENT.CENTER` is *silent false* where Python int-subclassing gives true). Byte- and object-faithful; **NOT a D-number** (design.md §2 A2 note; a global int-valued `eq` overload is deliberately NOT adopted — it would fork post-M3 Mat2Ppt). | P6-2 |
| **bare `<w:vMerge/>` continuation cell** — a vertical-merge continuation must serialize with **NO `@w:val`** (`<w:vMerge/>`), the top cell as `<w:vMerge w:val="restart"/>`; the port must NEVER write `w:val="continue"` (`ST_Merge.CONTINUE` is the `OptionalAttribute` **default**, so writing it would corrupt EVERY vertical merge) | `CT_Tc._grow_to` assigns `vMerge = "continue"` to continuation cells → `CT_TcPr.vMerge_val` setter `_add_vMerge().val = "continue"` → the `CT_VMerge` `OptionalAttribute` setter sees `value == default` and **DELETES `@w:val`** (D-delta-1) → a bare `<w:vMerge/>`. `"restart"` (≠ default) writes `@w:val="restart"`; `None` removes the element. **No special-casing** in `CT_Tc`/`CT_TcPr` — the P6-1 `CT_VMerge` default-deletion is the byte mechanism. Byte-proven on the WRITE side (Gate-3 `m02`, `references\s0063`). | P6-3a |
| **recursive read-only `@property` → zero-arg METHOD (V1)** — `CT_Tc.top` (`→_tc_above.top`) and `CT_Tc.bottom` (`→_tc_below.bottom`) are recursive `@property` members; MATLAB forbids ANY textual reference to a `Dependent` property's own name inside its getter, even on a DIFFERENT object of the class (`above.top` inside `get.top` raises *"Dependent properties don't store a value…"*) | port `top`/`bottom` as **zero-arg methods** (`function value = top(obj)` / `bottom(obj)`), not `Dependent` properties. MATLAB's method-call-without-parens keeps every call site identical — `tc.top`, `min(self.top, …)`, the future `_Cell` all read exactly like a property. Read-only upstream, byte-invisible; **NOT a D-number** (a MATLAB-language accommodation, ruled no-D Gate-2, confirmed Gate-3). Every other `@property` member stays `Dependent`. | P6-3a |
| **handle-identity in the merge walkers (H5)** — `tr_lst.index(self._tr)` (`_tr_idx`), `top_tc is not self` (`_grow_to`), `other_tc is self` (`_move_content_to`), `next_tc is …` — all Python `is`/`.index()` identity comparisons | MATLAB **handle identity** `==`/`~=`/`find(arr == h, 1)` (the Sealed `eq`/`ne` on `XmlElement`), **never `isequal` on content**. On a **uniform grid** (every row/cell byte-identical) an `isequal`-based match silently returns row 1 and corrupts the whole walk while passing every non-uniform fixture. Frozen probe: `_tr_idx` on the col-0 cell of each byte-identical row of a uniform 3×3 → `[0 1 2]` (never `[0 0 0]`). | P6-3a |
| `next((m for m in cls if m.xml_value == xml_value), None)` (`from_xml`, **no** None/empty short-circuit — the docx delta) | `for k=1:numel(enumeration(cls)); if xmlEq_(members(k).xml_value, query); break` — a straight scan, NO guard on the query; `from_xml(None)`→the `<missing>` member, `from_xml("")`→the `""` member (both reachable, unlike pptx) | P3-1 |
| `if not xml_value: raise` (`to_xml`, Python falsy = None OR `""`) | `if ismissing(xv) \|\| strlength(xv) == 0` → `mat2doc:ValueError` "`<Cls>.<NAME> has no XML representation`" (H4 falsy expanded to the two-branch test) | P3-1 |
| `xml_value: str \| None` — three-way None / `""` / token (H3 tri-state) | Python None → a `<missing>` string, Python `""` → the real empty string `""`, token → a real string; `asXmlVal_` normalizes on store + query (`[]`/`<missing>`→`<missing>`; `''`/`""`→`""`), `xmlEq_` gives Python `==` (None==None→T, None vs str→F, else `==`) | P3-1 |
| `cls(value)` enum call (member identity / value-lookup / `ValueError`) | `resolveMember_`: `isa(value,cls)` identity → `find(double([members.value])==v,1)` value-scan (first-declared-wins = Python alias resolution) → else `mat2doc:ValueError` "`<v> is not a valid <Cls>`"; the stdlib message keeps CPython `%r` so a **string** value renders single-quoted (Gate-2 **F1**), int/None unquoted | P3-1 |
| `f"{cls.__name__} has no XML mapping for '{xml_value}'"` — str-interpolation, **not** `repr` (the docx form; pptx used `repr`) | `error("mat2doc:ValueError","%s has no XML mapping for '%s'", shortName_(cls), queryStr_(query))` — `queryStr_` renders `<missing>`→`"None"`, else the string (so None→`'None'`, `""`→`''`), `shortName_` = the last dotted token (`cls.__name__`) | P3-1 |
| `DocsPageFormatter` (base.py:80-151 — generates an `.rst` doc page for an enum) | **NOT ported** (docs-tooling, no runtime consumer — grep-confirmed; Mat2Ppt **VERIFY-E4** precedent); the `doc` member is still stored per-member for fidelity but is non-behavioral | P3-1 |
| `class WD_X(BaseXmlEnum)` + `NAME = (v, xml, doc)` member tuples (concrete enum) | `classdef WD_X < mat2doc.enum.base.BaseXmlEnum` + `enumeration NAME (v, xml, doc)` block + own pass-through ctor forwarding to the base + thin `from_xml`/`to_xml` statics **hardcoding the fully-qualified class name** (`mat2doc.enum.<module>.WD_X`) — the no-metaclass forwarding pattern (design.md §2) | P3-3 |
| Python enum **member alias** `TEXT_WRAPPING = 11` duplicating `LINE_CLEAR_ALL = 11` (second same-valued member is an alias, excluded from iteration; `WD_BREAK_TYPE`) | a `properties (Constant)` entry `TEXT_WRAPPING = mat2doc.enum.text.WD_BREAK_TYPE.LINE_CLEAR_ALL` pointing at the canonical member — **NOT** a second `enumeration` member (MATLAB compares `==` by member IDENTITY not `value`, so a duplicate would be a distinct non-`==` object). Yields `name=="LINE_CLEAR_ALL"`, `==LINE_CLEAR_ALL` true, `.value`=11, and a 10-member `enumeration()` — matching Python exactly | P3-3 |
| module-level **class alias** `WD_ALIGN_PARAGRAPH = WD_PARAGRAPH_ALIGNMENT` (7 such: also `WD_BREAK`/`WD_COLOR`/`WD_HEADER_FOOTER`/`WD_ORIENT`/`WD_SECTION`/`MSO_THEME_COLOR`) | a **separate classdef** whose `properties (Constant)` re-export each canonical member (`ALIAS.X = Canonical.X`, the SAME member object → identity `==` true) and whose `methods (Static)` forward `from_xml`/`to_xml` to the canonical class; plain-enum aliases (`WD_BREAK`) carry NO statics (no XML). MATLAB has no class aliasing, so the alias is a thin re-export, not a rename | P3-3 |
| `WD_BREAK_TYPE` = a plain `enum.Enum` (bare `NAME = <int>`, no `xml_value`/docstr/`from_xml`) | a **plain value classdef** (NOT `BaseEnum`/`BaseXmlEnum`) with one immutable `value (1,1) int32` prop + an `enumeration` block of bare ints + a no-arg-tolerant ctor — the Mat2Ppt PROG_ID plain-enum precedent; consumer `Run.add_break` (P4-4b) uses member IDENTITY, so identity (not the int) is the load-bearing property | P3-3 |
| member `xml_value = None` (`WD_COLOR_INDEX.INHERITED`, `WD_UNDERLINE.INHERITED`) reachable via `from_xml(None)` on the docx no-None-guard scan | 3rd `enumeration` arg `string(missing)` → P3-1 `asXmlVal_` normalizes to `<missing>` (H3); `from_xml([])` (`[]`=None sentinel) matches it (`None==None`); `to_xml(INHERITED)` → `mat2doc:ValueError` `"<Cls>.INHERITED has no XML representation"` (the `if not xml_value` guard, Delta 2) | P3-3 |
| member `xml_value = "UNMAPPED"` (truthy placeholder — `WD_LINE_SPACING.{SINGLE,ONE_POINT_FIVE,DOUBLE}`, `MSO_THEME_COLOR_INDEX.NOT_THEME_COLOR`) | verbatim real string (NOT `None`, NOT `""`): `to_xml`→`"UNMAPPED"` (guard passes), `from_xml("UNMAPPED")`→ the **first**-declared member carrying it (declaration order behavioural, first-declared-wins) | P3-3 |
| `class WD_X(BaseEnum)` + `NAME = (v, doc)` member tuples — an MS-API int-value enum with **NO XML** mapping (`WD_BUILTIN_STYLE` 132, `WD_TABLE_DIRECTION` 2) | `classdef WD_X < mat2doc.enum.base.BaseEnum` + `enumeration NAME (int32 v, doc)` block + a pass-through ctor forwarding `(ms_api_value, docstr)` to the base; **NO `from_xml`/`to_xml` statics** (BaseEnum has none); `int(member)`→`double(member.value)`, `str(member)`→`str_()`=`"NAME (value)"` (negatives render `"NORMAL (-1)"`); a `BaseEnum` module alias re-exports the members as `Constant`s and forwards **no** statics | P3-4 |
| Python enum whose members carry **all-distinct** values (no duplicate → no `enum` alias) — `WD_BUILTIN_STYLE` (132), `WD_INLINE_SHAPE_TYPE` (5) | declare **every** member as an ordinary `enumeration` member — **NO internal `Constant` alias** (contrast `WD_BREAK_TYPE`, whose duplicate value 11 forced a `TEXT_WRAPPING` Constant). Verified against the oracle: iteration count == member count == distinct-value count, so no member collapses into an alias; a spurious Constant would be a divergence | P3-4 |
| `WD_INLINE_SHAPE_TYPE` = a plain `enum.Enum` (`shape.py`, the C2 fold-in) with a **negative** member (`NOT_IMPLEMENTED = -6`) | a **plain value classdef** (NOT `Base(Xml)Enum`) with one immutable `value (1,1) int32` prop (signed → −6 stored fine) + an `enumeration` block of bare ints + a no-arg-tolerant ctor — the `WD_BREAK_TYPE` PROG_ID plain-enum precedent; consumer is the **P7** inline-shape tier; the module alias `WD_INLINE_SHAPE` re-exports the 5 members, no statics (plain enum has no XML) | P3-4 |
| `@classmethod from_xml`/`to_xml` calling `cls.convert_from_xml`/`cls.validate` (late-binding to the most-derived class, simpletypes.py 25-33) | `from_xml`/`to_xml` templates hosted on the branch bases `BaseIntType`/`BaseStringType`, **re-declared** on every subclass that overrides a `convert_*`/`validate` (H10 — MATLAB statics have no late binding); the `BaseSimpleType.convert_from_xml` `int()` default is never inherited-and-used (dead-abstract-base path — `BaseSimpleType.from_xml` is not hosted on the root, so it raises) | P3-2 |
| `int(str_value)` (`BaseIntType.convert_from_xml`; bare-int + pre-`int()`ed measure types) | `intFromXml(str)` = `double(mat2doc.shared.Emu(str))` — reuses the audited P1-1 `pyIntArg` ASCII `[0-9]` int-grammar (D-002), so `int("2.5")` raises the verbatim `invalid literal for int() with base 10:` message; package-private helper, no python-docx counterpart (**D-STYPE-1** re int/float indistinguishability at `validate_int`) | P3-2 |
| `float(str_value)` (the ONLY float parse in the tier: `ST_UniversalMeasure` :415, `ST_SignedTwipsMeasure` :366) | `str2double(str)` — **D-STYPE-2 re-home** (docx has NO `BaseFloatType`; no such class created). Well-formed measure literals match CPython `float()` exactly; the malformed-input lexical divergence (underscore under-accept, thousands-comma over-accept) is folded under D-STYPE-2/D-002/D-004 | P3-2 |
| `int(round(x))` (round-half-to-even, `ST_UniversalMeasure`/`ST_SignedTwipsMeasure`) | `pyRound(x)` — CPython 3 banker's rounding; MATLAB `round` is half-away-from-zero and a defect at the `.5` tie (H6). Result already integral; callers wrap in `Emu`/`Twips` | P3-2 |
| `"%02X%02X%02X" % rgbcolor` (`ST_HexColor.convert_to_xml`) vs the decimal serialize sites | `sprintf("%02X%02X%02X", r, g, b)` — direct hex, **NOT** routed through `pyStr` (not a decimal serialize, H14 n/a); every OTHER serialize uses `mat2doc.shared.pyStr(value, "int")`; hex parse via `RGBColor.from_string` `[0-9A-Fa-f]` (D-STYPE-4, no length guard — the docx `ST_HexColor`/`ST_HexColorAuto` split) | P3-2 |
| dict multiplier lookup `{mm:36000, …}[unit]` (`ST_UniversalMeasure`) | `switch units_part` (fixed set, order-irrelevant — no H11, no `containers.Map`); unknown unit → `error("mat2doc:KeyError","'%s'",unit)` (Python dict-subscript `KeyError`) | P3-2 |
| `raise InvalidXmlError(...)` from `docx.exceptions` (XsdBoolean :116, ST_OnOff :340 — the only two sites) vs `raise ValueError`/`TypeError`/`KeyError` | `mat2doc.exc.InvalidXmlError(msg)` (canonical raiser → `mat2doc:InvalidXmlError`) at exactly those two; `mat2doc:ValueError` (membership/range/hex/parse), `mat2doc:TypeError` (validate_int/_string, XsdBoolean.validate, ST_DateTime.validate), `mat2doc:KeyError` (unknown unit) — raise-CLASS faithful line-for-line | P3-2 |
| `str_value[:-2]` / `str_value[-2:]` (`ST_UniversalMeasure` unit split) | `sv(1:end-2)` / `string(sv(end-1:end))` on `char(sv)` — H1 no ±1 arithmetic; the datetime slices use anchored regex (FLAT optional groups so a non-participating group does not collapse and shift later token indices) | P3-2 |
| `dt.datetime` / `datetime.fromisoformat` (`ST_DateTime`) | MATLAB `datetime` (TimeZone `''`=naive, offset/`Z`→`'UTC'` instant, VERIFY-tz compares instants); a **narrow** `fromisoformat` subset — exotic schema-invalid inputs → 1970 epoch (under-accept, unreachable until P8-2; `decision_2026-07-26_st_datetime_underaccept.md`). Serialize normalizes to UTC → bytes identical | P3-2 |
| range test carrying a `>2^53` bound as a double (`XsdLong` ±2^63, `XsdUnsignedLong` 0..2^64−1) | the bound renders `…808`/`…616` vs the exact `…807`/`…615` at the edge (**D-STYPE-3**); dead upper edge — every concrete subclass narrows well below 2^53 | P3-2 |
| `ZeroOrOne("w:x", successors=_tag_seq[N:])` on a real `CT_*` with a **non-contiguous** `_tag_seq` (tags between own and next descriptor have no descriptor — `CT_RPr` `sz=[24:]`/`highlight=[26:]`/`u=[27:]`/`vertAlign=[32:]`/`specVanish=[38:]`) | Constant `TAG_SEQ` (39 tags verbatim) + per-descriptor slice `TAG_SEQ(N+1:end)` fed to `insertChildInSequence` — the intervening non-descriptor tags **stay in the successor list, never collapsed**, so a scrambled/parsed insert re-sorts to schema order byte-exact (H11 — the M2 crux); H1 base shift applied ONCE at the slice, never in the engine | P4-1a |
| metaclass **`_new_x` creator override** on a `CT_*` (`CT_RPr._new_color`, `font.py:149-151`) winning over the generated default creator | own method `new_x_` (rotation) building the seeded fragment via `parse_xml` (`<w:color …w:val="000000"/>`), routed by `get_or_add_color`/`add_color_`; the other 26 descriptors use the generic `newChild` — design.md §2 OVERRIDE alert (Mat2Ppt `CT_Chart._new_title` precedent) | P4-1a |
| `OptionalAttribute("w:val", ST_OnOff, default=True)` — an optional attribute with a **non-None default** (`CT_OnOff`) | `getAttrTyped`/`setAttrTyped` with `VAL_DEFAULT = true`: absent-get→`True`; set `True`(==default) or `[]`(None)→**remove** `@w:val` (D-delta-1); set `False`→`ST_OnOff.to_xml("0")`→`w:val="0"` — the tri-state a bare `<w:b/>` = bold-on relies on | P4-1a |
| `RequiredAttribute("w:val", T)` typed child-attr + a `@staticmethod new(nsptagname, val)` factory (`CT_String`); `cast(CT_String, …)` type hint | `getAttrRequired`/`setAttrRequired(VAL_ATTR, VAL_TYPE)` (absent→`mat2doc:InvalidXmlError`) + `static function elm = new(nsptagname, val)` = `OxmlElement(nsptagname)` then `elm.val = val`; the `cast(...)` hint dropped | P4-1a |
| **asymmetric** `@property` setter — `CT_RPr.rFonts_hAnsi=None` sets `hAnsi=None` (removes only `@w:hAnsi`, KEEPS `w:rFonts`) unlike `rFonts_ascii=None` (removes `w:rFonts`), `font.py:201-206` | ported verbatim: `rFonts_hAnsi` setter guard `isequal(value,[]) && isequal(obj.rFonts,[])` is a no-op, else `get_or_add_rFonts().hAnsi = value` (removes only the attr); `rFonts_ascii` setter removes the whole child on `[]` — the two do NOT share a code path (faithful) | P4-1a |
| `if len(text.strip()) < len(text):` — a boundary-whitespace test whose result drives serialized OUTPUT (`CT_R.add_t` → `@xml:space="preserve"`, `run.py:43`) | **NOT** `strlength(strip(text)) < strlength(text)` — MATLAB `strip()` strips a NARROWER set than CPython `str.strip()` (misses U+0085/U+00A0/U+2007/U+202F/U+2028 …). Replicate CPython's exact **29-code-point** set as a `Constant PY_STRIP_WS` and test first/last-code-unit membership (equivalent to the length-decrease). **H16** — binding rule for any output-affecting `.strip()` | P4-1b |
| `ZeroOrOne` whose ordering is an **`_insert_x` inserter override** (not a successor list) — `CT_R._insert_rPr` `self.insert(0, rPr)` (`run.py:145-147`) forcing the child to the FRONT | own `insert_x_` override `obj.insert(1, child)` (H1 0→1 shift) winning over the generated inserter; the descriptor keeps `successors=()` (so the generic inserter would append) — the front placement is the explicit override, mirroring the `_new_x` creator-override shape (H11) | P4-1b |
| a `__str__` "text equivalent" dunder on run-content elements (`CT_Text`/`CT_Br`/`CT_Cr`/`CT_NoBreakHyphen`/`CT_PTab`) + `"".join(str(e) for e in xpath(...))` (`CT_R.text` getter) | `str_()` method (dunder→trailing-underscore) returning a `string`; the join seeds `value=""` then loops `value = value + elms(k).str_()`. Python newline/tab literals `"\n"`/`"\t"` → `string(newline)`/`string(char(9))` (H2 — a MATLAB `"\n"` literal is backslash-n) | P4-1b |
| a text→element **FSM over `for char in text`** buffering runs of regular chars, flushing on `\t`/`\r`/`\n` (`_RunContentAppender`, `run.py:281-307`) | `handle` class iterating `char(text)` (UTF-16 units): `ch==char(9)`→`w:tab`, `ch==char(13)\|\|ch==char(10)`→`w:br`, else `bfr_(end+1)=ch`; flush `if strlength(text)>0` (H4) via `string(bfr_)` (rejoins surrogate pairs, H2 surrogate-safe); `""`→`bfr_=''` | P4-1b |
| a `CT_*` that **shadows** the lxml `.text` attribute (`CT_R.text`, `run.py:129-143`) — a computed run-inner-content string, not element char data | OVERRIDE the protected `getText_`/`setText_` (MATLAB forbids redeclaring the inherited `text` property); the serializer + `text()` xpath read `text_raw_` (bypass), so the element's own char data (normally None) serializes unchanged — **D10**, M1 byte-verified | P4-1b |
| parser **registered-class** construction must forward the resolved namespace URI (`XmlParser.parseElement_`) exactly as the generic fallback does | `elm = feval(cls, name, ownDecls, uri)` (the `CT_*` ctors are transparent pass-throughs → `XmlElement(name, ownDecls, resolvedUri)` early-return); WITHOUT the forward, an element whose prefix is ancestor-declared and not in the fixed nsmap throws `mat2doc:KeyError` in the ctor before any post-construction setter runs (the P4-1b engine fix) | P4-1b |
| `OptionalAttribute("w:leader", WD_TAB_LEADER, default=WD_TAB_LEADER.SPACES)` — an optional attribute whose **default is a non-None enum MEMBER** (not a scalar like `True`) — `CT_TabStop.leader` | `getAttrTyped`/`setAttrTyped` with `LEADER_DEFAULT = mat2doc.enum.text.WD_TAB_LEADER.SPACES` (the actual member, not `[]`): absent-get→`SPACES`; set `[]`(None) or the `SPACES` member→**remove** `@w:leader` (D-delta-1/-2, `isequal(value,default)` matches on the member); set `DOTS`→`to_xml`→`w:leader="dot"` — the same tri-state shape as `CT_OnOff`'s `default=True`, but exercised with an enum member | P4-2 |
| an **overloaded element tag** — the same `<w:tab>` serves both a tab-STOP (in `<w:tabs>`, three typed attrs) and a run tab-CHARACTER (only needs `str(w:tab)`→`"\t"`) — `CT_TabStop` (`parfmt.py:352-375`) | a single `CT_TabStop` class carrying both the tab-stop attrs (`val`/`pos`/`leader`) AND `str_()`→`string(char(9))` (H2, never the literal `"\t"`); registering `w:tab`→`CT_TabStop` with this `str_` **closes the P4-1b carry-forward gap** — `CT_R.getText_`/`CT_P.getText_` join `str_()` over `w:br\|w:cr\|w:noBreakHyphen\|w:ptab\|w:t\|w:tab`, so a run holding `<w:tab>` (previously a generic `XmlElement` with no `str_`, which errored) now reads back the tab char | P4-2 |
| a **read-only** `@property` that **shadows** the lxml `.text` (`CT_P.text`, `paragraph.py:95-102` — getter only, assignment raises `AttributeError`) | OVERRIDE the protected `getText_` for the computed inner-content read (serializer reads `text_raw_`, bypass — D10, M1 byte-verified) AND override `setText_` to **RAISE** `mat2doc:AttributeError` (mirroring the getter-only property); contrast `CT_R.text` (P4-1b), whose setter is LIVE (drives `_RunContentAppender`). No internal path assigns `.text` on a `w:p` (parser uses `setTextRaw_`; deepcopy copies raw `text_` fields) | P4-2 |
| `insert_x_in_order(...)` — build a child, set its keys, then insert it before the **first existing child whose sort-key is greater**, else append (`CT_TabStops.insert_tab_in_order`, `parfmt.py:383-392`) | create via `new_tab_`, set `pos`/`val`/`leader` in tuple-unpack order, loop `tab_lst` **1-based** (H1) and `addprevious` on the first tab with greater `pos` (`insert_element_before`-style) else append; returns the new tab in both branches — keeps `<w:tabs>` sorted by position | P4-2 |
| a **proxy sequence** exposing the Python collection dunders `__getitem__`/`__delitem__`/`__len__`/`__iter__` (`TabStops`, `tabstops.py:20-51`) — the FIRST such sequence in Mat2Doc | **interim explicit methods** `getitem_`/`delitem_`/`len_`/`to_array` (the shared `RedefinesParen` collection base that would give native `x(i+1)`/`numel`/`for s=x.to_array()` is a **future WP** — per the Mat2Ppt `_GradientStops`→`GradientStops_` precedent, VERIFY-COLLECTION accepted at Gate 2). `getitem_`/`delitem_` keep the **Python 0-based** argument (H1 `+1` at the child list; negative index counts from the end; out-of-range→`mat2doc:IndexError`, verbatim `"list index out of range"`/`"tab index out of range"`/`"TabStops object is empty"`). `to_array` (`__iter__`) mints a FRESH proxy per child (H9 materialized). **Hand-off:** when the base lands, `TabStops` derives from it and exposes `tab_stops(1)`≡`getitem_(0)`; do NOT retrofit now | P4-5a |
| a `@property`/setter whose **write re-binds a DIFFERENT private handle than the identity handle** — `TabStop.position` reassigns only `self._tab` (the re-inserted, re-sorted `<w:tab>`), leaving `self._element` on the OLD detached element (`tabstops.py:118-123`) | ported **verbatim** (design.md §7 — a faithful quirk, not a bug to "fix"): the setter re-inserts + removes, then sets `obj.tab_` (the property basis) but NOT `obj.element_` (the H5 `eq()` basis), so `t==t2` stays True over the old element while `t.position` reads the new value; the two handles diverge exactly as in python-docx | P4-5a |
| a `@property` returning a **plain Python `list` comprehension** of a single proxy type (NOT a collection class) — `Paragraph.runs`/`hyperlinks`/`rendered_page_breaks`, `Hyperlink.runs` | a **homogeneous `1×N` object ARRAY** seeded `Class.empty(1,0)` (`mat2doc.text.Run.empty(1,0)` …), filled in a `for` loop; `numel(x)==len(x)`, native 1-based `x(i)` realizes the design.md §2 `x[i]→x(i+1)` with **no wrapper**, empty→typed `1×0`. Distinct from a genuine collection CLASS (`TabStops`, with `__getitem__`/`__len__`) which gets the explicit `getitem_`/`len_` interim surface — a plain-list property warrants no such machinery. A **heterogeneous** list (`iter_inner_content`'s mixed `Run\|Hyperlink`, which share no `matlab.mixin.Heterogeneous` base) becomes a **`1×N` cell** in document order instead. Both ACCEPTED at Gate 2 as the standing surface | P4-5b |
| a proxy class whose Python base **does NOT override `__eq__`** (`Parented`/`StoryChild` — only `ElementProxy` does) → Python default OBJECT identity, so two proxies over the SAME element are NOT equal | a `handle` subclass that defines **no `eq`/`ne`** → MATLAB **default handle (instance) identity**, the exact analogue: `Hyperlink(h,d)==Hyperlink(h,d)` is `false` — the OPPOSITE of an `ElementProxy` subclass (`ParagraphFormat`), where same-element proxies ARE equal (element identity, H5). The two regimes coexist correctly; `clear()` returning `self` is the one identity-bearing method (`p.clear()==p` true). Faithful to `Paragraph<StoryChild` / `Hyperlink,RenderedPageBreak<Parented` holding no `element_` (they declare their own private handles) | P4-5b |
| a Python element method named **`delete()`** that self-removes (`self.getparent().remove(self)` — `CT_Style.delete`/`CT_LsdException.delete`, `styles.py:168-170`/`:78-80`, the FIRST such method in the project) — **H17** name-collision (in MATLAB `delete` on a `handle` subclass IS the destructor) | a **guarded destructor override** — `p = obj.getparent(); if ~isequal(p,[]) && isvalid(p); try; p.remove(obj); catch; end; end` — detach only when currently parented, wrapped parent-guard + `try/catch`. The implicit-destructor detach does NOT fire (the parent's children array holds a **strong** handle ref → a wrapper going out of scope / `clear`ed never destroys a live child; auditor-probed, 25 unreachable-tree teardowns → 0 warnings). Both real callers (`styles/style.py:56`, `styles/latent.py:127`) do `element_.delete(); element_ = [];` — discard immediately, so "usable after delete" is never relied on. Benign UNREACHABLE divergence: explicit `delete()` on an **unparented** element = MATLAB no-op vs Python `AttributeError` — no byte/output impact, **no D#**. **Gate-4 rule:** never assert on an element after `delete()` (the MATLAB handle is invalid; Python's survives detached) — assert the parent-side effect only. SIGNED-PROVISIONAL (design.md §9 H17; `decision_2026-07-30_h17_delete_destructor.md`) | P4-6 |
| `setattr(self, attr_name, bool(value))` where the OptionalAttribute default is **None** — Python `bool(None)` is `False`, and `False ≠ default` so the attr is **WRITTEN** as `"0"` (`CT_LatentStyles.set_bool_prop`, `styles.py:62-64`) — the **F-1** None-vs-False truthiness trap | guard the None case to the logical `false`, NOT an empty logical: `if isequal(value,[]); obj.(attr_name) = false; else; obj.(attr_name) = logical(value); end`. A naive `obj.(attr_name) = logical(value)` maps `[]`(None)→a 0×0 empty logical, which the OnOff `setAttrTyped` path reads as None and **REMOVES** the attribute — wrong (Python writes `w:def..="0"`). Distinct from `~isequal(value,[]) && value` (the H4 `bool(value)` idiom on `CT_Style`'s setters, where None SHOULD short-circuit to remove): here `bool(None)` is a real `False` VALUE that must serialize. Gate-2 oracle-caught; Gate-3 truth table (True/False/1/0/`[]`) all serhex == oracle | P4-6 |
| a Python **proxy-layer** method named **`delete()`** that self-removes then drops its element (`self._element.delete(); self._element = None` — `BaseStyle.delete`, `styles/style.py:49-57`) — the **H17** collision at the PROXY layer, where the P4-6 element-layer guarded-destructor ruling does NOT transfer | a **FLAG-3 method rename `delete()`→`delete_()`**; `delete` is **NOT overridden** on any style proxy (the default `handle` destructor is left in place → GC-safe). A `BaseStyle` is minted transiently by EVERY `StyleFactory` call and is NOT held by the tree, so a faithful `delete` override would detach a still-parented `w:style` on ordinary iteration/GC (empirically proven, parent `1 kid → 0`); MATLAB gives no signal to tell an explicit call from a destructor. `delete_()` carries the faithful body (`element_.delete(); element_ = [];` via the P4-6 element-layer guarded `CT_Style.delete`), is called ONLY explicitly, and is **byte-identical** to python-docx `style.delete()` (Gate-3 re-derived SHA `cc0bb35d…8614`). A method-NAMING resolution (same FLAG-3 class as the private-class renames), **NOT an output deviation → no D#**. **Gate-4 rule:** never inspect the handle after `delete_` (destroyed) — assert only the parent-side effect. SIGNED-PROVISIONAL (design.md §9 H17 addendum; `decision_2026-07-30_h17_delete_destructor.md`) | P4-7a |
| a Python **private class** (leading underscore) — `_TableStyle` / `_NumberingStyle` (`styles/style.py:239`/`:250`) | the toolbox-wide **FLAG-3 leading-underscore rotation** `_X`→`X_`: `_TableStyle`→`TableStyle_`, `_NumberingStyle`→`NumberingStyle_` (same convention as `_Cell`→`Cell_`, `_Relationship`→`Relationship_`, `_GradientStops`→`GradientStops_`). The `StyleFactory` dispatch and the `probe_diff` `cls()` normalizer strip both the Python leading `_` and the MATLAB trailing `_` to one canonical token, so the two spellings compare equal at Gate-3. The non-public back-compat aliases `_CharacterStyle = CharacterStyle` / `_ParagraphStyle = ParagraphStyle` (`:193`/`:236`) are behaviorally inert and have no MATLAB equivalent (a class-alias file would collide) | P4-7a |
| the Python private class `_LatentStyle` (`styles/latent.py:110`) with a proxy-layer **`delete()`** (`self._element.delete(); self._element = None`, `:119-128`) | the **same** FLAG-3 rotation `_LatentStyle`→`LatentStyle_` AND the **same H17 proxy-layer resolution as `BaseStyle`** (row above): `delete` is **NOT overridden** (a `LatentStyle_` is minted transiently by `getitem_`/`to_array`/`add_latent_style` and NOT held by the tree → the MATLAB destructor would detach a still-parented `w:lsdException` on GC), and the faithful body is exposed as **`delete_()`** (via the P4-6 element-layer guarded `CT_LsdException.delete`), byte-identical to python-docx `_LatentStyle.delete()`. A method-NAMING resolution, **NOT an output deviation → no D#**. **Gate-4 rule:** never inspect the handle after `delete_` — assert only the parent-side effect (`len_` down by one). Also note the **H3 tri-state split** vs `BaseStyle`: `LatentStyle_.on_off_prop` returns the RAW `{[]/true/false}` (`[]`=inherit the parent default), where `BaseStyle`'s `*_val` collapse absent→`false`. SIGNED-PROVISIONAL (`decision_2026-07-30_h17_delete_destructor.md`, which names P4-7b explicitly) | P4-7b |
| an **orientation setter with NO width/height swap** — Python `pgSz.orient = value if value else WD_ORIENTATION.PORTRAIT` (`CT_SectPr.orientation`, `section.py:299-301`); `BaseXmlEnum` subclasses `int` and `PORTRAIT`'s int is **0**, so `if value` is falsy for BOTH `None` AND `PORTRAIT`, and the setter appends `@w:orient` on the UNCHANGED `@w:w`/`@w:h` | expand the H4 truthiness faithfully: `isFalsy = isequal(value,[]) || double(value.value) == 0` → PORTRAIT else value (observably identical to a None-only test for the 2-member enum, but honors H4 literally). **Critically NO dimension arithmetic** — `page_width`/`page_height` are straight pass-throughs (`section.py:188-216` has none), so LANDSCAPE keeps the portrait `w:w="12240" w:h="15840"` and merely adds `@w:orient="landscape"`. A port that swapped would diverge; the LANDSCAPE G-scenario (`s0041`, 1569 B `d165a628…`) byte-pins that it does not | P5-2a |
| a setter whose Python guard is **`if value in [None, False]`** (`==`-**membership** — `CT_SectPr.titlePg_val`, `section.py:404-408`), so `None`, `False`, AND any `x == False` (numeric `0`/`0.0`) all take the remove branch — CONTRAST `CT_Settings.evenAndOddHeaders_val`'s `is None or is False` (**identity**, P5-1) | reproduce the full breadth with `isequal(value,[]) || isequal(value,false)` — because `isequal(0,false)` is **true**, the numeric-0 case is covered (where the P5-1 identity form would NOT remove on a `0`). The else-branch sets `get_or_add_titlePg().val = true`; `CT_OnOff.val==default(True)` drops `@w:val` → empty `<w:titlePg/>` (**D-delta-1**). The two section/settings tri-states are genuinely different in the python-docx source and are ported distinctly, not normalized | P5-2a |
| a faithful **wrap-vs-direct setter ASYMMETRY** across sibling `@property` setters of one class — `CT_SectPr` wraps `int\|Length\|None` via `Length(value)` for `bottom_margin`/`footer`/`gutter`/`header`/`left_margin` (`section.py:182/210/242/259/282`) but assigns `Length\|None` DIRECTLY for `top_margin`/`right_margin`/`page_height`/`page_width` (`:315/331/373/421`) | transcribe **verbatim, NOT normalized** (design.md §7): a private `asLengthOrNone_` static realizes the wrap RHS (`isequal(value,[]) \|\| isa(value,'mat2doc.shared.Length')` pass-through, else `mat2doc.shared.Length(value)` — `isa` covers `Twips`/`Emu`/… like `isinstance(value, Length)`); the direct setters assign the value straight. Both paths byte/EMU-verified (raw-int wrap + `Twips(-720)` signed direct) | P5-2a |
| a Python member already carrying a **trailing underscore** to dodge a builtin — `CT_HdrFtrRef.type_` (`RequiredAttribute("w:type", …)`, `section.py:54`) named to avoid the `type` builtin | kept **verbatim `type_`** — the leading-underscore FLAG-3 rotation (`_X`→`X_`) applies to LEADING underscores only; a trailing-underscore Python name is copied as-is (no double-rotation, no strip) | P5-2a |
| a subclass that **redefines an inherited attribute as a lazy `@property`** — `_BaseHeaderFooter` never stores `self._element`; it overrides `_element` as a `@property` = `self._get_or_add_definition().element` (`section.py:351-354`), so the header/footer part is created on FIRST content access. **MATLAB cannot redefine a stored property in a subclass** | the **C3 element-accessor seam** (property→protected-method for lazy-override): the base `BlockItemContainer` exposes `element_` as a **zero-arg PROTECTED METHOD** (the house property-as-method convention, cf. `StoryChild.part`/`Section.part`) backed by a renamed concrete store `element_store_` — the base seam returns the stored handle, every internal element read routes through `obj.element_()`, and `BaseHeaderFooter_` OVERRIDES `element_()` to `get_or_add_definition_().element()` (lazy part-creation). Because a zero-arg method is read with the **same** `obj.element_` dot-syntax a property would use, the refactor is **BYTE-NEUTRAL for every existing subclass** (`Body_` passes a concrete element; `Document` extends `ElementProxy`, not `BlockItemContainer`) — re-proven M1 17/17 (`0e4dd503`) + M2 s0033 17/17 (`a71e5502`). The ctor chains `obj@mat2doc.BlockItemContainer([], [])` (both stores dead — `element_()` and `part()` both overridden). A live base-class touch → mandatory full-M1/M2 byte-neutrality re-runs | P5-3b |
| a `Sequence` proxy exposing `__getitem__` with **BOTH an int and a slice** overload (`Sections`, `section.py:267-286`) — the int returns one `Section`, the slice a `List[Section]` | the **interim explicit methods** `getitem_`/`to_array`/`len_` (the shared `RedefinesParen` 1-based `()` base is a **future WP**, VERIFY-COLLECTION — same TabStops/Styles precedent). `getitem_` keeps the **Python 0-based** key (H1: `+1` at the single `sectPr_lst` index; negative index counts from the end `i+n`; out-of-range→`mat2doc:IndexError` verbatim `"list index out of range"`) — the **Mat2Doc house convention, NOT** Mat2Ppt's 1-based `Slides` `()` (the toolboxes share no code by rule; `TabStops.getitem_` is the line-for-line precedent). The **slice** is carried as a `struct(start,stop,step)` (each scalar double or `[]`=None), detected via `isstruct(key)` (≡ `isinstance(key, slice)`); a private `sliceIndices_` is a **faithful port of CPython `slice.indices(n)` + `range(start,stop,step)`** (sign-dependent lower/upper `-1`/`n-1` vs `0`/`n`, negative clamp `max(x+n,lower)`, positive clamp `min(x,upper)`, exclusive stop via colon capped at `stop∓1`, then 0→1 `+1`), returning a `1×N` `Section` array (`1×0` when empty); step-0→`mat2doc:ValueError` verbatim `"slice step cannot be zero"`. `to_array` (`__iter__`) + `len_` (`__len__`) materialize / count `sectPr_lst`; H5 mints a FRESH `Section` per element | P5-3a |
| a computed `@property` **width UNION** where an integer attribute is a `Length` only under one `@w:type` value — `CT_TblWidth.width` (`table.py:408-418`): `@w:w` is typed `XsdInt` (a plain twips count, "only dxa values are used") and `width` returns `Twips(w)` **only** when `type=="dxa"`, else `None` (a pct/auto/nil width has no EMU length); the setter forces `type:="dxa"`, `w:=Emu(value).twips` | transcribe the union **verbatim, NOT normalized** (H6): the getter reads `type` **first** (so a required-attr-absent element raises `mat2doc:InvalidXmlError` from `width` too, faithful to Python `self.type`), then `~strcmp(obj.type,"dxa")`→`value=[]` (None) else `mat2doc.shared.Twips(obj.w)`; the setter `obj.type="dxa"; obj.w=mat2doc.shared.Emu(value).twips`. **Length-or-None ONLY for dxa** — every other `@w:type` yields `[]` on read, which is the faithful union, never a divergence. Byte-proven both ways (build-from-scratch serhex + parse-path round-trip): dxa 2880→1828800 EMU, pct/auto/nil→[], setter `Twips(1440)`→dxa/1440/914400 | P6-1 |
| a **table-authoring constructor** that builds a `<w:tbl>` from an emitted XML string with an **integer-floor per-column width** — `CT_Tbl.new_tbl(rows, cols, width)` (`table.py:191-197`) = `parse_xml(_tbl_xml(…))`, where `_tbl_xml`/`_tblGrid_xml`/`_trs_xml`/`_tcs_xml` (`:219-256`) emit a SPECIFIC pre-parse string and `col_width = Emu(width // cols) if cols > 0 else Emu(0)` (Python floor division), the twips written as `'%d' % col_width.twips` | transcribe the four builders **VERBATIM** (H11 child order tblPr→tblGrid→tr*): `//`→`floor(double(width)/cols)` (H6 — floor toward −∞, NOT `fix`; load-bearing on a negative width, `floor(-1000/3)=-334`), `Emu(0)` on `cols==0`; the `w:gridCol`/`w:tcW` `w:w` twips formatted via `mat2doc.shared.pyStr(col_width.twips, "int")` (H14 — never `num2str`/`sprintf('%g')`); f-string `\n`→`string(newline)` concatenation. Because `remove_blank_text` strips the pretty whitespace and the shared serializer is byte-proven, an identical pre-parse string yields identical post-parse bytes — byte-proven **7/7 across sizes** incl. the non-even `Inches(6.5)//7=849085` EMU→1337 twips floor spot (`references\s0065`, the permanent table-authoring oracle). NO `<w:tblStyle>` is emitted — the `"TableGrid"` style is API-tier (`Table.style`, P6-4a) | P6-3b |
| a public authoring method that shadows a **container** method of the SAME NAME but a **different third argument** — `Document.add_table(rows, cols, style=None)` (`document.py:150`, **style** 3rd; width comes from `self._block_width`, `:156`) vs `BlockItemContainer.add_table(rows, cols, width)` (`blkcntnr.py:61`, **width** 3rd, REQUIRED) — `Document` is NOT a `BlockItemContainer`, it delegates DOWN (`self._body.add_table(rows, cols, self._block_width)` then `table.style = style`) | port BOTH signatures **verbatim** — `+document\Document.m::add_table` declares `arguments … style = []` (Python `style=None`, H13) and computes the width itself via `block_width_()`; `+mat2doc\BlockItemContainer.m::add_table` takes a 3-arg **`width`** (a `Length`). The two are line-for-line faithful, so `d.add_table(2, 3)` (style-less, width=`_block_width`) and `body.add_table(2, 3, Inches(6))` (explicit width) are the two correct call shapes. **`d.add_table(2, 3, Inches(6))` is INVALID** — `Inches(6)` is bound to `style`, and python-docx raises `KeyError "no style with name '5486400'"` (oracle-confirmed); a doc example that wants an explicit width uses the container level, never a 3rd positional on `Document`. First byte-proven end-to-end at P6-4a (`document.add_table(2,3)`→`document.xml` `a1eda043…`, byte-identical to python-docx) | P6-4a |
| a **cell-merge authoring method** returning a NEW proxy over the merged element — `_Cell.merge(other)` (`table.py:237-245`) = `tc = self._tc; tc_2 = other._tc; merged_tc = tc.merge(tc_2); return _Cell(merged_tc, self._parent)` — a thin wrapper over the byte-proven `CT_Tc.merge` engine (writes `w:gridSpan` / `w:vMerge`) | port the wrapper **verbatim** (`+table\Cell_.m::merge`): `tc = obj.tc_; tc_2 = other_cell.tc_; merged_tc = tc.merge(tc_2); merged = mat2doc.table.Cell_(merged_tc, obj.parent_)`. The `CT_Tc.merge` engine (P6-3a, risk-register #2) does all the work — horizontal `gridSpan`, vertical/block `vMerge` (`restart` on top + **bare `<w:vMerge/>`** continuation), the `mat2doc:InvalidSpanError` "requested span not rectangular" on a non-rectangular region. `cell1.merge(cell2)` through the full public API is **byte-identical** to python-docx across every geometry (horizontal `9f626e2b…`, vertical `e833fac8…`, 2×2 block `ac155531…`, merge-then-text `c96a3351…`, 3×3 mixed `63b25b1a…`, each part-level 17/17 L1) AND **COM-verified** in real Word (all merge shapes open clean — `com_verify_P6_tables.md`) | P6-4b |
| the **cells-grid-walk span identity** — `Table._cells` / `_Row.cells` (`table.py:163-180`/`:395-438`) build the grid by REPEATING one `_Cell` reference at every grid position a spanned cell covers (`cells[-col_count]` / `cells[-1]` re-append the SAME object `grid_span` times; a `vMerge="continue"` continuation `yield from iter_tc_cells(tc._tc_above)` recurses to the span root), so `table.cell(r,c) is table._cells[…]` is the SAME object at each covered position — **H5** | `Cell_` is a **handle class**, so appending the ONE handle `grid_span` times (and recursing to `_tc_above` via the private `iter_tc_cells_` for a vMerge continuation) reproduces span identity exactly: two accesses of a spanned position compare `==` (`row0(1) == row0(2)` for a merged row), an unmerged row's cells are `~=`. `cells(end-col_count+1)` / `cells(end)` — MATLAB evaluates the RHS `end` against the PRE-growth array (exact `cells[-col_count]`/`cells[-1]` semantics). The 3×3 mixed-merge partition `[0,0,2,3,3,5,3,3,5]` (first-identity index repeated) is proven at the handle level AND byte-companioned by `s0082` (text through a NON-root position lands in the span-root `<w:tc>`, 17/17 L1). **VERIFY-2:** each `cells`/`cell(…)`/`*_cells` access mints FRESH wrappers → cross-access `~=` (== Python's recomputed-`@property` `is`-False); within one access identity holds; bytes unaffected — NOT a D# | P6-4b |
| a **[R] re-port of a pptx package that carries pptx-only PIL seams** — Mat2Ppt's `+image` is itself the docx `image/` port, but its Gate-3 oracle was python-pptx/**PIL**, so it grew members python-**docx** does NOT have (the WMF/EMF format + a `pil_dpi`/`int_dpi` re-oracle seam) — a re-port TRAP: copying verbatim would smuggle non-docx behavior into Mat2Doc | **EXCLUDE the pptx-only seams (T3), do not transit them** — verified ABSENT by grep (`wmf\|x_wmf\|pil_dpi\|int_dpi\|\bemf\b` → 0 hits): **no `Wmf.m`**; the **2 WMF/EMF `SIGNATURES` rows dropped** from `ImageHeaderFactory_.signatures_` (exactly the 8 docx rows, exact upstream order); **`MIME_TYPE.X_WMF` dropped**; the `pil_dpi` method dropped from BOTH `Image.m` and `BaseImageHeader.m`; the `int_dpi` round+`[1,2048]`-clamp (which lives in pptx `parts/image.py`, not docx) never created. Package-visibly pinned: a WMF placeable header (`D7 CD C6 9A`) / EMF (`01 00 00 00`) → `mat2doc:UnrecognizedImageError` on BOTH sides (docx has no such parser — the pptx PIL behavior did NOT transit; `s0083` 5/5) | P7-1a |
| **the dpi is docx math, NOT Mat2Ppt's PIL re-oracling** — a re-port REVERSION that bites the FORMAT parsers, not the core. Mat2Ppt deliberately fixed 3 dpi sites away from docx: `Bmp.dpi_` (PIL `px_per_meter/39.3701`, ppm==0→**72**), `TiffParser_.dpi_` (PIL joint-axes), Exif dpi (separate PIL algo) | in the CORE there is **nothing to revert** — `Image.horz_dpi`/`vert_dpi` are pure header delegation (`image.py:88-102`, no arithmetic) and `BaseImageHeader` has **no `default_dpi`** member (the docstring "72" is realized inside each parser). The reversion is a standing **P7-1b/P7-2 porter obligation**: `Bmp._dpi` = `int(round(px_per_meter * 0.0254))`, ppm==0 → **96** (`bmp.py:37-43`); `_TiffParser._dpi` **per-axis**, tag-absent → **72**, unit==1 → 72, cm → ×2.54 (`tiff.py:88-108`); `_App1Marker` (Exif) dpi straight from the embedded TIFF parse — each probed against the **python-docx** oracle, not PIL. `round()` goes through the co-located `+image\private\pyRound` (banker's half-to-even, H6) | P7-1a |
| the CPython `os.path.splitext(fn)[1][1:]` extension semantics for `Image.ext` (`image.py:70`) — the **F-1** trap: MATLAB `fileparts` gives `.bashrc`→`"bashrc"` where CPython gives `""` (leading-dot basenames are not a split point) | a package-private `+image\private\splitext_ext.m` replicating CPython `genericpath._splitext` EXACTLY — operate on the **basename** (last `/` or `\`), find the LAST dot, require ≥1 **non-dot** char before it (a leading dot-run is not a split point), return the chars after the dot (else `""`), **no lowercasing** (case preserved). `Image.ext` calls it, NOT `fileparts`. Output-visible at P7-4 (`ext` feeds the `ImagePart` partname `/word/media/imageN.<ext>`), so this is a fix, not a deferral: `.bashrc`/`.png`/`..png`/`...`→`""`, `IMG.PNG`→`"PNG"`, `.tar.gz`→`"gz"`, `/x/y.z/a.png`→`"png"`. Removes a latent divergence rather than adding one — **no D#** (23/23 through live `Image.ext`) | P7-1a |
| **the format-parser dpi is docx `int(round(px * 0.0254))`, NOT Mat2Ppt's PIL re-oracling** — the P7-1b REVERSION landing at `Bmp._dpi` (`bmp.py:37-43`): Mat2Ppt's Gate-3 oracle was python-pptx/**PIL**, so its `Bmp.dpi_` carried a PIL contract (`px_per_meter / 39.3701`, `ppm == 0 → 72`) — a re-port TRAP: copying it verbatim would smuggle non-docx dpi into Mat2Doc | **REVERT to the literal docx math** — `Bmp.dpi_(ppm)` = `96` when `ppm == 0` (the BMP default, **not** PIL's 72), else `fix(pyRound(ppm * 0.0254))` (`int(round(..))` with the `0.0254` metre-to-inch constant, CPython **round-half-to-even** via `+image\private\pyRound`, NOT MATLAB's half-away `round()`; H6/H14). Mat2Doc's value oracle is **python-docx, not PIL**. **Tie discriminators (the reversion guard, frozen `s0084`):** `0→96` (PIL→72), `2500→64` (PIL→63), `7500→190` and `17500→444` — the two floor-EVEN exact ties (`190.5`/`444.5`) where MATLAB `round()` gives 191/445. `PngParser_.dpi_` uses the same formula (units==1→dpi, else 72; `px_per_unit == 0 → 72`, H4); `Gif` dpi is unconditionally 72 (no computation). The Mat2Ppt `D-bmp-dpi` PIL deviation is fully stripped → **the docx math carries NO deviation, 0 new D#** (`s0084` probe 143/143; 17-file corpus 170/170 value-identical to `Image.from_file`) | P7-1b |

## Namespace policy

Mat2Doc uses the `mat2doc:` error-identifier namespace and the `mat2doc.*`
package namespace throughout — **no shared code with Mat2Ppt**. Designs common
to both toolboxes (the `Length` family, `pyIntArg`) are re-implemented, not
copied. The P0 same-package name-collision scan found **zero** collisions, so
**no symbol was renamed** (FLAG-3-docx resolved by convention).
